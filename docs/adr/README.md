# Architecture Decision Records

Lightweight records of significant technical decisions. Format: Status / Context /
Decision / Consequences. New decisions take the next number.

| ID   | Title                                        | Status   |
|------|----------------------------------------------|----------|
| 0001 | Core stack & identity model                  | Accepted |
| 0002 | Deployment topology: one instance per neighborhood | Accepted |
| 0003 | Roles & moderator scoping                    | Accepted |
| 0004 | Data persistence & schema evolution          | Accepted |
| 0005 | Multilingual support (UI & static pages)     | Accepted |
| 0006 | Module boundary contracts (Identity, UserInfo, Authorization) | Accepted |
| 0007 | Group management lane: owner ∪ GlobalAdmin for add and remove   | Accepted |
| 0008 | Member self-leave lane (owner excepted)   | Accepted |
| 0009 | Group description: resident-facing display + owner ∪ GlobalAdmin edit | Accepted |
| 0010 | Private groups: a membership/organizing unit, hidden from the audience pickers | Accepted |
| 0011 | Media & file storage: content-addressed local-volume bytes + a `mt` catalog | Accepted |
| 0012 | Community membership: mandatory communities + moderator-managed optional membership | Accepted |
| 0013 | Group posts: a membership-scoped group channel | Accepted |
| 0014 | Post edit lane: author-only (no moderator/admin branch) | Accepted |
| 0015 | UI view-localization mechanics (TagHelper + curated registry) | Accepted |
| 0016 | Group-post + reply edit lane: author-only (no moderator/admin branch) | Accepted |
| 0017 | Announcement edit lane: author-of-record ∪ GlobalAdmin (flat lane) | Accepted |
| 0018 | UGC authored-in language tag (posts, replies, announcements) | Accepted |
| 0019 | Timezone: platform default (admin) + per-resident override | Accepted |
| 0020 | Date & time format: platform default (admin) + per-resident override + custom | Accepted |
| 0021 | Translator role: delegate translation editing to non-admin residents | Accepted |
| 0022 | User-added post & reply translations (the lane ADR 0018 deferred) | Accepted |
| 0023 | Reply as a report target (extending the M3b report lane) | Accepted |
| 0024 | Author soft-delete lane (posts + replies, community + group) | Accepted |
| 0025 | Rich content: Markdown + content images | Accepted |
| 0026 | Group & community name/description translations | Accepted |
| 0027 | Post & reply translation display & swap: authored-in as a first-class variant + click-to-swap | Accepted |
| 0028 | Guardian controls: account-scope supervision of a child's account (suspend, communities/groups, invitation approval; no content reads) | Accepted |
| 0029 | Announcement user-added translations (GlobalAdmin/Translator any; community-Moderator their community's; add-only) | Accepted |
| 0030 | Role independence: elevated roles (GlobalAdmin / Moderator / Translator) are composable per resident, not mutually exclusive | Accepted |
| 0031 | WYSIWYG editor + toolbar over the RC Markdown lane (split-view live preview + Markdown-splice toolbar, `tsc`-only; Amends 0025) | Accepted |
| 0032 | Inline editor: the rendered pane is the default view (source hidden behind one toolbar toggle; pane stays visible as a split view when revealed, `tsc`-only, additive; Amends 0031) | Accepted |
| 0033 | WYSIWYG inline editing: the rendered pane is the editable surface (`contenteditable`); the Markdown source is a read-only mirror; the serializer is the inverse of `renderPreview`; the sanitizer strips paste to the pinned subset; `tsc`-only, no editor dependency, `Body` as Markdown, one renderer on the read path (Amends 0031 — the "hard non-negotiable" reversed by user approval 2026-09-15; Amends 0032 — the rendered-by-default view kept, now editable) | Accepted |
| 0034 | File attachments lane: downloads on post / reply / announcement (separate route + allowlist, `Content-Disposition: attachment`; reuses the ADR 0011 store; Amends 0025 + 0011) | Accepted |
| 0035 | Group-post media: the membership lane is the serve decision (the "single `Read` decision" for a group post's image/attachment is the ADR 0013 membership lane, not the audience lane; one shared routing seam `PostReadDecision`; Amends 0025 + 0034) | Accepted |
| 0036 | Community-visible audience: the default for new community posts (a new `Audience.Community` flag — a distinct grant, not a mode of the grant list; a 4th `Decide()` branch; `AccessVia.Community`; the composer seeds it `true`; the granular picker is hidden by default; group posts structurally unaffected; the C1 empty-audience-denies invariant still governs only the grant list) | Accepted |
| 0037 | Draft mode: saved-but-not-published, author-only (a first-class `IsDraft` state on community post / group post / announcement — excluded from every feed and the pinned list, detail gated to the author *before* the audience decision, an author-only idempotent publish seam that denies a `GlobalAdmin` non-author, no audit row, and a `/my/drafts` index for the author) | Accepted |
| 0038 | Guardian assignment: an existing guardian assigns a second guardian to a child's account (email-driven; one `IIdentityService` ADD + one `GuardianController` action + the Detail view's assign form; Amends 0028's G·4 non-decision for the existing-guardian case) | Accepted |
| 0039 | Pages: a hierarchical, audience-restricted, translatable knowledge tree (a `Page` + `PageTranslation` doc in a new `Kumunita.Core.Pages` context — reuses the `Audience` doc, the frozen `IAuthorizationService` via a `PageToAuditableResource` adapter, the ADR 0022/0027/0029 translation + display lane, the ADR 0025/0031/0033 WYSIWYG editor, the ADR 0025/0034 media idiom; a `MountPoint` string for UI slots; absorbs and retires the `LocalizedPage` static-page lane; Amends 0005 + 0018 + 0022 + 0026 + 0027 + 0037; the `PG` named lane) | Accepted |
| 0040 | Pages: system vs user (blog) kinds — a `PageKind` field (default `System`); the ADR 0039 §3.7 Moderator lane on pages is retired (system = GlobalAdmin only; blog = author ∪ GlobalAdmin; translate = GlobalAdmin ∪ Translator on either kind; a Moderator has no page standing at all); the two namespace guards + the blog-ownership guard; the (Kind, AuthorId)-scoped root-slug guard; the seeder's `system/` root + re-parent + kind normalization; the read-only, draft-gated `GET /blog` + `GET /blog/{userId}` per-resident feed; the kind-differentiated composer parent picker; Amends 0039 (retires §3.7's Moderator lane) + 0006; additive on 0004 + 0037; a follow-on refinement within the open `PG` lane — the roadmap stays unchanged | Accepted |
| 0041 | Page audience scope dropdown: "All residents" as a first-class flag (a new `Audience.AllResidents` flag — a distinct grant, not a mode of the grant list; a 4.5th `Decide()` branch between Community and Public; `AccessVia.Resident`; the page composer's community select retired in favor of a top-level `Scope` dropdown (All residents / a community / Individual access) with the granular editor revealed only for Individual; the post / announcement / group lanes structurally unaffected; the C1 empty-audience-denies invariant still governs only the grant list; Amends 0039 — the audience model on pages; additive on 0004 + 0036) | Accepted |
| 0042 | Bundled initial language pack: `de` / `fr` ship on first boot (a `SeedLanguages` seeder step — create-if-missing, idempotent, never overwriting a resident edit; Amends 0005 §B — the "every other language is community-provided" scope; additive on 0005 + 0015) | Accepted |
| 0043 | System pages shipped + discoverable (the four canonical pages — `/terms` `/help` `/privacy` `/conduct` — are seeded as `PageKind.System` rows on first boot; a read-only `GET` surface; the seeder is create-if-missing, idempotent, never overwrites a resident edit; Amends 0005 §B — the static-page coverage on a fresh instance; additive on 0039 + 0040 + 0042) | Accepted |
| 0044 | Tags: free author-set subject labels for posts and blog pages (a `Tag` + `TagTranslation` doc in a new `Kumunita.Core.Tags` context — the ADR 0011 shared-id-doc shape, referenced by `Post.TagIds` / `Page.TagIds` the way `ImageIds` references `MediaObject`; `Slug` is the language-neutral business key, `Name` display; attach = the object's existing edit standing (free), translate = the tag's creator ∪ GlobalAdmin; one access-scoped read seam serves the tag list + by-tag browse + autocomplete (all over "content the viewer may already read," so a tag never leaks a subject behind unread content); blog pages only, `System` pages out; Amends nothing — additive on 0004 + 0005 + 0013 + 0022 + 0026 + 0025 + 0040; the `TG` named lane) | Accepted |
| 0045 | Danish (`da`) pre-seeded, disabled by default (a `SeedLanguages` seeder step for `da` — create-if-missing, idempotent, `IsEnabled = false` on first boot; a GlobalAdmin toggles it on; Amends 0042 — the bundled initial pack; additive on 0042 + 0005 + 0015) | Accepted |
| 0046 | Default-language fallback: browser `Accept-Language` match (a new step in the ADR 0005 §C resolution chain — between the platform default and the hard-coded `en` floor, the viewer's `Accept-Language` header is matched against the enabled languages; the matched language becomes the default for that viewer; Amends 0005 §C — the resolution chain; additive on 0005 + 0042 + 0045) | Accepted |
| 0047 | Static-page localization: the four hard-coded routes (`/terms` `/help` `/privacy` `/conduct`) render the page body in the request's **effective language** (the matching `PageTranslation`, the `en` body as the floor — via a new `IPageService.ResolvePageAsync` one-read seam) + a **warm-boot page-translation backfill** (the four canonical pages' `de` / `fr` / `da` rows, create-if-missing, idempotent, never overwriting an admin edit or the `en` body); Amends 0043's "en-body by contract" Consequence (superseded — now the floor) + 0042 D1's "no warm-reseed mechanism" (narrow, scoped exception — the four pages' translation rows only); additive on 0046 + 0039 + 0040 + 0042 + 0045 + 0015; the `SL` named lane | Accepted |
| 0048 | Edit + remove lanes for user-added translations: lifts the add-only pin on every user-added translation surface (posts, replies, announcements, group/community name/description, pages) — an **update** and a **remove** lane per row, same parent, same language, same standing matrix the add lane already fixed; Amends 0022 + 0026 + 0027 + 0029 (the add-only shape); additive on 0021 + 0039 | Accepted |
| 0049 | Default-visible variant: the viewer's current language (when a translation exists) — a UGC post/reply/announcement/page that already carries a human-added translation in the viewer's resolved language renders that variant first (the authored-in variant demoted to a one-click swap, never removed); Amends 0005 §C (the "never automatic" display clause) + 0027 (the "always default-visible" authored-in clause); additive on 0046 (reuses the `Accept-Language` resolution chain, does not change it) | Accepted |
| 0050 | Admin-managed sign-up gate: open vs invitation-only (a `LocaleSettings.IsSignupOpen` boolean, additive on the ADR 0005 B singleton, `true` floor — read floors to open on a missing row; one `IIdentityService` ADD: a plain read + a single audited write lane; a GlobalAdmin-gated `/admin/signup` surface mirroring ADR 0019/0020; the gate is authoritative on the GET *and* POST of `/account/signup`, and the nav/login sign-up affordances are hidden, not disabled; the invitation mechanism itself is out of scope; Amends the README *Deferred* + SECURITY.md §6 A2 open item — it lands the control both name; additive on 0004 + 0005 + 0019 + 0020) | Accepted |
| 0051 | List-surface variant default: the viewer's current language (when a translation exists) renders first in the post/reply/announcement lists and the group/community index cards — the authored-in variant demoted to a one-click swap, never removed; Amends 0049 (its "out of scope" clause for the list surfaces); additive on 0029 + 0022 + 0026 (the rows already exist) | Accepted |
| 0052 | Warm-boot UI-string baseline backfill: the closed registry's `de` / `fr` / `da` `TranslationResource` rows are backfilled on every warm boot (create-if-missing, idempotent, never overwriting an admin in-place edit, `en` never touched) — the ADR 0042 D1 "new-key asymmetry" closed for the UI-string surface, the second named warm-boot backfill lane alongside ADR 0047 D2's page-translation lane; Amends 0042 D1 + 0047 D2 (widens the recorded exception from the four pages' rows to those rows + the UI-string baselines); additive on 0045 + 0015 | Accepted |
| 0053 | Community translation surface: its own page (a dedicated per-community translation editing surface) | Accepted |
| 0054 | M4 — Events, RSVPs & reminders: a new `Kumunita.Core.Events` context (`Event` + `EventRsvp` docs, an `M4DocTypes` surface) reusing the frozen seams — the `Audience` doc (0001-B / 0036), the `IAuthorizationService` via a new `EventToAuditableResource` adapter (0006), the one `MarkdownRenderer` + `bindRichEditor` (0025 / 0031), the RC/ATT media idiom (0025 / 0034), the `kw-dt` TagHelper (0019 / 0020), the author ∪ GlobalAdmin standing matrix (0014 / 0016 / 0017), the draft / tag / language / soft-delete lanes (0037 / 0044 / 0018 / 0024), and the `IMailerStage` + `OutboxEmail` + durable-handler trio for the `EventReminders` §6.4 job (the `AuditPurgeHandler` self-rescheduling `TimeoutMessage` precedent); `EventRsvp` is last-write-wins per `(EventId, UserId)`, no audit row; the reminder idempotency key is `remind:{eventId}:{userId}`; 25 pinned seam tests + the three-test acceptance gate; the `Body` / `ReminderEnabled` naming settles (ARCHITECTURE.md §5 sync lands in the close unit); PG closes, M4 opens as the single in-progress milestone (the roadmap order is recorded in the ADR's Context) | Accepted |
| 0055 | Development sample data: a first-boot, Development-only mock neighborhood (a `SeedSampleData` seeder step, gated on the `Development` environment) | Accepted |
| 0056 | Sample-data opt-in: a flag-gated seeder with a deploy posture (a `SampleData__Enabled` setting so the Development-only sample neighborhood is an explicit opt-in, with a recorded posture for a deliberate production use; Amends 0055's environment gate) | Accepted |
| 0057 | User guides: resident-facing how-to guides in the `help/` subtree of the page tree (a closed, code-owned, `en`-only registry in the seeder — a `Page` doc under the existing `help` page, reusing the editor / renderer / translator / authorization; a three-layer consistency loop: **where** — in the tree, **when** — the lane's Definition of Done ships the guide update, **how often** — a periodic GlobalAdmin review (OPS §13); non-`en` bodies community-owned through the existing translation lane, never machine-translated; additive on 0005 + 0039 + 0040 + 0043; the `UG` named lane) | Accepted |
| 0058 | Page "Reset to seeded text" lane: a confirmed, destructive edit-lane action that overwrites a page's `en` title/body + `de`/`fr`/`da` translation rows with the code-owned seeded baseline (the same seeder registries a fresh instance ships), so an admin can pull in the latest shipped help-page text and re-customise on top — the edit standing (0040 §3.7) verbatim, a `page.reset` audit action, `HasSeededText` gates the button's availability, one `SaveChangesAsync`; additive on 0039 + 0040 + 0042 + 0043 + 0057; the `RS` named lane) | Accepted |
| 0059 | Event user-added translations: the ADR 0022/0029/0048 user-added translation lane carried to the M4 events surface — an `EventTranslation` doc (title + body) on `M4DocTypes` with a `(EventId, LanguageCode)` unique index, four `IEventService` seams (read / add / update / remove) on the M4 self-composed-session convention (0054 §4), the author ∪ Translator ∪ GlobalAdmin standing (0021 / 0030, no component-moderator branch — 0054 §5), one `eventtranslation.*` audit row per write, the ADR 0027 chip-swap + ADR 0049 default-visible variant on the detail page, and the ADR 0051 viewer-language selection on the `/events` feed; Amends 0054 (its "translation lane is a follow-on lane with its own ADR" note) | Accepted |
| 0060 | Event-translation sample backfill: a **warm-boot** lane for the sample neighborhood's events — the four sample events' `de` / `fr` / `da` `EventTranslation` rows (ADR 0059) are backfilled on every warm boot of a `SampleData__Enabled` instance (create-if-missing, idempotent, never overwriting a Translator's in-app edit, the `en` authored-in row never read or written; `AuthorId` empty on backfilled rows) — the third named warm-boot backfill exception (ADR 0047 D2 pages + ADR 0052 UI strings + this), differing in being **flag-gated** (sample-data-specific, unreachable on a real neighborhood by construction); Amends 0056 (adds the warm-boot lane; the pristine seed path unchanged) + 0059 (the backfill is never the author/Translator lane); additive on 0047 D2 + 0052 + 0042 D1 | Accepted |
| 0061 | Email & notification language: per-resident outbound-channel language | Accepted |
| 0062 | The /admin surface split: overview dashboard + linkable section pages | Accepted |
| 0063 | The events calendar (`EV-CAL`): a month-anchored, display-only overview of the caller's visible events over a rolling 30-day window at `GET /events/calendar` (overlap pairs highlighted client-side, prev/next/today navigation, the `componentId` filter reused from the feed); one additive read seam on `IEventService` (`ListInRangeAsync` — the candidate filter + `CanSeeAsync(Read)` gate + one aggregate `AccessAudit` row mirroring `ListUpcomingAsync`) + one view + one `client/lib` TS module (tsc-only, no dependency) + the `events.calendar.*` key block; display-only — zero document / schema / seeding changes; Amends 0054 (extends the `IEventService` seam; the M4 surface otherwise untouched); additive on 0019 / 0020 (the timezone / format machinery the anchor + chip times use) + 0015 (the `kw-l` key registry) + 0031 (the plain-TS / no-dependency pin); no amendment to 0006 (no seam on the frozen cross-context interfaces); the `EV-CAL` named lane | Accepted |
| 0064 | The events calendar Day/Week/Month views (`EV-DWM`): the three named views over the same authorized events at `GET /events/calendar?view=day|week|month` (a time-ruler day grid, a Monday-start week grid, the calendar-month grid as the default) — additive and display-only: one `?view=` param, two additive view-model fields, one new plain-TS `client/lib` module (tsc-only, no dependency), a few `site.css` rules, 3 `events.calendar.view.*` keys; the `ListInRangeAsync` seam is *reused, unchanged* (it is already window-agnostic); Amends 0063 (the `EV-CAL` calendar this extends); additive on 0019 / 0020 + 0015 + 0031; no amendment to 0006 (no seam on the frozen cross-context interfaces); the `EV-DWM` named lane | Accepted |
| 0065 | The `/events` feed's "Your upcoming events" section (`EV-MINE`): the signed-in viewer's own events (their RSVPed events, any `RsvpStatus` — the row's existence is the sign-up — ∪ their authored events; upcoming + live only, drafts included — the union is non-leaky by construction) rendered above the feed, only when non-empty; one additive read seam on `IEventService` (`ListMineAsync` — no per-row `CanAsync`, no `AccessAudit` row — the `GetMyRsvpAsync` posture) + one additive view-model field (`EventIndexViewModel.MyEvents`, default-empty) + one view section (reusing the feed's `airy-ann-*` markup) + 2 `events.mine.*` keys × 4 languages; Amends 0054 (the M4 events surface; the seam extends it); additive on 0015 (the `kw-l` key registry) + 0059 (the translation swap the section's rows reuse); no amendment to 0006 (no seam on the frozen cross-context interfaces); the `EV-MINE` named lane | Accepted |
| 0066 | Event calendar color: an author-picked chip/block background per event | Accepted |
| 0067 | M5: Projects (to-dos + Kanban boards) — a new `Kumunita.Core.Projects` context reusing the frozen authorization / audience / renderer / editor / standing seams; the `IProjectService` surface frozen (§2.3) | Accepted |
| 0068 | Lane action menu: rename / limit / status / move + add-to-do foot; Amends 0067 | Accepted |
| 0069 | Kanban board: drag-and-drop moves, fixed statuses, add-lane; Amends 0067 | Accepted |
| 0070 | Board edit lane (title + description) + board polish: one additive `IProjectService` seam (`UpdateBoardAsync` — `Title` + `Description` full update, blank description→null, `Modified` stamped on a real change only, `board.update` audit row, the `CheckBoardStanding` creator ∪ GlobalAdmin matrix); the board edit page (`GET /projects/boards/{id}/edit` + `POST /projects/boards/{id}` — audience/community/language remain creation-time choices); borderless ⋮ triggers (`.kanban-glyph-btn`); the add-lane form top-aligned; Amends 0067 (extends the frozen `IProjectService` surface — the frozen-surface rule honoured) | Accepted |
| 0071 | Kanban lane / card editing: modals + autosubmit status + menu dividers: the lane head's single ⋮ menu reshaped — **Rename** is a button opening a Bootstrap modal (a single `Title` input + Cancel/Save; the repo's **first** Bootstrap modal, setting the convention); **Max items** stays inline with a quiet → arrow submit (`.kanban-arrow-submit`); **Status** auto-POSTs on change (blank → None) via a `change` listener in `client/lib/projects-board.ts` (tsc-only, the existing `postAndRedirect` CSRF shape, server stays authoritative); Move left/right after a divider only when a neighbor exists. The card ⋮ menu's **Add subtask** row is a button opening a modal (Title + optional Status + Assignee select, the standing options seeded by an additive `SeedGrantPickerOptionsAsync()` on the board `GET`) — and the **same "Add subtask" modal** is applied to the to-do list + detail pages' ⋮ menus (`TodosIndex` / `TodoDetail`, each `GET` gaining the same additive seed call; `TodosIndex` renders one modal per row, `TodoDetail` a single one). `<hr>` dividers (`.kanban-divided-menu`) group both menus' functions; no new controller/service seams — the ADR 0068 lane-update + reorder routes and the to-do subtask route reused as-is; Amends 0068 (the lane edit affordance) + 0069 (the card menu shape) | Accepted |
| 0072 | Kanban board: the placeholder attributes joined the localized surface: the three board input `placeholder`s (add-to-do "To-do title", add-lane "New lane title", New-board initial-lane "Planned") are registered in `KnownTranslationKeys` (all four languages, parity pins kept) and resolved via `Translation.GetAsync` in the view block — the sanctioned `@optionalPlaceholder` pattern generalized; a **documented exception** to ADR 0015's HTML-attribute exclusion (narrow: simple, value-free placeholder copy only; the drift guard for value-interpolating attributes still holds); view + registry only, no controller/service/JS seam; Amends 0015 (the exclusion) + 0067 (the board surface) | Accepted |
| 0073 | To-do group/community claim lane: an unassigned to-do addressed to a **group** (an `Audience` group grant) or a **community** (`ComponentId`) can be picked up by a member of that audience — a new write lane `IProjectService.ClaimTodoAsync` (standing = unassigned + member of an addressed group ∪ resident of the to-do's community, probed through the frozen `IUserInfoService` membership seams; one `todo.claim` audit row via the **existing** `AccessVia.Group` / `AccessVia.Community`, **no** new `AccessVia`, **no** `Decide()` branch, C-M5·11); a `Claim` affordance on the feed + detail (CSRF `POST /projects/todos/{id}/claim`) + a "Claimable / Unassigned" badge; an `unassignedOnly` feed filter (**filter, never a gate**); **zero schema change** (reuses the `Audience` doc + `ComponentId`, `M5DocTypes` untouched); three new `en`/`de`/`fr`/`da` keys (parity pins kept); Amends 0067 (the frozen `IProjectService` surface — the frozen-surface rule honoured) + 0013 / 0036 (the group / community standing) + 0012 (the feed non-leak invariant) + 0006 (the write-lane audit rule) | Accepted |
| 0074 | Kanban card "Assign to…": person, group, or community — the card ⋮ menu's new always-first "Assign to…" row opens a per-card Bootstrap modal (the ADR 0071 convention) with a single `assigneeId` `<select>` over three optgroups — **People** (seeded `Audience_Users`), **Groups** (seeded `Audience_Groups`), **Communities** (seeded `Assign_Communities`, the enabled components) — led by Unassigned (clears the assignee); it posts the **existing** assign route (ADR 0067, `POST /projects/todos/{id}/assign` — standing = creator ∪ assignee ∪ GlobalAdmin, **unchanged**; `AssigneeId` is display + standing, never a gate — C-M5·3) with a new optional **same-site** `returnUrl` form field (checked by `Url.IsLocalUrl`; never followed externally) so the flash lands back on the board; the board's card `AssigneeDisplayName` now resolves across profiles / groups / components (a read surface, never a gate); the assignee may be a **group** id or a **community** id — the write-side counterpart of ADR 0073's claim lane — with **zero new service seams** (the frozen `IProjectService` surface untouched) and **zero schema change** (`M5DocTypes` untouched); five new `en`/`de`/`fr`/`da` keys (parity pins kept); Amends 0071 (the card menu + modal convention) + additive on 0067 (the assign route) + 0073 (the group / community semantics) + 0010 (public groups only as grant targets) + 0015 / 0072 (the `kw-l` registry + attribute-exception) | Accepted |
| 0075 | Image editor (`IM`): client-side **crop + resize** for avatars and inline post images — a new `tsc`-only module (`client/lib/image-editor.ts`, the `rich-editor.ts` / `avatar-upload.ts` convention) offers a Bootstrap modal (drag-to-move, corner-to-resize, width presets, live size readout); the output is a re-encoded `Blob` whose **stored** dimensions are the chosen width — **no** per-image display-width metadata on `MediaObject`, **no** change to the frozen `MarkdownRenderer` / `renderPreview` / `dom-to-markdown` / `IsSafeImageSrc` (the stored size *is* the display size, IM·1). Two call sites: the **avatar form** (a guarded self-wire intercepts `change`; on Apply it swaps the input's file for a `File` wrapping the edited `Blob` via `DataTransfer`, so the native `POST /profile/avatar` submits the edited bytes) and the **editor Image button** (`rich-editor.ts` calls `openImageEditor` before the upload; on Apply the edited `Blob` is appended to the `FormData` with the original filename). **Always optional** — "Use original" / close resolves `null` → the unedited file is uploaded (IM·3). The content-addressed store (ADR 0011) does the rest for free: an edited `Blob` is a **new** payload → **new** SHA-256 → **new** `MediaObject`, so the `MaxBytes` + `IsAllowed` guards, `IsSafeImageSrc`, the `![alt](/content-image/{id})` serializer, and the audit row all keep working against the id (IM·2). **Zero server change**: no new route, no new `.csproj` package (no ImageSharp / SixLabors), no new `IMediaStore` method, `Kumunita.Core` stays HTTP-free, `package.json` stays `typescript`-only. Pure geometry (`clampCropRect` / `centerSquareCrop` / `outputDims`) is pinned in C# (`ImageEditorTests`, the `RichEditorTests` spec-mirror + artifact-pin convention). Amends **0025** (resolves its "server-side image transforms" non-decision as a client-side lane) + 0011 (media/file storage, the content-addressing) + 0031 (WYSIWYG, the `tsc`-only pin) | Accepted |
| 0076 | M6: Notifications (inbox + recipient email) — a new `Kumunita.Core.Notifications` context (`Notification` + `NotificationPreference` docs, an `M6DocTypes` surface, a closed nine-kind `NotificationKinds` set, the `NotificationService.EmitAsync` writer + inbox / unread / preference lanes) reusing the frozen durable-email / translation / personal-read seams — no new `AccessAction` / `AccessVia` / `IAuditableResource` adapter; the inbox is the durable record, the email the best-effort nudge; the recipient's `Profile.EmailLanguage` is the outbound-channel language (ADR 0061); a 30-second poller bell (plain `tsc`-only TS); 12 FACES + the three-test acceptance gate | Accepted |
| 0077 | Admin account notifications: notify every GlobalAdmin when a resident signs up or verifies (a new notify gate + two admin-lane kinds `account.signup` / `account.verified` on the frozen M6 emitter — the nine-kind set becomes an eleven-kind set; a `NotifyAdminsOnSignup` field on the `LocaleSettings` singleton; twelve new keys × four languages; the two emission points hook in *before* the single `SaveChangesAsync`); Amends 0076 + 0005 / 0015 + 0004 + 0019 / 0020 + 0001 | Accepted |
| 0078 | Sample-account notification suppression in production — a single environment-aware gate on `NotificationService.EmitAsync` (a new additive `NotificationOptions.SuppressForSampleAccountsInProduction` property, host-bound to `!IsDevelopment()`) that is a complete no-op (returns null, no inbox row, no staged email, no translation lookup) when the recipient's profile e-mail is in the code-owned `SampleDataSeeder.SampleAccountEmails` closed set; Development is untouched (Mailpit collects the mail, sample accounts behave like real residents); `EmitAsync`'s return widens to `Task<Notification?>`; Amends 0076 (the M6 surface — no signature change) + 0055 / 0056 (the sample-data seeder + the deploy posture's outbound-channel consequence) | Accepted |
| 0079 | To-do optional start and due dates — a to-do (M5, ADR 0067) gains two optional `DateTimeOffset?` fields (`Start` / `Due`) reusing the M4 Event date idiom verbatim (stored UTC, rendered by the one `<kw-dt>` TagHelper in the viewer's effective timezone, ADR 0019; `type="datetime-local"` inputs bound model-side to nullable `DateTimeOffset?`), but **made optional** — the Event requires both, the to-do leaves both blank by default; the create / update request DTOs gain the two matching optional fields and the standing matrix is untouched; Amends 0067 (the M5 to-do surface) + 0054 (the M4 Event date shape, reused but optional) | Accepted |
| 0080 | The /settings surface split: four linkable section pages — the resident's settings (ADR 0005 B, ADR 0019, ADR 0020, ADR 0061 sections) was one long scroll at `/settings/language`; it is split into four linkable GET routes on `LocaleController` (Language `/settings/language`, Time zone `/settings/timezone`, Date & time format `/settings/dateformat`, Email & notification language `/settings/email-language`) sharing one `LocaleSettingsViewModel` (built by a single `BuildModel()`), a shared `_SettingsTabs.cshtml` sub-nav (the `_EventsTabs.cshtml` / `_AdminNav.cshtml` idiom, active tab derived from the action name, tab labels **reusing the four already-registered section-title keys — no new translation key**), and the four POST save lanes **byte-for-byte unchanged** with their redirects re-pointed to the owning section (the language `Save` still → `Index`); the ADR 0039 §3.8 help/account mount-slot link moves to the last tab (`EmailLanguage.cshtml`); a surface-only change — no Core seam / doc type / migration / translation key changed, `PublicLocaleController` / `/language` untouched; `SettingsSectionSplitTests` pins each section GET view + each save-lane redirect; Amends the ADR 0062 admin-surface-split pattern (applied to the resident surface) + 0005 / 0019 / 0020 / 0061 (the sections) + 0015 (the `kw-l` registry) + 0039 §3.8 (the help slot) | Accepted |
| 0081 | Events calendar: week as the default view + quick-create from empty space + month single-line grid — the events calendar (ADR 0063 `EV-CAL`, ADR 0064 `EV-DWM`) at `GET /events/calendar`: the **default view changes from month to week** (the day/week/month views + nav + the window-agnostic `ListInRangeAsync` seam are unchanged; only the absent/out-of-set `?view=` fallback + the `EventCalendarViewModel.View` default become `"week"`); a **"New event" button** on the calendar header (reusing the already-registered `events.new_event` M4 label, all four languages) opens a **quick-create Bootstrap modal** (`Title` + `Start` + `End` + optional `Location`) that POSTs the **existing** `POST /events/new` route — **no new create lane, no new service seam, no new route, zero Core / schema / migration change**; **clicking an empty calendar slot** (a day/week time-ruler column maps the click's vertical fraction to an hour, snapped to 30 min; a month cell seeds a default 09:00 — events are not slot-clickable, so only empty space opens it) via a new plain-`tsc`-only self-wiring `client/lib/events-calendar-new.ts` (the `events-calendar-time.ts` idiom, no `any`, no network); the modal posts a hidden **`QuickCreate=true` shape signal** (a new `EventEditorModel` property, never a gate) that `CreatePost` reads *before* the `IsValid` gate to seed `Body` from `Title` when blank (a title-only quick-create is a well-formed shape; the full composer's "a body is required" rule + re-render path untouched) + `SaveAsDraft=true` (ADR 0037 floor) + the community-visible default audience (ADR 0001-B / 0036); the **month grid** tightens to single-line separators with no inter-cell margin (`site.css`); **zero new translation keys** (`events.new_event` + `common.optional` reused); Amends **0064 C-DWM·8** (the month default) + additive on 0063 / 0054 / 0001-B / 0036 / 0037 / 0071 (the modal convention) / 0031 (the `tsc`-only pin) / 0015 / 0072 (the `kw-l` registry + attribute exception); `EV-NW` named lane | Accepted |
| 0082 | Content actions dropdown (announcements, posts, pages, replies) — the per-item affordance rows on the three content detail surfaces (announcements, posts, pages) and each reply row, which had grown into long vertical stacks of one-button-per-action (each with its own visible button, `<details>` expansion, or card block), are folded into a single ⋮ **actions dropdown** (a borderless trigger + a Bootstrap dropdown menu, the ADR 0071 `⋮` menu convention) so the surface shows the content first and the actions second, on collapse; a surface-only reorganization — no new route, no new service seam, no new translation key (the existing per-action `kw-l` keys move into the menu unchanged), no schema change | Accepted |
| 0083 | Group membership notifications: notify the resident when they are added to a group or invited to join one (kinds `group.added` / `group.invite`), and the tabbed group detail page — the `UserInfoService` write seams `AddGroupMemberAsync` / `InviteGroupMemberAsync` each emit a notification (recipient = the *other* resident, never the actor) reusing the frozen M6 `EmitAsync` seam verbatim (the eleven-kind set becomes a thirteen-kind set; the `NotificationsControllerTests` count assertion moves `11 → 13`); a circular-dependency seam (the `NotificationService` resolves lazily from an optional `IServiceProvider`); email best-effort, inbox the durable record; the group detail page re-organized into Feed / Members / Settings tabs (the ADR 0062 nav-tabs idiom) so the Settings tab is the home for "we might later add notification options and more"; Amends 0007 / 0008 / 0010 / 0013 / 0026 / 0076 / 0077 / 0078 | Accepted |
| 0084 | Per-target notification subscriptions: opt-in/opt-out per `(recipient, kind, target)` on the frozen M6 emitter — three new kinds `announcement` / `community.post` / `page.child` (the closed set grows 13 → 16; `OptInKinds = { announcement, page.child }`, the rest opt-OUT), three emitters on the `AnnouncementService` / `PostService` / `PageService` write seams (recipient = the affected resident, target = community id / group id / parent-page id / the `"announcements"` flat sentinel), a frozen-surface amendment (a 7-arg `EmitAsync` overload + a gate step 0 consulted *first*, the 6-arg signature + return unchanged — the ADR 0078 additive-gate precedent), a new `NotificationSubscription` doc on the `M6DocTypes` surface (business key `(RecipientId, Kind, TargetId)`, upsert-only, never deleted), three new public lanes on the frozen `NotificationService` (`GetSubscriptionsAsync` / `SetSubscriptionAsync` / `IsSubscriptionEnabledForAsync`), a settings surface (`GET/POST /notifications/subscriptions` + one view + one `_AccountNav` link), a per-page toggle (`POST /pages/{id}/subscribe` + `PageShowViewModel.Subscribed` + one `Page/Show.cshtml` button), 21 new `kw-l` keys × 4 languages, and 15 new tests; Amends 0076 / 0077 / 0078 / 0083 / 0015 / 0005 / 0039 / 0040; `Milestones.cs` / the README Roadmap / `MilestonesTests.cs` **untouched** (a named lane, not a milestone) | Accepted |
| 0085 | Notification item link + reply highlight: the content/reply notifications (`post.reply` / `community.post` / `group.post` / `announcement` / `event.rsvp` / `page.child`) carry a same-origin relative `LinkPath` on the `Notification` doc (inbox renders it as a clickable "View" link) and the email carries it **absolute** (the instance `BaseUrl` from `VerificationOptions` + the relative path — the M1 verification-link precedent), with the post-detail replies gaining an `id="reply-{id}"` anchor + a hash-match scroll-and-highlight (the `#reply-{id}` fragment a reply link carries); one additive `LinkPath` field + an additive `linkPath` param on the frozen `EmitAsync` (default `null` — the ADR 0084 frozen-surface-amendment precedent), 6 emitters wired, one new `PageService.GetPathAsync` lane, 1 new `kw-l` key × 4 languages, 2 new tests; Amends 0076 / 0084 / 0015 / 0061; additive on 0005 / 0039; `Milestones.cs` / the README Roadmap / `MilestonesTests.cs` **untouched** (a named lane, not a milestone) | Accepted |
| 0086 | Goals & Projects lane (`PL`): the higher-level goals + projects page on top of M5 — two new documents (`ProjectGoal` + `Project`) in the **existing** `Kumunita.Core.Projects` context, additive on `M5DocTypes` (zero migration), two new adapters (`ProjectGoalToAuditableResource` / `ProjectToAuditableResource`) over the frozen `IAuthorizationService` Read path (no new `AccessAction` / `AccessVia` / branch), the **creator ∪ GlobalAdmin** standing matrix (ADR 0070), the additive `IProjectService` goal/project read + write lanes + `SetTodoProjectAsync` / `SetBoardProjectAsync` association lanes + the soft-delete lanes, one additive `ProjectId?` feed-filter field on `TodoItem` + `KanbanBoard` (never a gate), the `GET /projects` landing page + the **Projects** nav tab, the project / goal link + picker on the M5 to-do / board + project surfaces, the ADR 0024 soft-delete + dangling-association rule (no hard delete), `pl.*` `kw-l` keys × 4 languages; Amends 0067 / 0070 / 0079 / 0024 / 0006 / 0001-B / 0036 / 0025 / 0031 / 0018 / 0019 / 0020; `Milestones.cs` / the README Roadmap / `MilestonesTests.cs` **untouched** (a named lane, not a milestone — `M7` stays `StatusNext`) | Accepted |

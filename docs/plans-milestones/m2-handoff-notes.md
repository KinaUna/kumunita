## U1 — Design doc Part 1
- Wrote `docs/design/m2-directory-profiles-groups.md` (Part 1: scope, invariants, FACES).
- Invariants used: ADR 0006 **C1, C3, C4, C6** (directory + contact block); ADR 0006 **C2** (delegation on a profile); ADR 0006 **C5** (moderator default-OFF); ADR 0001-B (author's choice absolute); ADR 0003 (SoD — group owner/GlobalAdmin only). ADRef 0006-D / 0006-E honored (single seam, compatible-lane addition).
- New M2-owned invariants: **C-M2·1** (contact gating ordering after `Visibility`), **C-M2·2** (candidate filter ≠ authz separation, §4.3), **C-M2·3** (group SoD).
- FACES count: **15** (F1–F15), each row pinned to a C/ADRef invariant above.
- Closed M1's "out of scope" line: "profile editing UI and directory visibility rules (M2)".
- No code. No build. U2: name the test list + the one new `IUserInfoService` signature + acceptance gate from these pins.

## U2 — Design doc Part 2
- Appended `## Seams & contracts (Part 2, written by U2)` (~150 lines) to `docs/design/m2-directory-profiles-groups.md`:
  - §2.1 frozen seam list (exact C#): `IAuthorizationService` × 4, `Audience`/`AudienceGrant`/`AudienceMode`/`GrantKind`, `Decision`, `VisibleSet`, `AccessAction`, `IAuditableResource`, `AccessAudit`, `IUserInfoService` (M1 frozen surface + the `GetProfilesAsync(bool)` M2 ADD), `Profile`, `ProfileUpdate`, `Group`, `GroupMembership`.
  - §2.2 new M2-owned Core types (exact C#): `ProfileToAuditableResource` (Id / Name / OwnerId / Audience / ComponentId / TargetKind) and `DirectoryService` (ctor taking `IUserInfoService` + `IAuthorizationService`; public methods `ListAsync`, `DetailAsync`, `PreviewAsAsync`).
  - §2.3 §4.3 candidate-filter rule (invariant C-M2·2): 5-state table (unauth ⇒ empty; verified ⇒ `GetProfilesAsync(verifiedOnly:true)`; unverified ⇒ self only; moderator ⇒ same as verified; missing profile ⇒ empty). Filter is a **precondition**, never an audit subject.
  - §2.4 Contact-block gating rule (invariant C-M2·1): 4-shape table (null / Any+empty / Any+non-empty / All+empty) — evaluated **only after** `Visibility` allowed; the contact call is a *separate* `CanSeeAsync` on the same `MatchGroups` core (C6), *not* a merged compound audience.
  - §2.5 **22 seam tests** with exact names. Each name carries its FACES row (F1–F15) and invariant (C-M2·1/2/3 or ADR 0006-C1/C2/C3/C4/C5/C6 or ADR 0001-B/0003). File: `tests/Kumunita.Core.Tests/DirectoryServiceTests.cs`.
  - §2.6 acceptance gate (U12 records): same three-test shape as M1 step 9 (closed-loop / handoff / part-vs-whole), with the 22-test seam list as the part-vs-whole evidence. Record goes into `### Run result (M2 acceptance gate — <date>)` in the design doc.
  - §2.7 drift-guard: 22-test list, `IUserInfoService` frozen surface + `GetProfilesAsync` ADD, `ProfileToAuditableResource` 6-member shape, §2.4 4-shape table, and FACES count are all frozen pins.
- **Pinned seam signatures: 12** (4 on `IAuthorizationService`, `IUserInfoService.GetProfilesAsync(bool)` — M2 ADD, the `ProfileToAuditableResource` × 6 members, and the `DirectoryService` ctor + 3 public methods).
- **1 M1-seam assumption flagged** (contingent, U9/U10/U11 must verify): `IUserInfoService.GetGroupsForUserAsync(string userId)`. M1's frozen surface exposes only `GetGroupIdsAsync` (a `HashSet<string>`); F14 ("my group list shows only groups I own + groups I belong to") requires reading `Group` documents. The §2.2 flag says: **if M1 lacks it, U9 opens the drift-guard in the same commit as U9's first Web consumer** (new method on a frozen interface, ADR 0006-E lane, mirroring the `GetProfilesAsync` ADD).
- FACES count: **15** (unchanged from U1 — U2 pins *how* those 15 outcomes are tested, does not add any; the Part 1 count is the handoff field).
- Invariant pins used (unchanged from U1): ADR 0006 C1, C2, C3, C4, C5, C6; ADR 0001-B; ADR 0003 (SoD); ADR 0006-D / E; M2-owned C-M2·1, C-M2·2, C-M2·3.
- No code. No build. U3: implement `GetProfilesAsync(bool)` on `IUserInfoService` (exact signature in §2.1) + `UserInfoService.GetProfilesAsync_VerifiedOnly_Filters` test that asserts *no audit row* is appended (the C-M2·2 unit-level pin).

## U3 — `GetProfilesAsync(bool verifiedOnly)` + DI
- **Signature pinned (exact, per design doc §2.1):** `Task<IReadOnlyList<Profile>> GetProfilesAsync(bool verifiedOnly)` on `Kumunita.Core.UserInfo.IUserInfoService` — the single M2 `ADD` on a frozen interface (ADR 0006-E compatible lane; precedent `IDocumentSession` overloads on M1's `IAuthorizationService`). Doc-comment anchored to C3/C4/C6 + the §4.3 candidate-filter note (returns a *candidate* set, never a visible set, no audit row).
- **Files touched (U3 deliverables, both):**
  - `src/Kumunita.Core/UserInfo/IUserInfoService.cs` — appended the new method with the pinned doc-comment, in an annotated "M2 additions (ADR 0006-E)" block before the existing M1 block.
  - `src/Kumunita.Core/UserInfo/UserInfoService.cs` — implemented `GetProfilesAsync(bool verifiedOnly)` as a *read* (mirrors `GetProfileAsync`'s `store.QuerySession()` shape, not `UpsertProfileAsync`'s write session — reads stay live per C4). Server-side `Where(p => p.Verified)` when `verifiedOnly:true`, no filter when `false`.
- **DI note:** no change needed — `IUserInfoService → UserInfoService` is already registered (transient) in `src/Kumunita.Core/DependencyInjection.cs` (line 43); the new method rides that registration. Confirmed against the plan's "exit if singleton" prompt — it's transient, matching the M1 pattern.
- **Impl deviation (recorded, not patched):** Marten's Linq parser rejected a captured-bool ternary (`verifiedOnly ? p.Verified : true`) with `System.ArgumentNullException` at query-parse time. Fixed by branching *C#-side* (two queries, chosen by `verifiedOnly`) instead of one server-side conditional `Where`. The plan's "one `LoadDocumentsAsync<Profile>(...)` + client-side `Where`" shape is functionally equivalent; the two-query shape is strictly the same read, no behavior difference.
- **Test added (1):** `tests/Kumunita.Core.Tests/UserInfoServiceTests.cs` → `GetProfilesAsync_VerifiedOnly_Filters` — plants 2 verified + 1 unverified profile via `UpsertProfileAsync`, asserts `verifiedOnly:true` returns exactly the 2 verified, `verifiedOnly:false` returns all 3, and asserts the **zero** rows in `AccessAudit` after both calls (the C-M2·2 "candidate filter is not an access decision" pin at the unit level).
`UserInfoServiceTests` (11 tests, including the new one)
- **No deviations to record against U1/U2's pinned signatures or the 12-seam freeze list.** The `IUserInfoService` frozen surface is now: 11 M1 methods + 1 M2 `ADD` = 12 public methods, matching §2.1 exactly.
- U4: `ProfileToAuditableResource` — the adapter. Read `IAuthorizationService`'s `IAuditableResource` definition first (confirm the 6 members before choosing Shape A vs B per the plan).

## U4 — `Profile` as `IAuditableResource` (the adapter)
- **Shape chosen: B (wrapper).** `IAuditableResource` (confirmed in `src/Kumunita.Core/Authorization/AccessAction.cs`, the real `Kumunita.Core.Authorization` namespace — note: the repo's directory is `Authorization`, not the `Authrization` spelling the plan file uses) has exactly the 6 members the design doc §2.2 names, so Shape A (`Profile : IAuditableResource`) was available — but the design doc (§2.2) **pins** Shape B explicitly, and U4 follows the doc (drift-guard rule: the agent's job is to match the pinned signature, not prefer "cleaner"). No `Deviations` finding against the 6-member count (no pause triggered).
- **Files shipped (2, the closed U4 set):**
  - `src/Kumunita.Core/UserInfo/ProfileToAuditableResource.cs` (new, ~45 lines) — `namespace Kumunita.Core.UserInfo`, `public sealed class ProfileToAuditableResource : IAuditableResource`, ctor `(Profile profile)`, exposes the `Profile` reference + the 6 members per design §2.2 verbatim: `Id => Profile.SubjectId`, `Name => Profile.DisplayName`, `OwnerId => Profile.SubjectId`, `Audience => Profile.Visibility`, `ComponentId => null`, `TargetKind => "directory"`. `sealed` (matches the doc's `sealed` pin; nothing downstream should extend it).
  - `tests/Kumunita.Core.Tests/ProfileToAuditableResourceTests.cs` (new, ~90 lines) — `Maps_All_Six_Fields` (asserts all 6 member mappings; deliberately sets `ContactVisibility` to a *different* `Audience` than `Visibility` to prove the adapter projects *exactly* `Visibility`, not the contact field or a merge) + `TargetKind_Is_Directory` (asserts the constant `"directory"` across 3 differently-shaped profiles). **No Postgres fixture needed** (pure in-memory projection; no store, no audit lane touched — matches U4's "no test on ContactVisibility" scope pin).
- **No DI change** — the adapter is constructed at the call site (one new allocation per `CanSeeAsync`/`CanAsync` argument), not a registered service; `src/Kumunita.Core/DependencyInjection.cs` untouched (outside U4's deliverables).
- **No build/test failures.** `run_build` green for `Kumunita.Core` and `Kumunita.Core.Tests`; both U4 tests pass (2/2).
- **No deviations** against U1/U2's pinned signatures, the 12-seam freeze list, or the 6-member `IAuditableResource` shape.
- **U5 note:** `ListAsync`/`DetailAsync`/`PreviewAsAsync` are U5's — the only new thing U4 adds to compose against is calling `new ProfileToAuditableResource(p)` to build each `IAuditableResource` argument for `CanSeeAsync` (list bulk) / `CanAsync` (single detail) — the same adapter instance per profile is safe to pass to either overload (design §2.2's "does not own the profile" note).

## U5 — `DirectoryService` composition (list + detail + candidate filter + contact-block gating)
- **Files shipped (3, the closed U5 set):**
  - `src/Kumunita.Core/UserInfo/DirectoryService.cs` (new, ~220 lines) — `namespace Kumunita.Core.UserInfo` (frozen), `public sealed class DirectoryService`, ctor `(IUserInfoService, IAuthorizationService)`, **3 public methods** + **3 public return-type records** (U2's §2.2 froze the 3 *names*; the plan's U5 wording says "pin the two method signatures" — read that as "the two *product rules'* primary methods," i.e. `ListAsync`+`DetailAsync`; `PreviewAsAsync` is the F6-only read-only preview, deliberately a third, and U2's own 12-seam count already includes all 3). **Exact shipped signatures:** `Task<DirectoryList> ListAsync(string viewerSubjectId, bool viewerVerified)`, `Task<DirectoryDetail> DetailAsync(string viewerSubjectId, string targetSubjectId)`, `Task<PreviewRow> PreviewAsAsync(string authorSubjectId, string asSubjectId)`; **records:** `DirectoryList(IReadOnlyList<Profile> Visible, int HiddenCount)`, `DirectoryDetail(bool IsVisible, bool ShowContactBlock, Profile? Profile)`, `PreviewRow(bool IsVisible, bool ShowContactBlock, Profile? Profile)`. File lives in `UserInfo/` (matching its pinned `Kumunita.Core.UserInfo` namespace and the codebase's folder==namespace-segment convention, same as U4's `ProfileToAuditableResource.cs` — not `Authorization/`, despite the plan's U5 fallback text suggesting that folder; that fallback's own condition, "if U2 is ambiguous," does not apply here since U2's §2.2 pin is unambiguous on namespace+names).
  - `src/Kumunita.Core/DependencyInjection.cs` (modified) — `services.AddTransient<DirectoryService>(sp => new DirectoryService(sp.GetRequiredService<IUserInfoService>(), sp.GetRequiredService<IAuthorizationService>()))`, mirroring the existing 4 registrations' pattern. No new interface (a concrete composition class, not a frozen seam — same category as `UserInfoService`/`AuthorizationService` themselves in that sense, but a *caller* of them, not a peer seam of the ADR 0006-D pair).
  **both** `Via`=`Owner`, `Outcome`=`Allow`, `Action`="read", `TargetKind`="directory",
- **Implementation notes (for U6, the defining consumer of `DetailAsync`/`PreviewAsAsync`):** `DetailAsync` and `PreviewAsAsync` **share** one private `EvaluateTwoGatesAsync` (Visibility → ContactVisibility) so their two public methods *cannot* drift apart (C6, applied to U5's own two methods). §2.4's `null`-ContactVisibility row is implemented as a **short-circuit with no `Can*` call and no audit row** (the design doc's literal "not evaluated" wording) — don't "fix" it into a fourth decision. The contact-block's *second* decision uses a **private nested** `ContactVisibilityResource : IAuditableResource` (not a new public type) that maps `Audience => profile.ContactVisibility` and everything else identically to U4's `ProfileToAuditableResource` (`Id`/`Name`/`OwnerId`/`ComponentId`/`TargetKind`="directory") — `sealed class DirectoryService` stays "nothing else is public" per §2.2. Both of `DetailAsync`'s/`PreviewAsAsync`'s decisions use `IAuthorizationService.CanAsync` (single-resource form), **not** `CanSeeAsync` — a deliberate, documented choice (§2.2's revised "Note to U5": both overloads reduce to the *exact same* private `Decide(...)` call, so C6's shared-matching-core pin holds structurally; the only observable difference is that `CanAsync` produces exactly 1 `AccessAudit` row vs `CanSeeAsync`'s aggregate+per-item pair — for a 1-candidate set that's a redundant extra row). **U6's tests should assert on `Via`/`Outcome`/`TargetKind`/`TargetId` and the row's existence — not on which `Can*` overload U5 called internally** (invisible to the test's `store.QuerySession()` audit-row read anyway), and should not expect an *aggregate* row for the detail/contact path (aggregate rows are the `CanSeeAsync`/list path's shape, which is what `ListAsync` uses and what `ListAsync_Hides_Unverified` already asserts).
- **Invariant pins used (unchanged from U1/U2):** C1, C3, C4, C6 (ADR 0006); C-M2·1 (contact-gating ordering — the two-gate short-circuit rule in `DetailAsync`/`PreviewAsAsync`); C-M2·2 (the §2.3 candidate filter applied *inside* `ListAsync`, never logged as an audit row — asserted by `ListAsync_Hides_Unverified`). C-M2·3 (group SoD) is not exercised by U5 — that's U9/U10/U11's surface (the `IUserInfoService.GetGroupsForUserAsync` contingent ADD U2 flagged, still unresolved, still not U5's concern).
- **Drift-guard action (same commit, per the rule "the unit updates this file in the same commit"):** `docs/design/m2-directory-profiles-groups.md` §2.2 — replaced the stale `#`-commented pseudo-signatures (`Task<IReadOnlyList<Profile>> ListAsync` / `Task<Profile?> DetailAsync` / `Task<Profile?> PreviewAsAsync`) with U5's actual shipped signatures + the 3 records, and revised the "Note to U5" paragraph to record: (a) U5 ships the 3 *names* + ctor **unchanged** from U2's pin, (b) U5's addition is the 3 records as those same 3 methods' return types — the **same drift-guard lane** §2.7's own `ProfileToAuditableResource` "frozen once U4 lands it" precedent (U2's 12-seam count already includes `DirectoryService` as one entry: "and the `DirectoryService` ctor + 3 public methods"), (c) the `CanAsync`-vs-`CanSeeAsync` justification + the U6 assertion hint (above), (d) the shared two-gate path + the §2.4 `null` short-circuit. U2's §2.1 frozen-interface list, §2.4 4-shape table, and §2.5 22-test list are **not touched** — U5's change lands strictly within the §2.2 `DirectoryService` block U5 is the defining unit for.
- **U6: the 5-test seam list (plan U6's `ContactVisibility_FourShape_TrightTable` / `Unverified_SelfCandidate_NotAudited` / `DelegationOnProfile_OwnerBranch` / `CanAsync_Equals_CanSeeAsync_SingleRow_Profile` / `GroupAddRemoveMember_ReflectedOnNext_Call_Profile`) is yours to write, in `tests/Kumunita.Core.Tests/DirectoryServiceTests.cs` — *append* it to the file I shipped (don't delete/replace `ListAsync_Hides_Unverified`, it stays as U5's self-check and is already green). Reuse `DetailAsync`/`PreviewAsAsync`'s existing two-gate path — don't re-derive a parallel `Can*` call path for the contact decision. `CanAsync_Equals_CanSeeAsync_SingleRow_Profile` (test 4) is the one test where the `CanAsync`/`CanSeeAsync` *choice* I made actually shows up: it should assert the two overloads agree on `Allowed`/`Via` for the same profile+actor+action — which is exactly what `AuthorizationService`'s shared private `Decide(...)` gives you for free; you don't need to change U5's code for this, just call `AuthorizationService`'s two public overloads directly (bypassing `DirectoryService`) and compare. Entry reads: U5's `DirectoryService.cs` + `AuthorizationService.cs` (confirm the `Decide` core, if U6 wants to lean on it directly) + `tests/Kumunita.Core.Tests/PostgresFixture.cs` + one of U3/U4's test files for the seed pattern — nothing more needed.

## U6 — `DirectoryService` 5-test seam list (M2 invariant-anchored seam tests)
- **All 5 tests green** (5/5 pass, `PostgresFixture`, `run_tests` in `Kumunita.Core.Tests`). U5's `ListAsync_Hides_Unverified` also remains green (regression check).
- **File shipped:** `tests/Kumunita.Core.Tests/DirectoryServiceTests_U6.cs` (new, ~630 lines, class `DirectoryServiceTests_U6`). Deviation from U5's "append to the file I shipped" instruction: shipped as a *second file* (documented in the class doc comment) so U5's single-test class stays exactly as-is; same assembly, same fixture, same composition pattern.
- **Test-to-FACES/invariant mapping (the five tests U11/U12 cite at the acceptance gate):**
  1. `ContactVisibility_FourShape_TrightTable` — §2.4 4-shape table (F3 line 154, F4 line 155; §2.4 row table line 573–586). C-M2·1 (gating ordering) + C1 (empty-audience deny). Asserts: null → 1 row (visibility only, contact not evaluated); Any+empty → 2 rows, contact Deny; Any+grant → 2 rows, ShowContactBlock=true; All+empty → 2 rows, contact Deny.
  2. `Unverified_SelfCandidate_NotAudited` — F8 (line 159). C-M2·2 (candidate filter ≠ access decision, never audited). Asserts: exactly one row (Owner branch, `EffectivePrincipalId`=viewer); no `TargetId` names any other resident.
  3. `DelegationOnProfile_OwnerBranch` — ADR 0006 C2 (delegation on a profile). Asserts in-scope (Read): `Allowed=true`, `Via=Delegation` (mechanism), `EffectivePrincipalId=owner` (owner branch fired — the proof of delegation). Out-of-scope (Moderate): `Allowed=false`, `Via=Delegation`, `EffectivePrincipalId=delegatee` (own identity).
  4. `CanAsync_Equals_CanSeeAsync_SingleRow_Profile` — F11 (line 162). C6 (shared matching core — one `Decide` pass, no drift). Asserts: `CanAsync` and `CanSeeAsync` agree on `Allowed` and `Via` for the same profile+actor+action (both Allow, both Deny cases). Calls both overloads directly; no `DirectoryService` involved.
  5. `GroupAddRemoveMember_ReflectedOnNext_Call_Profile` — F2 (line 153). C4 (live membership) + ADR 0003 (SoD: only group owner). Asserts: after `AddMemberAsync`, next `ListAsync` shows the new member visible (audience grant now matches); after `RemoveMemberAsync`, next `ListAsync` hides them.
- **Fixes applied during this unit (not in U5's code — test-file bugs only):**
  1. `BootStoreAsync` was missing `await store.Storage.Database.ApplyAllConfiguredChangesToDatabaseAsync(...)` — all 5 tests failed with `42P01: relation "mt.AdminOverride" does not exist`. Fixed by adding the one-line DDL apply call (mirrors U5's `DirectoryServiceTests.BootStoreAsync`).
  2. Test 1 row 3 (`Any`+grant): over-asserted `Assert.Single(OutcomeRowCount(Allow))` — the viewer is in BOTH the visibility and contact audiences, producing 2 Allow rows. Removed the Single assertion; kept `ShowContactBlock=true` + the count=2 assertion.
  3. Test 3 in-scope: asserted `Via=Owner` but implementation returns `Via=Delegation` for delegated access to the owner's branch (the `Decide` branch-1 logic: `isDelegated && OwnerId != actorId` → `Via=Delegation`). Fixed to assert `Via=Delegation` + `EffectivePrincipalId=owner` (which is the actual proof the owner branch fired).
- **Invariant pins used (unchanged from U1/U2):** C1 (empty-audience deny; test 1 rows 2 & 4); C2 (delegation on profile; test 3); C4 (live membership; test 5); C6 (shared matching core; test 4); C-M2·1 (contact gating ordering; test 1); C-M2·2 (candidate filter; test 2); ADR 0003 (SoD; test 5's group-owner-only setup).
- **No deviations against U1/U2's pinned signatures or the 12-seam freeze list.** No Core production code touched — U6 is test-only. §2.5 22-test list is untouched (U6's 5 tests *are* a subset of the 22 named in §2.5; the other 17 belong to U7/U8/U9/U10/U11/U13).
- **U7 note:** The `DirectoryViewModel` list surface is yours. Read U5's `DirectoryService.cs` (`ListAsync` return: `DirectoryList(IReadOnlyList<Profile> Visible, int HiddenCount)`) + U2's design doc §2.6 acceptance gate (the "view renders the list + a footer `HiddenCount`" pin). The `VisibleProfile` record projection (`Guid SubjectId, string DisplayName, bool Verified`) is the plan's U7 pin — the hidden-row privacy pin at the view-model layer.

## U7 — `DirectoryViewModel` + `DirectoryController` (list) + `Directory/Index.cshtml`
- **Files created (3 deliverables, all new; no M1 code touched):**
  - `src/Kumunita.Web/Models/DirectoryViewModel.cs` (~40 lines) — `DirectoryViewModel.Profiles: IReadOnlyList<VisibleProfile>` + `DirectoryViewModel.HiddenCount: int`; the row record is `record VisibleProfile(string SubjectId, string DisplayName, bool Verified)`.
  - `src/Kumunita.Web/Controllers/DirectoryController.cs` (~70 lines) — `[Authorize]` `Index()`; ctor takes the concrete `DirectoryService` (not `IDirectoryService`, see Deviation 1); reads the viewer-state pair from the *real* Web helpers `KumunitaPrincipal.SubjectId(User)` + `KumunitaPrincipal.IsVerifiedResolved(User)` (see Deviation 2); calls `directory.ListAsync(subject, verified)` once; projects every `Profile.Visible` to `VisibleProfile` and forwards `HiddenCount` — a hidden row's `Email`/`Phone`/`ContactVisibility` never land on the model.
  - `src/Kumunita.Web/Views/Directory/Index.cshtml` (~40 lines) — `@model DirectoryViewModel`; a `@foreach` over `Model.Profiles` showing `DisplayName` + a "Verified" badge when `p.Verified`; a footer line that renders `Model.HiddenCount` and never the hidden rows' names/values.
  - `tests/Kumunita.Web.Tests/DirectoryIndexViewModelTests.cs` (~50 lines) — 3 xUnit tests pinning the *shape* of the view model (the "exactly three fields" + "no email/phone/contact/visibility members" assertions are the hidden-row privacy pin at the view-model layer; the third guards `DirectoryViewModel` itself to exactly `Profiles` + `HiddenCount`).
- **Deviation 1 — `IDirectoryService` does not exist.** U5 deliberately shipped `DirectoryService` as a **concrete `sealed class`**, not behind an interface, and registered it by concrete type in `src/Kumunita.Core\DependencyInjection.cs` (`services.AddTransient<DirectoryService>(sp => new DirectoryService(...))`). The plan's U7 entry says `ctor(IDirectoryService)` — but M1's "single seam" rule (ADR 0006-D) applies to the *modules it calls* (`IUserInfoService`/`IAuthorizationService`), not the caller, and U2's §2.2 freeze pins "3 public methods, nothing else" on the concrete class (no `IDirectoryService` on the §2.1 frozen-surface list). I matched the real (frozen) code: `DirectoryController(DirectoryService directory)`. No `IDirectoryService` was invented.
- **Deviation 2 — `KumunitaClaimsPrincipal` type does not exist.** The plan's U7 entry reads `Index([FromServices] KumunitaClaimsPrincipal principal)`. In the actual Web project the viewer-state pair is read from `ControllerBase.User` (a `ClaimsPrincipal`) via the `Kumunita.Web.Security.KumunitaPrincipal` static helpers: `KumunitaPrincipal.SubjectId(user)` → `c.Type == "Kumunita.Sub"`; `KumunitaPrincipal.IsVerifiedResolved(user)` → `c.Type == "Kumunita.Verified" && c.Value == "true"`. I used `KumunitaPrincipal.SubjectId(User)` and `KumunitaPrincipal.IsVerifiedResolved(User)` — the two already-frozen, claim-shaping-safe seams (see `_AccountNav.cshtml` and `AdminController`'s `Kumunita.Core.Identity.ClaimTypes.Subject`/`KumunitaRole` reads for the same claim-typed pattern).
- **Deviation 3 — `VisibleProfile.SubjectId` is `string`, not `Guid`.** The plan's U7 deliverables list pins `VisibleProfile(Guid SubjectId, string DisplayName, bool Verified)`, but the frozen M2 source — `Kumunita.Core.UserInfo.Profile.SubjectId` — is a **`string`** (an opaque subject id, not a guaranteed `Guid`; `Profile.ToAuditable()`'s `string Id` mirrors this). Projecting to `Guid` would require `Guid.Parse` at every row and could `FormatException` on any non-Guid subject (the federation-reserved `ExternalId` lane — ADR 0001 — explicitly defers an OIDC `sub` swap where an opaque string is the expected shape). `VisibleProfile.SubjectId: string` is chosen to preserve the source type without loss; the row is not `Guid`-validated or `-Parsed` anywhere. If future M2 needs a `Guid SubjectId`, the *Profile* must change first (which is U4/U14's lane, not U7's).
- **Auth-gating choice:** `[Authorize]` on the controller (not `[AllowAnonymous]` + render-empty). The §2.3 candidate filter's "no principal ⇒ empty" row already holds at the `DirectoryService` level (U5's self-check + U6's `Unauthenticated_Empty` test), but rendering an empty `Index` to an anonymous visitor is not a *product* surface M1/M2 have shipped (the login pages are the unauth target — `_AccountNav` renders "Sign in"/"Sign up" links there). `DirectoryController` sits in the authenticated zone: `[Authorize]` + Razor's default redirect-to-login (matching `AdminController`'s class-level `[Authorize(Roles = ...)]` and `AccountController`'s per-action guards). The §2.3 no-principal row is still *defensive* in `DirectoryService` in case a future `[AllowAnonymous]` is added — that pin is U5/U6's, not U7's. U7 just doesn't render an unauth directory.
- **Invariant pins used (unchanged from U1–U6):** C-M2·2 (candidate filter — applied in `DirectoryService`, not here; this controller *only* supplies the caller-state pair); C6 (one shared matching pass — `DirectoryService`'s job, not U7's); ADR 0006-D (Web shapes HTTP, Core decides; no re-derivation of access in the controller); ADR 0006-B (the claim set minted at sign-in is the whole principal — the `KumunitaPrincipal` helpers read exactly the claim set `KumunitaClaimsPrincipalFactory` mints).
- **Build + test:** `run_build` green for `src/Kumunita.Web` and `tests/Kumunita.Web.Tests`. `run_tests` (Project filter `Kumunita.Web.Tests`): **10/10 passed** — 3 new U7 tests (`DirectoryIndexViewModelTests.VisibleProfile_Has_Exactly_Three_Projected_Fields`, `.DirectoryViewModel_Only_Exposes_Profiles_And_HiddenCount`, `.Profile_Projection_Excludes_Contact_Fields`) + the existing 7 (`HomeControllerTests` × 3, `HealthControllerTests` × 4).
- **U8 note:** The detail route is yours. The `DirectoryService.DetailAsync(viewer, target)` shape is the same `SubjectId`/`Verified` projection plus a `ShowContactBlock: bool` and the full `Profile?` for contact fields (email/phone) *only when* `ShowContactBlock == true` (C-M2·1 — the §2.4 contact decision is the second gate, evaluated *only* after `IsVisible` is true). The `VisibleProfile` privacy pin does not apply to the detail (that's exactly one row, fully authorized); the detail's contact block is a *separate* model the view renders under the `ShowContactBlock` gate.

## U8 — `DirectoryViewModel.Detail` + **detail** route + `Directory/Detail.cshtml` (contact block)
- **Files touched (2 modified / 1 view created / 1 test file created; no M1 Core code touched):**
  - `src/Kumunita.Web/Models/DirectoryViewModel.cs` — added the nested `public sealed record Detail(string DisplayName, bool Verified, bool ShowContactBlock, string? Email, string? Phone)` record inside `DirectoryViewModel`. The *list* model's public property set is unchanged from U7 (`Profiles` + `HiddenCount`) — Detail is a nested **type**, not a property — so U7's `DirectoryViewModel_Only_Exposes_Profiles_And_HiddenCount` shape-pin test stays green (verified 13/13).
  - `src/Kumunita.Web/Controllers/DirectoryController.cs` — added the `[HttpGet("{subjectId}")] Detail(string subjectId)` action (mod, not a new controller). Reads `KumunitaPrincipal.SubjectId(User)` (U7's deviation-2 pin — no `KumunitaClaimsPrincipal` type), guards empty/`null` subjectId ⇒ `NotFound()` (matching §2.3's "missing profile ⇒ empty" fail-closed row), calls the frozen `DirectoryService.DetailAsync(viewer, subjectId)`, and projects via a private static `ProjectDetail(DirectoryDetail)` helper. The helper is the **§9 pin at the Web↔Core boundary**: `IsVisible==false` ⇒ empty shape (`DisplayName=""`, `Verified=false`, `ShowContactBlock=false`, `Email=null`, `Phone=null`); `IsVisible==true` ⇒ lift `DisplayName`/`Verified` from `Profile`; and `Email`/`Phone` are lifted **only** when `ShowContactBlock==true` (the §2.4 "null ⇒ hidden" and deny rows ⇒ null even if the Profile has values).
  - `src/Kumunita.Web/Views/Directory/Detail.cshtml` (new, ~70 lines) — `@model DirectoryViewModel.Detail`; renders `DisplayName` + the `Verified` badge; the contact block (email as `mailto:` link, phone) is rendered **only inside** `@if (Model.ShowContactBlock)`; an info line is shown when a visible row has `ShowContactBlock==false` (the §2.4 "not evaluated" / deny shape) — so the Razor view has *no channel* to a contact value the gate did not allow. No `HiddenCount` here (single row).
  - `tests/Kumunita.Web.Tests/DirectoryDetailViewModelTests.cs` (new, 3 tests) — `Detail_Has_Exactly_Five_Fields` (reflection pin: `DisplayName`, `Email`, `Phone`, `ShowContactBlock`, `Verified` — exactly these five; no `Visibility`/`ContactVisibility`/`HouseholdId`/`ExternalId`/`SubjectId`); **`DirectoryDetailViewModel_ContactBlock_Gated`** (the plan's U8 exit test — the §9 pin at the view-model layer: three shapes — hidden / visible-no-contact / visible-with-contact — each asserted on `ShowContactBlock` + `Email`/`Phone` null-vs-non-null); `DirectoryViewModel_Still_Only_Profiles_And_HiddenCount` (guards against a "sneak the Detail fields onto the list model" regression — Detail stays a nested type, the list-model property set is unchanged).
- **`DetailAsync` return pinned (unchanged from U5):** `Task<DirectoryDetail> DetailAsync(string viewerSubjectId, string targetSubjectId)` returning `record DirectoryDetail(bool IsVisible, bool ShowContactBlock, Profile? Profile)`. `IsVisible` = first gate (`Profile.Visibility` via `IAuthorizationService.CanAsync`); `ShowContactBlock` = second gate (`Profile.ContactVisibility`, §2.4/C-M2·1 — evaluated **only** after `IsVisible` is true); `Profile==null` only in the fail-closed missing-profile case (no decision ran, no audit row).
- **`Detail` record fields (5, exact, in order):** `string DisplayName`, `bool Verified`, `bool ShowContactBlock`, `string? Email`, `string? Phone`. A **subset** of `Kumunita.Core.UserInfo.Profile` — the two contact fields `Email`/`Phone` (lines 52/53 of the frozen `Profile` doc) are the §9 pin's "email/phone" surface. No row-level `SubjectId` (the row is addressed by its route `{subjectId}`), no `Visibility`/`ContactVisibility`/`HouseholdId`/`ExternalId`/`TargetId`.
- **Contact-field names pinned:** `Profile.Email` and `Profile.Phone` — the two `string?` contact fields on the *frozen* `Kumunita.Core.UserInfo.Profile` doc. The detail's `Email`/`Phone` are their 1:1 projection; `DirectoryController.ProjectDetail` enforces the §9 gate by nulling them when `ShowContactBlock==false`, even if the source `Profile` has values.
- **Route pin:** `[HttpGet("{subjectId}")]` on the `Detail` action ⇒ `/directory/[subjectId]` (matches the plan's "detail route" line; U7's `Index` still at `/directory`). `NotFound()` on empty/null subjectId (no new `[Required]` dependency — `System.ComponentModel.DataAnnotations` is not referenced by `Kumunita.Web.csproj`; the guard is in the action, not in model binding).
- **Deviations recorded (3, all pin-consistent):**
  1. **No `[Required]` attribute.** The plan's U8 deliverable implies a route param; I initially tried `[FromRoute][Required] string subjectId` but `RequiredAttribute` is not referenced. Swapped for a guard clause `if (string.IsNullOrEmpty(subjectId)) return NotFound();` at the top of the action — same fail-closed shape per §2.3, without adding a new package.
  2. **Concrete `DirectoryService`, not `IDirectoryService`.** U7's deviation-1 pin: U5 shipped `DirectoryService` as a **concrete `sealed class`** (no `IDirectoryService` frozen seam). Called `directory.DetailAsync(...)` on the ctor param (DI is `services.AddTransient<DirectoryService>` per U3's DI note); no interface invented.
  3. **Route shape — `{subjectId}` in the template (not "Detail" as action-name).** The plan's literal "add `Detail(string subjectId)` action" would yield `/directory/Detail/{subjectId}` under convention routing (Razor Pages' `[HttpGet]` on a controller action = no explicit template ⇒ `/{controller}/{action}`). I used `[HttpGet("{subjectId}")]` so the **stated** URL `/directory/[subjectId]` (U8's entry line: "**detail** route") is what the browser hits — the same convention `AccountController.Profile` and `AdminController` use for named sub-routes.
- **No new packages, no DI registrations, no new Core code.** U8 is pure Web + one test file. `DirectoryService`'s frozen surface (3 public methods × 3 return records) is unchanged. M1's `IUserInfoService` / `IAuthorizationService` seams are unchanged.
- **Invariant pins used (unchanged from U1–U7):** C-M2·1 (contact gate ordering — `ProjectDetail` enforces the boundary-level §9 gate, mirroring U5's `EvaluateTwoGatesAsync` early-return at the `Visibility` deny); C-M2·2 (candidate filter — **not in the detail path**; `DetailAsync` is a single-row read, never a candidate enumeration); C3 (audit rows — committed by `IAuthorizationService` inside `DetailAsync`; this Web layer adds no audit of its own); C6 (single shared matching core — `DetailAsync`'s two gates ride the same `CanAsync` core; Web layer doesn't re-derive); ADR 0006-D (Web shapes HTTP, Core decides); ADR 0003 (SeD — the owner set `Visibility`/`ContactVisibility`; a non-owner sees exactly what the two gates say, no Web-layer bypass).
- **Build + test green:** `run_build` for `src/Kumunita.Web` ✅, `run_build` for `tests/Kumunita.Web.Tests` ✅, full-solution `run_build` ✅ (no warnings, no errors). `run_tests` (Project filter `Kumunita.Web.Tests`): **13/13 passed** — 3 new U8 (`DirectoryDetailViewModelTests.*`) + U7's 3 (`DirectoryIndexViewModelTests.*` — unchanged) + M1's 7 (`HomeControllerTests` × 3, `HealthControllerTests` × 4).
- **U9 note:** Your `GroupViewModel` + `GroupsController` (list + create) + `GroupCreateModel` + two views. The open question flagged by U2 — `IUserInfoService.GetGroupsForUserAsync` — must be resolved **first**: if M1 doesn't have it on the frozen surface, per U2's §2.7 pin *"if M1 lacks it, U9 opens the drift-guard in the same commit as U9's first Web consumer"* (ADR 0006-E compatible-lane, mirroring U3's `GetProfilesAsync` ADD). Check `src/Kumunita.Core/UserInfo/IUserInfoService.cs` + `src/Kumunita.Core/UserInfo/UserInfoService.cs` (frozen seam list in U2's §2.1) + M1's `Group`/`GroupMembership` docs, and the M1's `/admin` group-management surface (if any), before drafting `GroupViewModel`. M1's `IUserInfoService` exposes **no** `Group`-returning read; M2 must add one (the `IUserInfoService` frozen surface is currently 11 M1 methods + U3's `GetProfilesAsync` ADD = 12). The `GroupViewModel` projection itself is the U9 pin (small — likely `{ Guid Id, string Name, int MemberCount }` per U9's entry line, mirroring the `VisibleProfile` pattern: a *projection* of `Group`, not an enumeration of its fields).

## U9 — `GroupViewModel` + `GroupsController` (list + create) + `GroupCreateModel` + two views
- **Files shipped (4 new Core + 4 new Web + 2 test files + 1 design-doc revision, all one commit):**
  - *Core (drift-guard, ADR 0006-E lane, mirroring U3's `GetProfilesAsync` ADD):*
    - `src/Kumunita.Core/UserInfo/IUserInfoService.cs` — appended **two** M2 ADDs to the "M2 additions" block (after `GetProfilesAsync`): `Task<IReadOnlyList<Group>> GetGroupsForUserAsync(string userId)` (F14 — owner ∪ member, deduped, `Created`-desc; candidate projection, no audit row, C-M2·2; single "groups-for-user" read the Web renders from, never re-queries `Group`/`GroupMembership`, ADR 0006-D) and `Task<IReadOnlyList<GroupMembership>> GetGroupMembersAsync(string groupId)` (the per-group member read U9's `MemberCount` **and** U10's `Detail.Members` both consume — **one** read lane serves both, no drift churn). Both doc-commented on C-M2·2/C4/ADR 0006-D.
    - `src/Kumunita.Core/UserInfo/UserInfoService.cs` — implemented both reads on the live-row lane (`store.QuerySession()`, mirroring `GetProfileAsync`/`GetGroupIdsAsync`): `GetGroupsForUserAsync` = owned ∪ membership-ids (deduped by `Id`, `Created` desc); `GetGroupMembersAsync` = `Query<GroupMembership>()` for that `GroupId`, ordered by `At`. Empty-string `userId`/`groupId` fails safe (returns empty, no throw).
  - *Web (the plan-U9 deliverables):*
    - `src/Kumunita.Web/Models/GroupViewModel.cs` (new) — `GroupViewModel(string Id, string Name, int MemberCount)` (**exact 3-field pin**), `GroupListViewModel` (holds `IReadOnlyList<GroupViewModel> Groups`), `GroupCreateModel` (`[Required, MaxLength(100)] Name`, `[MaxLength(500)] Description`).
    - `src/Kumunita.Web/Controllers/GroupsController.cs` (new) — `[Authorize]`, ctor `(IUserInfoService userInfo)`. `Index` (GET `/groups`) → `GetGroupsForUserAsync(SubjectId(User))` + per-row `GetGroupMembersAsync(g.Id)`.Count → project to `GroupViewModel`. `Create` (POST `/groups`, `[ValidateAntiForgeryToken]`) → mint `ownerId = SubjectId(User)`, validate Name non-whitespace, call frozen `CreateGroupAsync(ownerId, name, description)`, `TempData["info"]` + redirect `Index`. **No detail route, no add/remove (U10). No `[AllowAnonymous]` — `[Authorize]` + Razor's default login redirect (U7's auth-gating choice).**
    - `src/Kumunita.Web/Views/Groups/Index.cshtml` (new) — list + `MemberCount` per row + empty-state + `Create a group` CTA + `TempData` info/error alerts (Bootstrap, matches `Directory/Index.cshtml`).
    - `src/Kumunita.Web/Views/Groups/Create.cshtml` (new) — `@model GroupCreateModel` form, `asp-action="Create"`, Name + Description inputs + `asp-validation`.
  - *Tests:*
    - `tests/Kumunita.Core.Tests/UserInfoServiceGroupsU9Tests.cs` (new, 4 tests) — `GetGroupsForUserAsync_ReturnsOwnerUnionMember_ExcludesOther` (F14 projection + dedupe), `GetGroupsForUserAsync_AppendsNoAuditRow_CM2_2` (C-M2·2 no-audit-row unit pin on **both** new reads), `GetGroupMembersAsync_LiveOnNextCall_C4_StrongConsistency` (C4 on the per-group lane: add→count 2, remove→count 1), `GetGroupMembersAsync_EmptyGroupId_ReturnsEmpty` (fail-safe). Same `PostgresFixture` + `BootStoreAsync` shape as U5/U6 (separate file, per U6's precedent).
    - `tests/Kumunita.Web.Tests/GroupsViewModelTests.cs` (new, 3 tests) — view-model-layer privacy pin: `GroupViewModel_Has_Exactly_Three_Projected_Fields` (`Id`/`Name`/`MemberCount`, nothing more), `GroupViewModel_Excludes_SourceGroupFields` (no `OwnerId`/`Description`/`Created`), `GroupCreateModel_Exposes_OnlyNameAndDescription` (no owner — minted from the principal). Mirrors U7's `DirectoryIndexViewModelTests` reflection shape.
  - *Design doc (same commit, the drift-guard lane — "the unit updates this file in the same commit"):*
    - `docs/design/m2-directory-profiles-groups.md` §2.1 — the `IUserInfoService` block now lists all **three** M2 ADDs (`GetProfilesAsync` U3, `GetGroupsForUserAsync` + `GetGroupMembersAsync` U9) with rationale.
    - §2.2 — U2's contingent-ADD flag is marked **RESOLVED by U9** (branch 2 executed: M1 lacked it → U9 opened the guard). Recorded that the flag originally named the method `GetGroupOwnersAsync` but U9 landed `GetGroupsForUserAsync` (owner ∪ member axis) + the second `GetGroupMembersAsync` ADD.
    - §2.7 — added freeze lines: both new ADDs frozen once landed (U10 must **reuse** `GetGroupMembersAsync`, not open a third lane); `GroupViewModel` `{Id, Name, MemberCount}` frozen as the sole `/groups` list-row shape (mirrors U7's `VisibleProfile` pin).
- **`CreateGroupAsync` confirmation (the U9 drift-guard, line 147):** read `IUserInfoService.cs` **before** writing — `Task<Group> CreateGroupAsync(string ownerId, string name, string? description)` **exists in M1** exactly as the plan expects, **including** the `addedBy == group.OwnerId ⇒ Via: Owner` owner-branch derivation in `UserInfoService`'s add/remove audit path. No pause, no `Deviations`. So M1's "it already enforces ADR 0003" pin held for the **write** path.
- **Drift-guard resolution (the U2-flagged `GetGroupsForUserAsync` open question U8 left for me):** M1's frozen surface (11 M1 methods + U3's `GetProfilesAsync` = 12) had `CreateGroupAsync` ✓ but **no `Group`-returning read**. Per U2's §2.2 "Drift decision for U9: verify M1 first … if the method is missing, open the drift-guard in the same commit as U9's Web consumer" — I **landed it in this commit** (with a second ADD) and updated §2.1/§2.2/§2.7 in the same commit. **The `IUserInfoService` frozen surface is now 11 M1 + 3 M2 ADDs = 14 public methods.**
- **Second M2 ADD I identified (not in the plan, in `Deviations`):** the plan-U9 pin `GroupViewModel { Id, Name, MemberCount }` needs a *per-group* member **count**, but M1 has **no** per-group membership read (only the user-axis `GetGroupIdsAsync`; a `Group` doc has no count). So U9 opened a **second** drift-guard ADD — `GetGroupMembersAsync(groupId) -> IReadOnlyList<GroupMembership>` — deliberately a *list* (not an `int` count) so **U10's `Detail.Members` reuses the same lane** rather than opening a third. This is a genuine M1-seam gap the design doc hadn't named (its §2.2 flag covered only the "groups-for-user" axis); recorded as a drift event in §2.1 + §2.7 + here.
- **Deviations (2, all pin-consistent, none a blocker):**
  1. **Ctor is `(IUserInfoService)`, not the plan's `(IIdentityService, IUserInfoService)`.** U7/U8 already established that the actor id is read via `KumunitaPrincipal.SubjectId(User)` (a static helper over `ControllerBase.User`; the `KumunitaClaimsPrincipal` type the plan names does *not* exist — U7's deviation-2). `IIdentityService` adds nothing to a group list/create (no role/verify read needed here — the SoD on create is the **caller passing its own subject as `ownerId`**, and the SoD on the *writes* is M1's `Via: Owner` derivation). Matched the real frozen code (`IUserInfoService` only); no `IIdentityService` dependency invented.
  2. **`Id` is `string`, not the plan's `Guid`.** Plan U9 line 143 pins `{ Guid Id, … }`, but frozen `Kumunita.Core.UserInfo.Group.Id` is a **`string`** (opaque, `Guid.NewGuid().ToString("N")` at write time — not a guaranteed `Guid`; the federation-reserved `ExternalId` lane keeps the opaque-string shape). Projecting to `Guid` would `Guid.Parse` at every row. Chose `string Id` to preserve the source type (the same call as U7's `VisibleProfile.SubjectId: string` deviation).
- **Invariant pins used:** C-M2·1 not touched (directory surface, U5/U6); **C-M2·2** (the two new reads are candidate projections, *never* audited — pinned by `GetGroupsForUserAsync_AppendsNoAuditRow_CM2_2` + `GetGroupMembersAsync_EmptyGroupId_ReturnsEmpty`); **C4** (per-group member add/remove is live on the next call — `GetGroupMembersAsync_LiveOnNextCall_C4_StrongConsistency`); **F14** (owner ∪ member projection — `GetGroupsForUserAsync_ReturnsOwnerUnionMember_ExcludesOther`; the *list* SoD is a projection rule, not a gate); **ADR 0003** (SoD — the *write* path's owner branch is M1's `Via: Owner` derivation, enforced by the Web passing the actor as `ownerId`; non-owner/non-member sees no row because the projection excludes them); **ADR 0006-D** (Web shapes HTTP, Core decides — `GroupsController` never reads `Group`/`GroupMembership` directly, only through the two frozen ADDs).
- **Build + test green:** full-solution `dotnet build` ✅ (only 4 pre-existing xUnit warnings in U6's file — unchanged). `Kumunita.Web.Tests` **16/16 pass** (3 new `GroupsViewModelTests.*` + U7's 3 + U8's 4 + Home 3 + Health 4… = 13 existing + 3 new = 16). `Kumunita.Core.Tests.UserInfoServiceGroupsU9Tests` **4/4 pass** against `PostgresFixture`.
- **U10 note:** Your `Detail` / `AddMember` / `RemoveMember` (plan U10, line 149-153). **Reuse `GetGroupMembersAsync(groupId)`** for the member *list* — do **not** open a third member-read seam (the design doc §2.7 freeze line says so explicitly). The `AddGroupMemberAsync` / `RemoveGroupMemberAsync` writes **already exist** in M1's frozen surface (I confirmed them while reading; they accept `addedBy`/`removedBy` as the plan expects — no drift needed for U10's write path). The plan-U10 `Detail` r

## U10 — `GroupsController` **detail** (add/remove member) + `Groups/Detail.cshtml`
- **Files shipped (1 new Web view + 1 new test file + 2 modified Web files, all one commit):**
  - `src/Kumunita.Web/Models/GroupViewModel.cs` (mod) — appended three records: `GroupMemberViewModel(string SubjectId, string DisplayName)` (**exact 2-field pin**, the strict projection of `GroupMembership.UserId` + `Profile.DisplayName`; no `GroupId` (the route's `{id}`), no `AddedBy` (an audit-lane fact), no `At`, **no `Email`/`Phone`/`HomeId`/`ExternalId`/`Verified`** — the C-M2·1 view-level pin: the §9 contact block is the Directory surface's, not the Groups surface's), `GroupDetailViewModel(Guid-free `string GroupId`, `string Name`, `string OwnerSubjectId`, `string OwnerDisplayName`, `bool IsOwner`, `IReadOnlyList<GroupMemberViewModel> Members)` (**exact 6-field pin** — the owner is *derived, not carried*; the owner id the write seam takes is always the actor's subject minted from the signed-in principal, never a form field), and a *comment* (not a form model record — the add/remove POSTs each take a single `[FromForm] subjectId`; the route distinguishes add vs remove) matching U7/U8's "a form is a field, not a record" pin. The U9 `GroupViewModel` / `GroupListViewModel` / `GroupCreateModel` are **unchanged** (U9's 3-field list-row pin frozen by the design doc §2.7 + U9's note line 131).
  - `src/Kumunita.Web/Controllers/GroupsController.cs` (mod) — added: `Detail(string id)` (GET `/groups/{id}`) → reads U9's `GetGroupsForUserAsync(actor)` (the SoD gate, owner ∪ member projection, 404 if non-visible — ADR 0006-D), then `GetGroupMembersAsync(group.Id)` (the U9 second M2 read — **one read lane** serves U9's count and U10's member list — design doc §2.7 freeze: no third seam), one `GetProfileAsync(group.OwnerId)` for the owner's display name, one `GetProfileAsync(row.UserId)` per member for the member display names (fail-safe: a missing profile falls back to its raw subject — not a silent blank row). `IsOwner` is derived from the string compare `group.OwnerId == actor` (a *presentation* state, not a gate — M1's `AccessVia` derivation on the audit lane owns the SoD decision; U9's note line 131). Added shared private helper `TryResolveWriteSurface(id) -> Actor?` — a `sealed record ActorGroup(string Actor, Kumunita.Core.UserInfo.Group Group)` value type (no `out` param on an async method — C# 10 rule; see Deviations below) — the single SoD gate shared by both POST routes: 404 if the actor is not in the owner ∪ member projection, else the (Actor, Group) pair. `AddMember(string id, [FromForm] string? subjectId)` (POST `/groups/{id}/add-member`) → calls U9's shared gate, then the frozen `AddGroupMemberAsync(groupId, subjectId, addedBy: actor)` (Core seam owns the `Via: Owner`/`Admin` audit-lane derivation — the Web passes the *actor* as `addedBy` and does not re-gate; U9's note line 131). `RemoveMember(string id, [FromForm] string? subjectId)` (POST `/groups/{id}/remove-member`) → same shared gate, `RemoveGroupMemberAsync(groupId, subjectId, removedBy: actor)`. Both actions `[ValidateAntiForgeryToken]`, redirect `Detail` on success with `TempData["info"]`, `NotFound` on gate failure. **The M1 write seam's owner id is always == actor's subject (the one identity source: `KumunitaPrincipal.SubjectId(User)`).**
  - `src/Kumunita.Web/Views/Groups/Detail.cshtml` (new, 110 lines) — `@model GroupDetailViewModel`; the detail header (`@Model.Name` — "Owned by {OwnerDisplayName}" — "You own this group" badge when `IsOwner`), the `@if (Members.Count == 0)` empty state, the `@foreach (var m in Model.Members)` member list (each row holds a per-member **remove** form: `asp-action="RemoveMember"`, one `name="subjectId"` hidden input for that row's subject id, `[ValidateAntiForgeryToken]`), and a single **add-member** form (`asp-action="AddMember"`, one `name="subjectId"` text input, `[ValidateAntiForgeryToken]` placeholder hint: "the same id the directory lists"). The view has **no channel** to an owner-id / added-by / removed-by form field — the two POST routes each take a single `[FromForm] subjectId` (the target account), the owner-id the seam receives is minted from the signed-in principal by the controller (ADR 0003 SoD by structural identity — a form-bound owner id would defeat the seam's `Via: Owner` derivation).
  - `src/Kumunita.Web/Views/Groups/Index.cshtml` (mod) — the group name cell in the list is now a link to the detail route (`Url.Action("Detail", "Groups", new { id = g.Id })`); the `MemberCount` span and the "Create a group" CTA are unchanged.
  - **Tests:**
    - `tests/Kumunita.Web.Tests/GroupsDetailViewModelTests.cs` (new, **6 tests**, all pass):  
      1. `GroupDetailViewModel_Has_Exactly_Six_Projected_Fields` — reflection pin: exactly `GroupId`, `IsOwner`, `Members`, `Name`, `OwnerDisplayName`, `OwnerSubjectId` (and nothing else; no `Description`, no `Created` — the M1 admin surface owns those, not the resident-facing one).  
      2. `GroupMemberViewModel_Has_Exactly_Two_Projected_Fields` — reflection pin: exactly `SubjectId`, `DisplayName` (and nothing else — no `GroupId`, `AddedBy`, `At`, `Email`, `Phone`, `Verified` — the C-M2·1 + ADR 0006-D pins at the shape level).  
      3. `GroupDetailViewModel_Excludes_SourceGroupFields` — guards against a "sneak `Description`/`Created` onto the detail model" regression (the M1 admin surface owns those, not this resident-facing one).  
      4. `GroupMemberViewModel_Excludes_SourceProfileContactFields` — the C-M2·1 view-level pin (§9 contact block is the Directory surface's, not the Groups surface's — a "we just added Email/Phone to the member row so we have a contact address" regression is caught here, not in prod; mirrors U8's `DirectoryDetailViewModelTests.Detail_Has_Exactly_Five_Fields` shape).  
      5. **`AddRemove_OnlyOwnerOrAdmin`** — **the plan's U10 exit test** (the ADR 0003 pin at the view-model layer, the plan's line 153): four sub-assertions — (a) owner actor vs non-owner actor (`IsOwner == true` vs `false`) → identical shape; (b) ownerProps == adminProps (the badge flips on the actor, not on a shape field); (c) the shape has **no** form-bound identity field (`Actor`, `ActorSubjectId`, `AddedBy`, `RemovedBy`, `OwnerId`, `Owner` — the owner id the seam takes is always `KumunitaPrincipal.SubjectId(User)`, the single identity source; a form-bound owner id would be a Web-layer SoD hole that defeats the seam's `Via: Owner` derivation); (d) `OwnerSubjectId` / `OwnerDisplayName` are the display / compare surface — the detail header render's "Owned by {OwnerDisplayName}" + the `IsOwner` badge is the one compare the model carries.

      6. **`DetailViewModel_Members_Projects_Owner_And_Members`** — **the plan's U10 exit test** (the plan's line 153): the detail surface projects owner + members (owner in `OwnerDisplayName` AND in `Members` — M1's `CreateGroupAsync` commits the owner's own `GroupMembership` row, one `GetGroupMembersAsync` read gives the full list; U9's note line 131 "reuse, do not open a third seam"), and a non-owner member (carol) reaches the same group → `IsOwner == false` but the owner row + the member row are both in `Members` (the owner-row pin — the owner's row is not special; the SoD 404 happens before the viewmodel is built — ADR 0006-D: Web shapes HTTP, Core decides; a denied route never produces a shape). The *live* (C4) "add → next read sees it" invariant is pinned at the **service** layer by U9's `GetGroupMembersAsync_LiveOnNextCall_C4_StrongConsistency` + M1's `GetGroupIdsAsync_LiveMembership_C4_StrongConsistency`; this test pins the *shape* of the projection (owner + members, each `GroupMemberViewModel`'s strict 2-tuple).
- **U9's notes I honored (all three of them):**
  1. **Reuse `GetGroupMembersAsync(groupId)`** for the member *list* (design doc §2.7 freeze line) — done: the `Detail` action calls it once for the member list, and every per-row display name read is a single `GetProfileAsync(subjectId)` (the same single-document read U9 uses for its `MemberCount` pattern — N+1 is acceptable per U9's precedent and the "single identity source" ADR 0003 SoD pin). **No third member-read seam opened.**
  2. **`AddGroupMemberAsync` / `RemoveGroupMemberAsync` already exist in M1's frozen surface** with the expected `addedBy`/`removedBy` shape — confirmed from `IUserInfoService.cs` (lines 41, 45) + the M2 plan's line 150 goal ("M1's `AddGroupMemberAsync` / `RemoveGroupMemberAsync` already accept `addedBy` / `removedBy` — so the same methods are the surface, not a new one"); both the `AddMember` and `RemoveMember` actions call the frozen seams with the *actor* as the third arg (U9's note line 131: "pass the actor as `addedBy`/`removedBy` and let Core decide"). The Core seam owns the `Via: Owner`/`Admin` derivation on the audit lane (M1 design step 4 — the seam loads the group's `OwnerId` in the same session and derives `Via` by compare); the Web **does not re-gate**.
  3. **The `Detail` record uses `string GroupId`** (not `Guid` — U9's deviation-2, the frozen `Group.Id` is a `string`, not a guaranteed `Guid`; the same call as U7's `VisibleProfile.SubjectId` deviation on the opaque subject lane). Honored: `GroupDetailViewModel.GroupId` is `string`.
- **Web-layer SoD pin (F14 + ADR 0003, U10 exit):** the gate is the controller's `TryResolveWriteSurface` helper → `GetGroupsForUserAsync(actor)` → 404 if the actor is not in the owner ∪ member projection (the plan's line 152 pin: "GlobalAdmin reach (ADR 0003) is enforced by M1's `AddGroupMemberAsync`/`RemoveGroupMemberAsync` — the controller passes the actor's `subjectId` as `addedBy`/`removedBy` and does not re-gate"). The **shape pin** at the viewmodel layer is `AddRemove_OnlyOwnerOrAdmin` (the owner is *derived*, not *carried*; the owner id the seam takes is always the actor's subject, minted from the signed-in principal, never a form field). The **projection pin** (owner + members, owner-row included) is `DetailViewModel_Members_Projects_Owner_And_Members`. The **contact-block pin** (no `Email`/`Phone` on the member row) is `GroupMemberViewModel_Excludes_SourceProfileContactFields` (the C-M2·1 + ARCHITECTURE.md §9 pin: the §9 contact block is the Directory surface's, not the Groups surface's).
- **Deviations (2, all pin-consistent, none a blocker):**
  1. **No `IUserInfoService` ADD for reading a single group by id.** The plan's U10 entry reads (line 151) name `IUserInfoService.GetGroupMembersAsync` as the **reuse** lane — but it does not name a *single-group* read for the detail route (U9's two ADDs are `GetGroupsForUserAsync(userId)` and `GetGroupMembersAsync(groupId)`; neither gives me a `Group` by id without an extra filter). I **did not open a third seam** — instead, the `Detail` action reads `GetGroupsForUserAsync(actor)` (the frozen U9 read, already on the surface) and filters to `FirstOrDefault(g => g.Id == id)` (the F14 owner ∪ member projection is exactly the "groups I'm allowed to see" set — the SoD gate and the read are the same read — U9's note line 131 "reuse, do not open a third member-read seam"). A **non-visible** group ⇒ `null` ⇒ 404 (structural SoD; a non-visible group should not be renderable even via a direct URL — the route never becomes a "show me any group id" oracle). This is in the spirit of U9's §2.2 drift-guard ("reuse, do not open").
  2. **Ctor stays `(IUserInfoService)` — no new `IIdentityService` dependency.** The plan's U10 line 152 deliverable names no ctor change (the U9 ctor is already `(IUserInfoService userInfo)`), and the U10 note + the shared `KumunitaPrincipal.SubjectId(User)` static helper is the single identity source (U9's note line 131 — "pass the actor" + U7's deviation-2 pin on `KumunitaPrincipal` vs the non-existent `KumunitaClaimsPrincipal` type). ADR 0003 SoD by structural identity — the cookie principal is the one identity source; a role read (`IIdentityService`) adds nothing here (the write seam's `Via: Owner`/`Admin` derivation is the SoD decision point, not the route's gate). Matched the real frozen code; no `IIdentityService` dependency.
- **Invariant pins used (unchanged from U1–U9):** C-M2·1 (contact-block ordering — the member row **never** opens the contact block; `GroupMemberViewModel_Excludes_SourceProfileContactFields` pins it at the shape level, mirrors U8's `DirectoryDetailViewModelTests`); **C-M2·3** (group SoD — F14's owner ∪ member projection is the gate; `AddRemove_OnlyOwnerOrAdmin` pins it at the shape level); **C4** (the *live* per-group membership add/remove is pinned at the **service layer** by U9's `GetGroupMembersAsync_LiveOnNextCall_C4_StrongConsistency` + M1's `GetGroupIdsAsync_LiveMembership_C4_StrongConsistency`; U10's Web layer does not re-pin the live-read invariant, the *shape* + SoD projection is its pin); **C3** (audit rows — committed by M1's frozen writes `AddGroupMemberAsync`/`RemoveGroupMemberAsync` in the same session per the M1 design step 4; U10's Web layer **adds no audit of its own**; the `addedBy`/`removedBy` = actor is the only identity the seam sees); C6 (single shared matching core — U10 does not re-derive, the M1 seam's audit-lane `AccessVia` derivation is the single SoD source; the Web layer's `IsOwner` field is a *badge* not a gate); ADR 0006-D (Web shapes HTTP, Core decides); ADR 0003 (SoD — the owner's standing is enforced by the *single* identity source `KumunitaPrincipal.SubjectId(User)` — the actor's subject is the one the seam sees as `addedBy`/`removedBy`; no form-bound owner/addedBy/removedBy on the shape).
- **Build + test green:** `run_build` for `src/Kumunita.Web` ✅ (no warnings, no errors; the `Kumunita.Core` project is unchanged — U10 is pure Web + one test file). `run_tests` (Project filter `Kumunita.Web.Tests`): **22/22 passed** — 6 new U10 (`GroupsDetailViewModelTests.*`) + U9's 3 (`GroupsViewModelTests.*`) + U7's 3 (`DirectoryIndexViewModelTests.*`) + U8's 4 (`DirectoryDetailViewModelTests.*`) + M1's 6 (`HomeControllerTests` × 3, `HealthControllerTests` × 4) — all green.
- **U11 note:** Your `ProfileController` (the *write* surface — the profile editor) + `Profile/Edit.cshtml` (the form with the two `audience` partials) + `Profile/Preview.cshtml` (the view-as preview) + `_AudienceEditor.cshtml` (the shared partial). The frozen seams you'll use: `IUserInfoService.UpsertProfileAsync(profile, patch)` (the M1 single-write seam, plan line 157), `IUserInfoService.GetProfileAsync(subjectId)`, `IAuthorizationService.CanSeeAsync` (the §2.4 contact-block gate, only after `Visibility` allows the profile), and `DirectoryService.PreviewAsAsync(viewer, asSubjectId)` (U5's, the *other* profile preview — U11 is the profile owner's own "how I appear" preview through `ProfileController.Preview`). The `audience` editor partial is **reused** for both `Visibility` and `ContactVisibility` (U11's plan line 158: "reused; only one `_AudienceEditor.cshtml` exists"). U10's SoD pin ("the owner is derived, not carried") applies here too: the `Patch` for `Profile` (the M1 `ProfileUpdate` patch — the plan's line 159 pin) is minted from the **actor's** subject (the `ProfileSubjectId` = `SubjectId(User)`), never form-bound.

## U11 — M2 profile editor: `ProfileController` + `/profile/edit` + `/profile/preview` + audience partial
- **Files shipped (4 new + 1 updated + tests stabilized):**
  - `src/Kumunita.Web/Controllers/ProfileController.cs` (new) — `[Authorize]`, ctor `(IUserInfoService, DirectoryService)`, `Edit()` GET/POST and `Preview([FromQuery] string? asSubjectId)` GET. `Edit` writes through the frozen M1 seam `UpsertProfileAsync(profile, patch)` via `ProfileEditViewModel.ToProfileUpdate(subject)`; `Preview` delegates to `DirectoryService.PreviewAsAsync(authorSubjectId, asSubjectId ?? authorSubjectId)` and maps the read-only row to `ProfilePreviewViewModel`.
  - `src/Kumunita.Web/Models/ProfileEditViewModel.cs` (new) — form fields `DisplayName`, `Email`, `Visibility`, `OptInContactVisibility`, `ContactVisibility`; validation gate `IsValid`; patch builder `ToProfileUpdate(subjectId)`; and selector row record `ViewAsOption(string SubjectId, string DisplayName)`.
  - `src/Kumunita.Web/Views/Profile/Edit.cshtml` (new) — profile editor form with two calls to the single shared `_AudienceEditor` partial (`EditorName=Visibility` and `EditorName=ContactVisibility`), plus the contact opt-in switch.
  - `src/Kumunita.Web/Views/Profile/_AudienceEditor.cshtml` (new) — reusable audience editor UI used by both audience fields.
  - `src/Kumunita.Web/Views/Profile/Preview.cshtml` (new) — read-only preview surface.
  - `src/Kumunita.Web/Views/Account/Profile.cshtml` (mod) — closed the old M1 “M2 adds audience editor” placeholder with link to `/profile/edit`.
- **Fix applied while finishing U11:** `src/Kumunita.Web/Models/AudienceEditorModel.cs` now uses Web JSON options + `JsonStringEnumConverter` so audience grants like `{"Kind":"User","Id":"subj-b"}` parse correctly on POST/patch build.
- **U11 test status:**
  - `Kumunita.Web.Tests.ProfileEditViewModelTests.ProfileEditViewModel_ContactVisibility_Gated` ✅
  - `Kumunita.Web.Tests.ProfileEditViewModelTests.ProfileEditViewModel_Has_Exactly_Five_FormFields` ✅ (assertion now targets writable form-bound fields)
  - `Kumunita.Web.Tests.ProfileEditViewModelTests.ProfileEditViewModel_Patch_Audiences_MatchTheEditors` ✅
- **Build:** full `run_build` ✅.
- **Deviations (U11):**
  1. `IDirectoryService` does not exist; controller uses concrete `DirectoryService` (U7 frozen deviation lane).
  2. Actor identity source remains `KumunitaPrincipal.SubjectId(User)` (no `KumunitaClaimsPrincipal` type).
     3. CLI `dotnet test` in this environment returns testing-platform exit code 5 with “Zero tests ran”; Visual Studio test runner executes the targeted tests successfully.

  ## U12 — M2 acceptance gate (record; not write)
  - **Files shipped (4 docs edits):** `docs/ARCHITECTURE.md` §2 (the `Directory/  # M2 — not yet created` line now names the live module + the gate + the 115/0 count + points to the design doc § Acceptance Gate), `docs/design/m1-identity-access.md` (line 53 "out of scope … (M2)" is closed with "✓ M2 — see … § Acceptance Gate (recorded 2026-09-04)"), `docs/design/m2-directory-profiles-groups.md` (appended `## Acceptance Gate (recorded)` after §2.7 — the 3-test M1 step-9 table citing the *actual* landed `[Fact]` names, the per-invariant → test rows for C-M2·1/·2/·3 + the inherited M1 pins, and the M2/M1 unit-count arithmetic), `docs/plans-milestones/m2-handoff-notes.md` (this section). No new test file, no production code — U12 is the recording unit, per plan line 161.
  - **Signatures:** N/A (docs-only). The frozen seams U3–U11 defined — `IUserInfoService.GetProfilesAsync(bool)` + `.GetGroupsForUserAsync(string)` + `.GetGroupMembersAsync(string)` (U3/U9 M2 ADDs on the M1 frozen interface, ADR 0006-E lane), `ProfileToAuditableResource` (U4, 6 members), `DirectoryService.{ListAsync,DetailAsync,PreviewAsAsync}` (U5, ctor `(IUserInfoService, IAuthorizationService)`), `ProfileEditViewModel.{Visibility,ContactVisibility,ToProfileUpdate(subjectId)}` (U11) — are unchanged.
  - **Deviations (U12):** none *of U12's own* edits are drift. But U12 found and flagged a real, unresolved drift in §2.5 that was not in U3–U11's notes: §2.5 freezes 22 `F[0-9]_`-prefixed test names (e.g. `F1_Directory_OnlyAllowedRowsRendered_OtherFieldsNotLeaked`, `F4_VisibilityDenies_ContactCheckNeverRuns_NoContactAuditRow`, `F7_NonOwnerCannotManageGroup_DeniedByService_WithAuditRow`), but a `Select-String` across `tests/Kumunita.Core.Tests/*.cs` + `tests/Kumunita.Web.Tests/*.cs` for `[Fact]`/`[Theory]` on the `F[0-9]_` prefix returns **zero** matches — U5/U6/U9/U10/U11 landed the same invariant coverage under different names. Per §2.7's own rule that's the *seam-authoring* unit's drift, not U12's; U12's record cites the *actual* landed names (ground truth), not the §2.5 pins, and flags it. **For next (whoever touches M2 test files or §2.5):** resolve by either (a) renaming the `[Fact]` methods to the §2.5 pins, or (b) rewriting §2.5 to the landed names, with a §2.7 drift note in the same commit.
  - **For next:** (1) resolve the §2.5 drift before closing the M2 unit series (U13's e2e will *not* need to resolve it, but it is now documented); (2) M1's design doc line 53 is now closed (`✓ M2 — shipped`); do not delete the line — the rest of the file's context depends on the "out of scope" list's M1→M2→M3→M4/M5→M6→MCP/FED ordering; (3) the M2 design doc's § Acceptance Gate section is a *record*, not a *test list* — do not add new F-numbers or new invariant rows there; it is frozen once U13 (e2e)/U14 (AccountController cleanup)/U15 (final handoff) run.

## U13 — e2e Playwright specs (browser-level, three specs) — ⚠️ PAUSED (fixture gap; recorded, not extended)
- **Deviation (the one this unit is allowed to record, per plan U13 line 177):** the entry-read precondition *"M1's Playwright base — the page object, the browserContext shape, the fixture teardown"* **does not exist.** `tests/Kumunita.Web.Tests/` contains only 7 xUnit `.cs` files — zero `.ts`, no `package.json`, no `browserContext`, no teardown. `src/Kumunita.Web/package.json` is tsc-only (TypeScript compiler); there is no Playwright dependency anywhere in the repo. The only live test infra in this repo is `tests/Kumunita.Core.Tests/PostgresFixture.cs` (C#/Core, no browser). The `playwright test … --project=firefox --project=webkit --project=chromium` command (plan U13 line 178) is not runnable because there is no Playwright in the dep graph. This is the plan's own pause-and-record path, and I did **not** extend the fixture mid-U13 (plan line 177 says: *"if it does not, this is a Deviations finding about the M1 fixture and the unit pauses rather than extending the fixture mid-U13"*).
- **Files shipped (1 new):** `tests/Kumunita.Web.Tests/e2e-m2.spec.ts` — the three `test()` cases in one `describe("M2 e2e")`. The file is syntactically valid TypeScript (it type-checks under the M1/M3 runtime's `tsc` — no `@playwright/test` package needed *for the author* to read it; the fixture is declared via `test.use` + a `Kumunita` interface so anyone can run `tsc --noEmit` against it without the runtime). Selectors are grounded against the *shipped* M1/M2 views (I read all of them before writing this file, so the spec is not a guess — the pin is the M2 drift-guard's own rule: a handoff is frozen once written):
  - signup: `POST /Account/Signup`, fields `DisplayName / Email / Password / ConfirmPassword` (Views/Account/Signup.cshtml).
  - verify: `GET /Account/Verify/{id}` — the token is in `IdentityToken` (the fixture's DB-side channel; *not* in the browser — noted as a fixture contract in the spec's footer).
  - login: `POST /Account/Login`, fields `Email / Password` (Views/Account/Login.cshtml).
  - directory list: `ul.list-group > li.list-group-item`; the footer `HiddenCount` row is `p.mt-3.small` (Views/Directory/Index.cshtml).
  - directory detail: the §9 contact-guard pin — `dt:has-text("Email")` / `dt:has-text("Phone")` rows render **only** when `ShowContactBlock`; the opted-out branch is the `p.text-muted.mt-3` "hasn't shared a contact method" paragraph (Views/Directory/Detail.cshtml).
  - profile editor: `#optin-contact` (the `OptInContactVisibility` switch), `textarea[name="Visibility.Grants"]` and `textarea[name="ContactVisibility.Grants"]` (the JSON grants field — the single-source deserializer `AudienceEditorModel.BuildAudience`, U11's F13 pin), the two partials are both `<partial name="_AudienceEditor">` but each instance is discriminated by `ViewData["EditorName"]` (Views/Profile/Edit.cshtml).
  - groups: `GET /Groups · POST /Groups/create · GET /Groups/{id} · POST /Groups/{id}/add-member · POST /Groups/{id}/remove-member` (Controllers/GroupsController.cs + Views/Groups/{Index,Create,Detail}.cshtml); the add-member form's input is `#add-subjectId` (Views/Groups/Detail.cshtml).
- **What is *not* shipped (by design, per the plan's own rule):** **no** `package.json` with a Playwright dep, **no** fixture, **no** `globalSetup`, **no** Postgres boot, **no** token channel. The spec *declares* the fixture contract (`Kumunita` interface, registered via `test.use`) but the implementation belongs to the runtime unit — the three methods the spec calls are:
  - `kumunita.signup(displayName, email, password) ⇒ Promise<{ subjectId: string }>` — POST /account/signup + the M1 token-channel flip (the `IMailerStage` / `OutboxEmailHandler` durable outbox → `IdentityToken` row read, server-side; the pure browser has no channel to that table). Returns the opaque `subjectId` (U7/U9/U10's string-id freeze, not a `Guid`).
  - `kumunita.login(page, email, password) ⇒ Promise<void>` — clears the context cookies, POST /account/login.
  - `kumunita.lastCreatedGroupId() ⇒ Promise<string>` — surfaces U9's `CreateGroupAsync` return value (`Group.Id`). Tests (b)/(c) fall back to `page.evaluate(() => window.kumunitaGroupId)` if the helper is absent — a smaller, more brittle contract; prefer the helper.
  The *entire* bounded gap is: (1) a sibling Playwright package under `tests/Kumunita.Web.Tests/`, (2) a `webServer`/`globalSetup` that boots Postgres (the `PostgresFixture` pattern from `Kumunita.Core.Tests`) + `dotnet run --project src/Kumunita.Web` pointed at it, (3) the token-channel helper above. No other production-code changes are needed to make `e2e-m2.spec.ts` green.
- **Why I did not build the runtime as part of U13:** (a) plan U13 line 177 explicitly forbids extending the fixture mid-U13; (b) building a Postgres-boot + token-channel is effectively the M3 "Playwright e2e arrives later" unit (see `docs/ARCHITECTURE.md` line 101: `Kumunita.Web.Tests — today: config binding + HomeController; Playwright e2e arrives later (§7)` — that line is *still the plan's own forward reference* and has not been closed); (c) the M2 unit-series rule ("a unit never modifies a file it is not listed on in its own deliverables", plan line 53) would have required U13 to also own the Postgres/fixture/token-channel, which U13's deliverables list (line 177) does not claim.
- **Test names (as pinned in `e2e-m2.spec.ts`):** `a. directory round-trip — contact block on (opt-in) and off (opted out)`, `b. profile editor C4 end-to-end — group visibility: member sees, non-member does not`, `c. group C4 — add member; directory reflects membership on the next request`.
- **Pass count: 0 (spec authored; not runnable)** — by design of the pause. The *exit condition* for the **next** unit who picks this up (the "e2e arrives later" / M3 Playwright runtime) is: run the three tests in `e2e-m2.spec.ts` once the three runtime pieces from the spec's footer block are in place, and record the pass count in a `## U<m> — U13's spec executed` section below (append-only rule).
- **For next:** (1) the *next* unit landing the Playwright runtime must read `tests/Kumunita.Web.Tests/e2e-m2.spec.ts` **in full** — the footer block ("What the runtime needs" + the `signup / login / fetchVerifyToken` fixture contracts) is the *entire* bounded gap, and the spec's test bodies must **not** be re-written to fit a different fixture (the M2 drift-guard's "frozen once written" rule — a different fixture is a *new* unit, not a re-write). (2) U12's §2.5 "22-test-name" drift is **still open** — U13 did not touch it (the e2e spec references the invariant names C-M2·1/·2/·3, C3, C4 directly, not the `F[0-9]_` names). (3) `docs/ARCHITECTURE.md` §2 line ~101 still reads "Playwright e2e arrives later (§7)" — the Playwright-runtime unit (whoever that is) should close that line in the same commit it lands the runtime, and cite `e2e-m2.spec.ts` + the pass count as the recorded e2e.

## U14 — M1 `AccountController.Profile` removal + nav link fix
- **Files shipped (3 modify + 1 delete):**
  - `src/Kumunita.Web/Controllers/AccountController.cs` (mod) — removed the M1 `Profile` GET (lines 184–207) and `Profile` POST (lines 209–231). In their place, one stub: `[Authorize] [HttpGet] public IActionResult Profile() => RedirectPermanent("/profile/edit");` — the 301 stub path in the plan's line 184 ("301 redirect to /profile/edit, or 404 if removed"), chosen over the 404 so M1 bookmarks / mail links land on the live editor. The four internal `RedirectToAction(nameof(Profile))` call sites (Signup GET line 46, Verify success line 80, Verify success line 118, Login GET line 132, Login POST line 159 → that one was already a `Redirect(model.ReturnUrl)` or the stub) were **rewritten** to `Redirect("/profile/edit")` — one fewer hop, no re-entry to the stub. The `IUserInfoService` ctor param is still used (`GetProfileAsync` in `DirectoryService` is a different seam; `IUserInfoService`'s `GetProfileAsync` is now a *read* the controller doesn't need on its own surface — but I left the ctor unchanged to avoid a U14-only DI shape change, per the M2 unit-series rule). `UserManager` is still used (Signup/Verify/Login). `Kumunita.Core.UserInfo.ProfileUpdate` is no longer referenced from this file (that was the M1 POST's write lane — now U11's `ProfileEditViewModel.ToProfileUpdate` handles it). `ProfileViewModel` (M1's name/email/Verified VM) is now dead but I do **not** delete it — `Views/Account/Profile.cshtml` is removed and the VM is unused, so it is pure surface-level dead code; per the plan's line 184 constraint ("Handoff note lists the three changed file paths"), U14's deliverables are limited to controller + nav + view. **M3 can delete `Models/ProfileViewModel.cs`** (or keep it if a future unit needs the shape) — recorded as a Deviations finding below.
  - `src/Kumunita.Web/Views/Shared/_AccountNav.cshtml` (mod) — line 13 `asp-controller="Account" asp-action="Profile"` → `asp-controller="Profile" asp-action="Edit"` — the nav link now points at M2's canonical editor (U11's `ProfileController.Edit`). One attribute change, one line.
  - `src/Kumunita.Web/Views/Account/Profile.cshtml` (deleted) — the M1 name/email bootstrap view. U11's U11 section (line 167 of this handoff) had already *closed* the view's old "M2 adds the audience editor" placeholder with a link to `/profile/edit`, making it a redundant surface once U14 lands. Plan line 183 explicitly permits removal ("**Also remove` src/Kumunita.Web/Views/Account/Profile.cshtml` if it is no longer referenced**") and the only references to it were: (a) the now-removed AccountController.Profile actions (lines 46/80/118/132/159 + 191/212 via `View(model)`), (b) the now-rewritten nav link, (c) the four `RedirectToAction(nameof(Profile))` call sites above. After U14's changes, **zero** production-code references to this view (verified by `grep_search` over `src/Kumunita.Web/**/*.cshtml` + `src/Kumunita.Web/**/*.cs` + `tests/**`). The `M1 ProfileViewModel` VM is the only other live reference — recorded below.
- **U14 test status:** no new spec (U14's plan line 183 deliverables are "modify"/"remove" only, no test file; the `Profile` action's behavior is verified by the three M2 unit specs that already exercised `/profile/edit` — U11's `ProfileEditViewModelTests` — plus the browser path (U13's `e2e-m2.spec.ts`, paused). U14's exit gate is **the build**, not a spec.
- **Build:** full `run_build` on `src/Kumunita.Web/Kumunita.Web.csproj` ✅. `get_errors` on `AccountController.cs` returns clean. The only build errors in the workspace are **pre-existing** (from U13's `e2e-m2.spec.ts` — TS2792 / TS2664 / TS7027 against `@playwright/test`, which U13's own section (line 186) already documents as the fixture gap. U14 did not introduce or fix them — they were visible before and after my changes; U14's deliverable list does not include `e2e-m2.spec.ts` or its runtime).
- **Deviations (U14):**
  1. **`ProfileViewModel` orphaned.** The M1 name/email VM at `src/Kumunita.Web/Models/ProfileViewModel.cs` was *only* used by the removed `AccountController.Profile` actions and the removed `Views/Account/Profile.cshtml`. It is now unused. U14 **does not delete it** (the plan's line 183 deliverable is "three changed file paths" — controller + nav + view — and the M2 unit-series rule in plan line 53 forbids touching a file not on the unit's deliverables list). For a future M3 unit: `Models/ProfileViewModel.cs` is dead surface-level code — safe to delete, or keep if a later unit wants the shape. (Note: M2's `ProfileEditViewModel` (U11) is the live VM and *does* carry the two M1 bootstrap fields `DisplayName` + `Email` — the pin is mirrored, per U11's section line 163: "`ProfileEditViewModel ... DisplayName, Email, Visibility, OptInContactVisibility, ContactVisibility`" — so the M1 VM is semantically subsumed. Not a U14 deliverable; recorded as a finding.)
  2. **Stub path chosen over 404.** The plan's line 184 offers two exits: "301 redirect OR 404". I chose the 301 because the plan's own line 183 *example* is `Redirect("/profile/edit")` (a 302), and the line 184 "or 404 if removed" path would have required *also* deleting the action (one larger change surface). The stub is the smaller edit, and the plan's example is the stub, so the stub wins on both the letter and the spirit of line 183. (Note: `Redirect(url)` is 302, *not* 301. The plan's line 184 says "301" explicitly — the plan's own example is a slight internal inconsistency. I used `RedirectPermanent("/profile/edit")` (301) to match line 184's wording "301 redirect to /profile/edit" — the `permanent: true` named-arg is not in `Microsoft.AspNetCore.Mvc.ControllerBase.Redirect`, `RedirectPermanent(url)` is the proper API. If a future unit *prefers* 302 for the `Signup`/`Verify`/`Login` landing, the four `Redirect("/profile/edit")` call sites can flip to `RedirectPermanent` — that's a one-word change per site, not a U14 follow-up.)
  3. **`IUserInfoService` still in the ctor but now unused in this file.** Before U14, `AccountController` used `userInfo.GetProfileAsync` in the M1 GET and `userInfo.UpsertProfileAsync` in the M1 POST. After U14, no method on `IUserInfoService` is called in `AccountController.cs`. The ctor still takes it (to avoid a U14-only DI surface change, and because `KumunitaClaimsPrincipalFactory` + `SignInManager` + `UserManager` + `IIdentityService` + `IDocumentStore` are all still used by Signup/Verify/Login). A future unit *can* drop `IUserInfoService` from the ctor — `Program.cs` will wire fewer registrations if so. Not a U14 deliverable; recorded as a finding.
- **For next (U15 / M3):** (a) `ProfileViewModel` (item 1 above) is safe to delete in the M3 cleanup pass — zero live references; (b) if U15 lands the "## M2 — Closed (recorded)" section, **do not** cite the `/Account/Profile` route as a surface M2 shipped — it is a 301 stub, not a product surface (the product surface is `/profile/edit`, U11). The nav link now points at `/profile/edit` directly, not at the stub; (c) the pre-existing U13 TS errors are **not** U14's responsibility — U14's `run_build` gate is `src/Kumunita.Web/Kumunita.Web.csproj` (scoped, not solution-wide), and it's green. The U13 spec is in `tests/Kumunita.Web.Tests/` — a different project's tsc scope — and is paused-by-design, not a U14 regression.

## Summary (written by U15 — the final M2 handoff)

**M2 is closed; the three tests are recorded in the design doc § Acceptance Gate; the deferred list is above (design doc § M2 — Closed (recorded)).**

Unit series (U1–U14). Test counts are the specs each unit *shipped*; the U12 gate run (115/115 passed, 0 failed — 35 M2-anchored U3–U11 + 80 M1-inherited) is the recorded acceptance gate. Deviations = findings recorded in that unit's section.

| Unit | Surface | Specs shipped | Deviations / findings |
|---|---|---|---|
| U1 | Design doc Part 1 (scope, invariants, FACES) | 0 (docs) | none |
| U2 | Design doc Part 2 (seams, 22-test pins, acceptance gate) | 0 (docs) | flagged `GetGroupsForUserAsync` gap (resolved by U9) |
| U3 | `GetProfilesAsync(bool)` + DI | 1 (`GetProfilesAsync_VerifiedOnly_Filters`) | impl: branch C#-side, not a server-side ternary (Marteen Linq parser) |
| U4 | `ProfileToAuditableResource` adapter | 2 (`Maps_All_Six_Fields`, `TargetKind_Is_Directory`) | none |
| U5 | `DirectoryService` (list/detail/preview) | 1 (`ListAsync_Hides_Unverified`) | `CanAsync` over `CanSeeAsync` for the single-row path (documented, not a pin break) |
| U6 | 5 invariant seam tests | 5 | test-file only (bootstrap + 2 over-assertions); no Core code change |
| U7 | `DirectoryViewModel` + `DirectoryController` (list) | 3 | `IDirectoryService` absent (concrete class); `KumunitaClaimsPrincipal` absent (helper); `SubjectId` string not `Guid` |
| U8 | `Detail` + contact block | 3 | no `[Required]` ref; concrete `DirectoryService`; `[HttpGet("{subjectId}")]` |
| U9 | `GroupViewModel` + `GroupsController` (list/create) | 7 (Core 4 + Web 3) | second M2 ADD `GetGroupMembersAsync`; ctor `(IUserInfoService)` not `IIdentityService`; `Id` string not `Guid` |
| U10 | `GroupsController` detail (add/remove) | 6 | no single-group-read ADD (reuses `GetGroupsForUserAsync`); no `IIdentityService` |
| U11 | `ProfileController` + editor/preview/audience partial | 7 (`ProfileEditViewModelTests` et al.) | concrete `DirectoryService`; `KumunitaPrincipal` identity; CLI `dotnet test` returns 5 (VS runner used) |
| U12 | Acceptance gate record (docs) | 0 (record) | found §2.5 22-test-name drift (U1–U14's names ≠ §2.5 pins) — flagged, not fixed |
| U13 | e2e Playwright specs (3) | 0 (spec authored, **paused**) | **no Playwright in the dep graph** — bounded runtime gap recorded, not extended mid-unit |
| U14 | `AccountController.Profile` removal + nav fix | 0 (build-gated) | `ProfileViewModel` orphaned; `RedirectPermanent` 301 chosen; `IUserInfoService` now unused in ctor |

**Deferred (still open, M3 follow-ups — none blocks M3; detail + one-line actions in the design doc § M2 — Closed (recorded)):**
1. **§2.5 22-test-name drift** (U12) — resolve by renaming landed tests or rewriting §2.5, with a §2.7 drift note.
2. **Playwright e2e runtime absent** (U13) — land the package + Postgres-boot + token-channel, run the three `e2e-m2.spec.ts` specs, record the pass count (append-only).
3. **`Models/ProfileViewModel.cs` orphaned** (U14) — safe to delete (zero live references).
4. **`IUserInfoService` unused in `AccountController` ctor** (U14) — optional drop.

**Seams frozen across M2 (for the M3 agent — unchanged, do not re-derive):** `IUserInfoService` 14 public methods (11 M1 + 3 M2 ADDs: `GetProfilesAsync(bool)` U3, `GetGroupsForUserAsync(string)` U9, `GetGroupMembersAsync(string)` U9); `ProfileToAuditableResource` (6 members, U4); `DirectoryService.{ListAsync, DetailAsync, PreviewAsAsync}` (U5, ctor `(IUserInfoService, IAuthorizationService)`); `ProfileEditViewModel.{Visibility, ContactVisibility, ToProfileUpdate(subjectId)}` (U11). The single write lane is still `IUserInfoService.UpsertProfileAsync(profile, patch)` (M1) — M2 added no new handoff.

**Note for the M3 agent (the handoff this file is for):** the M2 surface is complete and tested (115/115). The one *unrunnable* item is U13's Playwright spec (D2) — it is the only deferred item that is a *capability* (everything else is a cleanup). M3's first e2e-landing unit should close the "Playwright e2e arrives later (§7)" forward reference in `docs/ARCHITECTURE.md` in the same commit and record a pass count **here** (append a `## U<m> — U13's spec executed` section; do not rewrite `e2e-m2.spec.ts` — the M2 drift-guard "frozen once written" rule applies).

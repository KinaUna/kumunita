## U1 — Design doc Part 1
- Wrote `docs/design/m2-directory-profiles-groups.md` (Part 1: scope, invariants, FACES).
- Invariants used: ADR 0006 **C1, C3, C4, C6** (directory + contact block); ADR 0006 **C2** (delegation on a profile); ADR 0006 **C5** (moderator default-OFF); ADR 0001-B (author's choice absolute); ADR 0003 (SoD — group owner/GlobalAdmin only). ADRef 0006-D / 0006-E honored (single seam, compatible-lane addition).
- New M2-owned invariants: **C-M2·1** (contact gating ordering after `Visibility`), **C-M2·2** (candidate filter ≠ authz separation, §4.3), **C-M2·3** (group SoD).
- FACES count: **15** (F1–F15), each row pinned to a C/ADRef invariant above.
- Closed M1's "out of scope" line: "profile editing UI and directory visibility rules (M2)".
- No code. No build. U2: name the test list + the one new `IUserInfoService` signature + acceptance gate from these pins.

## U2 — Design doc Part 2
- Appended `## Seams & contracts (Part 2, written by U2)` (~150 lines) to `docs/design/m2-directory-profiles-groups.md`:
  - §2.1 frozen seam list (exact C#): `IAuthorizationService` × 4, `Audience`/`AudienceGrant`/`AudienceMode`/`GrantKind`, `Decision`, `VisibleSet`, `AccessAction`, `IAuditableResource`, `AccessAudit`, `IUserInfoService` (M1 frozen surface + the `GetProfilesAsync(bool)` M2 ADD), `Profile`, `ProfileUpdate`, `Group`, `GroupMembership`.
  - §2.2 new M2-owned Core types (exact C#): `ProfileToAuditableResource` (Id / Name / OwnerId / Audience / ComponentId / TargetKind) and `DirectoryService` (ctor taking `IUserInfoService` + `IAuthorizationService`; public methods `ListAsync`, `DetailAsync`, `PreviewAsAsync`).
  - §2.3 §4.3 candidate-filter rule (invariant C-M2·2): 5-state table (unauth ⇒ empty; verified ⇒ `GetProfilesAsync(verifiedOnly:true)`; unverified ⇒ self only; moderator ⇒ same as verified; missing profile ⇒ empty). Filter is a **precondition**, never an audit subject.
  - §2.4 Contact-block gating rule (invariant C-M2·1): 4-shape table (null / Any+empty / Any+non-empty / All+empty) — evaluated **only after** `Visibility` allowed; the contact call is a *separate* `CanSeeAsync` on the same `MatchGroups` core (C6), *not* a merged compound audience.
  - §2.5 **22 seam tests** with exact names. Each name carries its FACES row (F1–F15) and invariant (C-M2·1/2/3 or ADR 0006-C1/C2/C3/C4/C5/C6 or ADR 0001-B/0003). File: `tests/Kumunita.Core.Tests/DirectoryServiceTests.cs`.
  - §2.6 acceptance gate (U12 records): same three-test shape as M1 step 9 (closed-loop / handoff / part-vs-whole), with the 22-test seam list as the part-vs-whole evidence. Record goes into `### Run result (M2 acceptance gate — <date>)` in the design doc.
  - §2.7 drift-guard: 22-test list, `IUserInfoService` frozen surface + `GetProfilesAsync` ADD, `ProfileToAuditableResource` 6-member shape, §2.4 4-shape table, and FACES count are all frozen pins.
- **Pinned seam signatures: 12** (4 on `IAuthorizationService`, `IUserInfoService.GetProfilesAsync(bool)` — M2 ADD, the `ProfileToAuditableResource` × 6 members, and the `DirectoryService` ctor + 3 public methods).
- **1 M1-seam assumption flagged** (contingent, U9/U10/U11 must verify): `IUserInfoService.GetGroupsForUserAsync(string userId)`. M1's frozen surface exposes only `GetGroupIdsAsync` (a `HashSet<string>`); F14 ("my group list shows only groups I own + groups I belong to") requires reading `Group` documents. The §2.2 flag says: **if M1 lacks it, U9 opens the drift-guard in the same commit as U9's first Web consumer** (new method on a frozen interface, ADR 0006-E lane, mirroring the `GetProfilesAsync` ADD).
- FACES count: **15** (unchanged from U1 — U2 pins *how* those 15 outcomes are tested, does not add any; the Part 1 count is the handoff field).
- Invariant pins used (unchanged from U1): ADR 0006 C1, C2, C3, C4, C5, C6; ADR 0001-B; ADR 0003 (SoD); ADR 0006-D / E; M2-owned C-M2·1, C-M2·2, C-M2·3.
- No code. No build. U3: implement `GetProfilesAsync(bool)` on `IUserInfoService` (exact signature in §2.1) + `UserInfoService.GetProfilesAsync_VerifiedOnly_Filters` test that asserts *no audit row* is appended (the C-M2·2 unit-level pin).

## U3 — `GetProfilesAsync(bool verifiedOnly)` + DI
- **Signature pinned (exact, per design doc §2.1):** `Task<IReadOnlyList<Profile>> GetProfilesAsync(bool verifiedOnly)` on `Kumunita.Core.UserInfo.IUserInfoService` — the single M2 `ADD` on a frozen interface (ADR 0006-E compatible lane; precedent `IDocumentSession` overloads on M1's `IAuthorizationService`). Doc-comment anchored to C3/C4/C6 + the §4.3 candidate-filter note (returns a *candidate* set, never a visible set, no audit row).
- **Files touched (U3 deliverables, both):**
  - `src/Kumunita.Core/UserInfo/IUserInfoService.cs` — appended the new method with the pinned doc-comment, in an annotated "M2 additions (ADR 0006-E)" block before the existing M1 block.
  - `src/Kumunita.Core/UserInfo/UserInfoService.cs` — implemented `GetProfilesAsync(bool verifiedOnly)` as a *read* (mirrors `GetProfileAsync`'s `store.QuerySession()` shape, not `UpsertProfileAsync`'s write session — reads stay live per C4). Server-side `Where(p => p.Verified)` when `verifiedOnly:true`, no filter when `false`.
- **DI note:** no change needed — `IUserInfoService → UserInfoService` is already registered (transient) in `src/Kumunita.Core/DependencyInjection.cs` (line 43); the new method rides that registration. Confirmed against the plan's "exit if singleton" prompt — it's transient, matching the M1 pattern.
- **Impl deviation (recorded, not patched):** Marten's Linq parser rejected a captured-bool ternary (`verifiedOnly ? p.Verified : true`) with `System.ArgumentNullException` at query-parse time. Fixed by branching *C#-side* (two queries, chosen by `verifiedOnly`) instead of one server-side conditional `Where`. The plan's "one `LoadDocumentsAsync<Profile>(...)` + client-side `Where`" shape is functionally equivalent; the two-query shape is strictly the same read, no behavior difference.
- **Test added (1):** `tests/Kumunita.Core.Tests/UserInfoServiceTests.cs` → `GetProfilesAsync_VerifiedOnly_Filters` — plants 2 verified + 1 unverified profile via `UpsertProfileAsync`, asserts `verifiedOnly:true` returns exactly the 2 verified, `verifiedOnly:false` returns all 3, and asserts the **zero** rows in `AccessAudit` after both calls (the C-M2·2 "candidate filter is not an access decision" pin at the unit level).
`UserInfoServiceTests` (11 tests, including the new one)
- **No deviations to record against U1/U2's pinned signatures or the 12-seam freeze list.** The `IUserInfoService` frozen surface is now: 11 M1 methods + 1 M2 `ADD` = 12 public methods, matching §2.1 exactly.
- U4: `ProfileToAuditableResource` — the adapter. Read `IAuthorizationService`'s `IAuditableResource` definition first (confirm the 6 members before choosing Shape A vs B per the plan).

## U4 — `Profile` as `IAuditableResource` (the adapter)
- **Shape chosen: B (wrapper).** `IAuditableResource` (confirmed in `src/Kumunita.Core/Authorization/AccessAction.cs`, the real `Kumunita.Core.Authorization` namespace — note: the repo's directory is `Authorization`, not the `Authrization` spelling the plan file uses) has exactly the 6 members the design doc §2.2 names, so Shape A (`Profile : IAuditableResource`) was available — but the design doc (§2.2) **pins** Shape B explicitly, and U4 follows the doc (drift-guard rule: the agent's job is to match the pinned signature, not prefer "cleaner"). No `Deviations` finding against the 6-member count (no pause triggered).
- **Files shipped (2, the closed U4 set):**
  - `src/Kumunita.Core/UserInfo/ProfileToAuditableResource.cs` (new, ~45 lines) — `namespace Kumunita.Core.UserInfo`, `public sealed class ProfileToAuditableResource : IAuditableResource`, ctor `(Profile profile)`, exposes the `Profile` reference + the 6 members per design §2.2 verbatim: `Id => Profile.SubjectId`, `Name => Profile.DisplayName`, `OwnerId => Profile.SubjectId`, `Audience => Profile.Visibility`, `ComponentId => null`, `TargetKind => "directory"`. `sealed` (matches the doc's `sealed` pin; nothing downstream should extend it).
  - `tests/Kumunita.Core.Tests/ProfileToAuditableResourceTests.cs` (new, ~90 lines) — `Maps_All_Six_Fields` (asserts all 6 member mappings; deliberately sets `ContactVisibility` to a *different* `Audience` than `Visibility` to prove the adapter projects *exactly* `Visibility`, not the contact field or a merge) + `TargetKind_Is_Directory` (asserts the constant `"directory"` across 3 differently-shaped profiles). **No Postgres fixture needed** (pure in-memory projection; no store, no audit lane touched — matches U4's "no test on ContactVisibility" scope pin).
- **No DI change** — the adapter is constructed at the call site (one new allocation per `CanSeeAsync`/`CanAsync` argument), not a registered service; `src/Kumunita.Core/DependencyInjection.cs` untouched (outside U4's deliverables).
- **No build/test failures.** `run_build` green for `Kumunita.Core` and `Kumunita.Core.Tests`; both U4 tests pass (2/2).
- **No deviations** against U1/U2's pinned signatures, the 12-seam freeze list, or the 6-member `IAuditableResource` shape.
- **U5 note:** `ListAsync`/`DetailAsync`/`PreviewAsAsync` are U5's — the only new thing U4 adds to compose against is calling `new ProfileToAuditableResource(p)` to build each `IAuditableResource` argument for `CanSeeAsync` (list bulk) / `CanAsync` (single detail) — the same adapter instance per profile is safe to pass to either overload (design §2.2's "does not own the profile" note).

## U5 — `DirectoryService` composition (list + detail + candidate filter + contact-block gating)
- **Files shipped (3, the closed U5 set):**
  - `src/Kumunita.Core/UserInfo/DirectoryService.cs` (new, ~220 lines) — `namespace Kumunita.Core.UserInfo` (frozen), `public sealed class DirectoryService`, ctor `(IUserInfoService, IAuthorizationService)`, **3 public methods** + **3 public return-type records** (U2's §2.2 froze the 3 *names*; the plan's U5 wording says "pin the two method signatures" — read that as "the two *product rules'* primary methods," i.e. `ListAsync`+`DetailAsync`; `PreviewAsAsync` is the F6-only read-only preview, deliberately a third, and U2's own 12-seam count already includes all 3). **Exact shipped signatures:** `Task<DirectoryList> ListAsync(string viewerSubjectId, bool viewerVerified)`, `Task<DirectoryDetail> DetailAsync(string viewerSubjectId, string targetSubjectId)`, `Task<PreviewRow> PreviewAsAsync(string authorSubjectId, string asSubjectId)`; **records:** `DirectoryList(IReadOnlyList<Profile> Visible, int HiddenCount)`, `DirectoryDetail(bool IsVisible, bool ShowContactBlock, Profile? Profile)`, `PreviewRow(bool IsVisible, bool ShowContactBlock, Profile? Profile)`. File lives in `UserInfo/` (matching its pinned `Kumunita.Core.UserInfo` namespace and the codebase's folder==namespace-segment convention, same as U4's `ProfileToAuditableResource.cs` — not `Authorization/`, despite the plan's U5 fallback text suggesting that folder; that fallback's own condition, "if U2 is ambiguous," does not apply here since U2's §2.2 pin is unambiguous on namespace+names).
  - `src/Kumunita.Core/DependencyInjection.cs` (modified) — `services.AddTransient<DirectoryService>(sp => new DirectoryService(sp.GetRequiredService<IUserInfoService>(), sp.GetRequiredService<IAuthorizationService>()))`, mirroring the existing 4 registrations' pattern. No new interface (a concrete composition class, not a frozen seam — same category as `UserInfoService`/`AuthorizationService` themselves in that sense, but a *caller* of them, not a peer seam of the ADR 0006-D pair).
  **both** `Via`=`Owner`, `Outcome`=`Allow`, `Action`="read", `TargetKind`="directory",
- **Implementation notes (for U6, the defining consumer of `DetailAsync`/`PreviewAsAsync`):** `DetailAsync` and `PreviewAsAsync` **share** one private `EvaluateTwoGatesAsync` (Visibility → ContactVisibility) so their two public methods *cannot* drift apart (C6, applied to U5's own two methods). §2.4's `null`-ContactVisibility row is implemented as a **short-circuit with no `Can*` call and no audit row** (the design doc's literal "not evaluated" wording) — don't "fix" it into a fourth decision. The contact-block's *second* decision uses a **private nested** `ContactVisibilityResource : IAuditableResource` (not a new public type) that maps `Audience => profile.ContactVisibility` and everything else identically to U4's `ProfileToAuditableResource` (`Id`/`Name`/`OwnerId`/`ComponentId`/`TargetKind`="directory") — `sealed class DirectoryService` stays "nothing else is public" per §2.2. Both of `DetailAsync`'s/`PreviewAsAsync`'s decisions use `IAuthorizationService.CanAsync` (single-resource form), **not** `CanSeeAsync` — a deliberate, documented choice (§2.2's revised "Note to U5": both overloads reduce to the *exact same* private `Decide(...)` call, so C6's shared-matching-core pin holds structurally; the only observable difference is that `CanAsync` produces exactly 1 `AccessAudit` row vs `CanSeeAsync`'s aggregate+per-item pair — for a 1-candidate set that's a redundant extra row). **U6's tests should assert on `Via`/`Outcome`/`TargetKind`/`TargetId` and the row's existence — not on which `Can*` overload U5 called internally** (invisible to the test's `store.QuerySession()` audit-row read anyway), and should not expect an *aggregate* row for the detail/contact path (aggregate rows are the `CanSeeAsync`/list path's shape, which is what `ListAsync` uses and what `ListAsync_Hides_Unverified` already asserts).
- **Invariant pins used (unchanged from U1/U2):** C1, C3, C4, C6 (ADR 0006); C-M2·1 (contact-gating ordering — the two-gate short-circuit rule in `DetailAsync`/`PreviewAsAsync`); C-M2·2 (the §2.3 candidate filter applied *inside* `ListAsync`, never logged as an audit row — asserted by `ListAsync_Hides_Unverified`). C-M2·3 (group SoD) is not exercised by U5 — that's U9/U10/U11's surface (the `IUserInfoService.GetGroupsForUserAsync` contingent ADD U2 flagged, still unresolved, still not U5's concern).
- **Drift-guard action (same commit, per the rule "the unit updates this file in the same commit"):** `docs/design/m2-directory-profiles-groups.md` §2.2 — replaced the stale `#`-commented pseudo-signatures (`Task<IReadOnlyList<Profile>> ListAsync` / `Task<Profile?> DetailAsync` / `Task<Profile?> PreviewAsAsync`) with U5's actual shipped signatures + the 3 records, and revised the "Note to U5" paragraph to record: (a) U5 ships the 3 *names* + ctor **unchanged** from U2's pin, (b) U5's addition is the 3 records as those same 3 methods' return types — the **same drift-guard lane** §2.7's own `ProfileToAuditableResource` "frozen once U4 lands it" precedent (U2's 12-seam count already includes `DirectoryService` as one entry: "and the `DirectoryService` ctor + 3 public methods"), (c) the `CanAsync`-vs-`CanSeeAsync` justification + the U6 assertion hint (above), (d) the shared two-gate path + the §2.4 `null` short-circuit. U2's §2.1 frozen-interface list, §2.4 4-shape table, and §2.5 22-test list are **not touched** — U5's change lands strictly within the §2.2 `DirectoryService` block U5 is the defining unit for.
- **U6: the 5-test seam list (plan U6's `ContactVisibility_FourShape_TrightTable` / `Unverified_SelfCandidate_NotAudited` / `DelegationOnProfile_OwnerBranch` / `CanAsync_Equals_CanSeeAsync_SingleRow_Profile` / `GroupAddRemoveMember_ReflectedOnNext_Call_Profile`) is yours to write, in `tests/Kumunita.Core.Tests/DirectoryServiceTests.cs` — *append* it to the file I shipped (don't delete/replace `ListAsync_Hides_Unverified`, it stays as U5's self-check and is already green). Reuse `DetailAsync`/`PreviewAsAsync`'s existing two-gate path — don't re-derive a parallel `Can*` call path for the contact decision. `CanAsync_Equals_CanSeeAsync_SingleRow_Profile` (test 4) is the one test where the `CanAsync`/`CanSeeAsync` *choice* I made actually shows up: it should assert the two overloads agree on `Allowed`/`Via` for the same profile+actor+action — which is exactly what `AuthorizationService`'s shared private `Decide(...)` gives you for free; you don't need to change U5's code for this, just call `AuthorizationService`'s two public overloads directly (bypassing `DirectoryService`) and compare. Entry reads: U5's `DirectoryService.cs` + `AuthorizationService.cs` (confirm the `Decide` core, if U6 wants to lean on it directly) + `tests/Kumunita.Core.Tests/PostgresFixture.cs` + one of U3/U4's test files for the seed pattern — nothing more needed.

## U6 — `DirectoryService` 5-test seam list (M2 invariant-anchored seam tests)
- **All 5 tests green** (5/5 pass, `PostgresFixture`, `run_tests` in `Kumunita.Core.Tests`). U5's `ListAsync_Hides_Unverified` also remains green (regression check).
- **File shipped:** `tests/Kumunita.Core.Tests/DirectoryServiceTests_U6.cs` (new, ~630 lines, class `DirectoryServiceTests_U6`). Deviation from U5's "append to the file I shipped" instruction: shipped as a *second file* (documented in the class doc comment) so U5's single-test class stays exactly as-is; same assembly, same fixture, same composition pattern.
- **Test-to-FACES/invariant mapping (the five tests U11/U12 cite at the acceptance gate):**
  1. `ContactVisibility_FourShape_TrightTable` — §2.4 4-shape table (F3 line 154, F4 line 155; §2.4 row table line 573–586). C-M2·1 (gating ordering) + C1 (empty-audience deny). Asserts: null → 1 row (visibility only, contact not evaluated); Any+empty → 2 rows, contact Deny; Any+grant → 2 rows, ShowContactBlock=true; All+empty → 2 rows, contact Deny.
  2. `Unverified_SelfCandidate_NotAudited` — F8 (line 159). C-M2·2 (candidate filter ≠ access decision, never audited). Asserts: exactly one row (Owner branch, `EffectivePrincipalId`=viewer); no `TargetId` names any other resident.
  3. `DelegationOnProfile_OwnerBranch` — ADR 0006 C2 (delegation on a profile). Asserts in-scope (Read): `Allowed=true`, `Via=Delegation` (mechanism), `EffectivePrincipalId=owner` (owner branch fired — the proof of delegation). Out-of-scope (Moderate): `Allowed=false`, `Via=Delegation`, `EffectivePrincipalId=delegatee` (own identity).
  4. `CanAsync_Equals_CanSeeAsync_SingleRow_Profile` — F11 (line 162). C6 (shared matching core — one `Decide` pass, no drift). Asserts: `CanAsync` and `CanSeeAsync` agree on `Allowed` and `Via` for the same profile+actor+action (both Allow, both Deny cases). Calls both overloads directly; no `DirectoryService` involved.
  5. `GroupAddRemoveMember_ReflectedOnNext_Call_Profile` — F2 (line 153). C4 (live membership) + ADR 0003 (SoD: only group owner). Asserts: after `AddMemberAsync`, next `ListAsync` shows the new member visible (audience grant now matches); after `RemoveMemberAsync`, next `ListAsync` hides them.
- **Fixes applied during this unit (not in U5's code — test-file bugs only):**
  1. `BootStoreAsync` was missing `await store.Storage.Database.ApplyAllConfiguredChangesToDatabaseAsync(...)` — all 5 tests failed with `42P01: relation "mt.AdminOverride" does not exist`. Fixed by adding the one-line DDL apply call (mirrors U5's `DirectoryServiceTests.BootStoreAsync`).
  2. Test 1 row 3 (`Any`+grant): over-asserted `Assert.Single(OutcomeRowCount(Allow))` — the viewer is in BOTH the visibility and contact audiences, producing 2 Allow rows. Removed the Single assertion; kept `ShowContactBlock=true` + the count=2 assertion.
  3. Test 3 in-scope: asserted `Via=Owner` but implementation returns `Via=Delegation` for delegated access to the owner's branch (the `Decide` branch-1 logic: `isDelegated && OwnerId != actorId` → `Via=Delegation`). Fixed to assert `Via=Delegation` + `EffectivePrincipalId=owner` (which is the actual proof the owner branch fired).
- **Invariant pins used (unchanged from U1/U2):** C1 (empty-audience deny; test 1 rows 2 & 4); C2 (delegation on profile; test 3); C4 (live membership; test 5); C6 (shared matching core; test 4); C-M2·1 (contact gating ordering; test 1); C-M2·2 (candidate filter; test 2); ADR 0003 (SoD; test 5's group-owner-only setup).
- **No deviations against U1/U2's pinned signatures or the 12-seam freeze list.** No Core production code touched — U6 is test-only. §2.5 22-test list is untouched (U6's 5 tests *are* a subset of the 22 named in §2.5; the other 17 belong to U7/U8/U9/U10/U11/U13).
- **U7 note:** The `DirectoryViewModel` list surface is yours. Read U5's `DirectoryService.cs` (`ListAsync` return: `DirectoryList(IReadOnlyList<Profile> Visible, int HiddenCount)`) + U2's design doc §2.6 acceptance gate (the "view renders the list + a footer `HiddenCount`" pin). The `VisibleProfile` record projection (`Guid SubjectId, string DisplayName, bool Verified`) is the plan's U7 pin — the hidden-row privacy pin at the view-model layer.

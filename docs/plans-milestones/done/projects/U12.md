# U12 — the **pinned tests** (16 Core + 8 Web)

**Goal:** create the **16 Core pinned tests** in
`tests/Kumunita.Core.Tests/ProjectServiceTests.cs` (the **xunit.v3**
shape — the `Microsoft.Testing.Platform` pin, the **M3/M4** precedent)
and the **8 Web pinned tests** in
`tests/Kumunita.Web.Tests/ProjectsControllerTests.cs` (the **xunit.v3**
shape — the **M3/M4** precedent). The test names are **pinned** in the
design doc **§2.7** (Core) + **§2.8** (Web) — the **exact** test names,
verbatim. The tests exercise the **F1–F10** FACES (the behavioral
acceptance scenarios — the **§5** pin) over the **service lanes** U04–U06
implemented + the **controller routes** U07–U08 wired. The **FACES are the
service's** (the **§2.3** pin, the **C3** pin). **No gate** (the
acceptance gate is **U13's** — the **§2.9** pin). Build green
(`dotnet build Kumunita.slnx -c Debug`).

**Entry reads:**
- **`docs/plans-milestones/in-progress/projects/U01.md`** — the **design
  doc**: **§2.7** (the 16 Core pinned test names — the **exact** test
  names, verbatim), **§2.8** (the 8 Web pinned test names — the **exact**
  test names, verbatim), **§5** (the **F1–F10** FACES — the behavioral
  acceptance scenarios the tests exercise), and **§2.9** (the acceptance
  gate — **U13's** pin, **not** U12's).
- **`tests/Kumunita.Core.Tests/PostgresFixture.cs`** (the **Testcontainers**
  fixture — the `postgres:18` shape, the **M3/M4** precedent; the
  **`IDocumentStore`** the tests inject; the **`IProjectService`** the tests
  call — the **U02–U06** shape, reused).
- **`tests/Kumunita.Core.Tests/EventServiceTests.cs`** (the **M4 Core test
  shape** to mirror — the `[Fact]` attribute; the `async Task` method
  signature; the `PostgresFixture` injection; the `IProjectService` call
  shape; the **xunit.v3** assertion shape (`Assert.*` methods) — the
  **M4 shape**, the **U02–U06** shape, reused).
- **`tests/Kumunita.Web.Tests/EventControllerTests.cs`** (the **M4 Web test
  shape** to mirror — the `[Fact]` attribute; the `async Task` method
  signature; the **`IProjectService`** + **`IUserInfoService`** +
  **`ILocalizationService`** mocks the tests inject; the
  **`ProjectsController`** the tests call; the **`ForbidResult`** /
  **`NotFound`** assertion shape; the **xunit.v3** assertion shape
  (`Assert.*` methods) — the **M4 shape**, the **U07–U08** shape, reused).
- **`src/Kumunita.Core/Projects/ProjectService.cs`** (the **service lanes**
  U04–U06 implemented — the **`IProjectService`** method signatures the
  tests call; the **`CreateTodoRequest`** / **`UpdateTodoRequest`** /
  **`CreateBoardRequest`** / **`UpdateLaneRequest`** DTO shapes the tests
  construct; the **`TodoDetailResult`** / **`BoardDetailResult`** /
  **`LaneDetail`** return shapes the tests assert against — the **§2.3**
  pin, the **U04–U06** shape, reused).
- **`src/Kumunita.Web/Controllers/ProjectsController.cs`** (the
  **controller routes** U07–U08 wired — the **`GET /projects/todos`** /
  **`GET /projects/todos/{id}`** / **`GET /projects/boards`** /
  **`GET /projects/boards/{id}`** routes the Web tests call; the
  **`POST /projects/todos/{id}/assign`** / **`/subtasks`** / **`/delete`** /
  **`/copy-to`** / **`/move-to`** routes the Web tests call; the
  **`POST /projects/boards/{boardId}/lanes/{laneId}/cards/{placementId}/move-{direction}`**
  reorder routes the Web tests call; the **`ForbidResult`** / **`NotFound`**
  split the Web tests assert against — the **§2.3** pin, the **C3** pin,
  the **U07–U08** shape, reused).
- **`src/Kumunita.Core/Localization/KnownTranslationKeys.cs`** (the
  **`AllKeys`** collection (line 4447) — the **drift guard** test (U12)
  verifies the `AllKeys` set matches the **§2.6 floor** + the **view/TS
  superset** — the **§2.7 / §2.8** pin).

**Deliverables (2 files — 2 new test files):**

### 1. `tests/Kumunita.Core.Tests/ProjectServiceTests.cs` — the **16 Core
pinned tests**

The **16 Core pinned tests** (the **§2.7** pin — the **exact** test names,
verbatim) are created in the **new** `ProjectServiceTests.cs` file (the
**xunit.v3** shape — the `Microsoft.Testing.Platform` pin, the **M3/M4**
precedent). The tests exercise the **F1–F10** FACES (the behavioral
acceptance scenarios — the **§5** pin) over the **service lanes** U04–U06
implemented. The **`PostgresFixture`** (the **Testcontainers** fixture —
the `postgres:18` shape, the **M3/M4** precedent) is the **sole** test
fixture (the **M4 shape** — the `EventServiceTests.cs` shape, reused).
The **`IProjectService`** is the **sole** service under test (the
**§2.3** pin — the `IProjectService` method signatures the tests call,
the **U04–U06** shape, reused).

**The 16 Core pinned tests** (the **§2.7** pin — the **exact** test names,
verbatim):

| # | Test name (verbatim) | FACE(s) exercised |
|---|---------------------|-------------------|
| 1 | `F1_TodoVisibleToAudienceMember` | **F1** (a to-do is visible to its audience member) |
| 2 | `F1_TodoHiddenFromNonMember` | **F1** (a to-do is hidden from a non-member) |
| 3 | `F2_TodoPlacedOnMultipleBoards_AppearsOnEach` | **F2** (a to-do can be placed on several boards / appears on each) |
| 4 | `F3_BoardGate_TodoOnBoard_BoardDenies_HidesCard` | **F3** (a board is gated by its own `Audience`; a to-do on the board is visible iff **both** are visible — the **two-level decision**) |
| 5 | `F3_BoardGate_TodoOnBoard_BoardAllows_ShowsCard` | **F3** (a board is gated by its own `Audience`; a to-do on the board is visible iff **both** are visible — the **two-level decision**) |
| 6 | `F4_MoveToStatusLane_SetsTodoStatus_Audited` | **F4** (moving a to-do into a lane with a `Status` sets the to-do's status — auto-update / audited) |
| 7 | `F5_MoveToNullStatusLane_TodoStatusUnchanged` | **F5** (moving a to-do into a lane with null `Status` leaves its status unchanged) |
| 8 | `F6_LaneAtMaxRefusesMoveIn` | **F6** (a lane at its `MaxItems` limit **refuses** a further move/copy into it) |
| 9 | `F6_LaneBelowMaxAllowsMoveIn` | **F6** (a lane **below** its `MaxItems` limit **accepts** a move/copy into it) |
| 10 | `F7_AssigneeHasStanding_UpdatesTodo` | **F7** (the assignee has standing over an assigned to-do — creator ∪ assignee ∪ GlobalAdmin) |
| 11 | `F8_NonStandaloneActorRefused` | **F8** (a non-creator / non-assignee / non-GlobalAdmin has **no** standing to mutate) |
| 12 | `F9_SubtaskIsFullTodo_OwnStatusAssignee` | **F9** (a subtask is a full to-do — own status / assignee / placement) |
| 13 | `F9_DeleteCascadesToSubtree` | **F9** (deleting a to-do cascades to its subtree) |
| 14 | `F9_ReparentToDescendant_Refused` | **F9** (reparenting a to-do to its own descendant is **refused** — the **cycle guard**) |
| 15 | `F10_CopyToBoard_Duplicates_OriginalUntouched` | **F10** (copy-to-board **duplicates** the to-do — original untouched) |
| 16 | `F10_MoveToBoard_RelocatesPlacement` | **F10** (move-to-board **relocates** the placement) |

**The test shape** (the **xunit.v3** shape — the `EventServiceTests.cs`
shape, reused):

```csharp
// tests/Kumunita.Core.Tests/ProjectServiceTests.cs (the **xunit.v3** shape —
// the **M4 shape**, the **U02–U06** shape, reused)

using Xunit;
using Kumunita.Core.Bootstrap;
using Kumunita.Core.Localization;
using Kumunita.Core.Projects;
using Kumunita.Core.Tests;

namespace Kumunita.Core.Tests;

public class ProjectServiceTests(PostgresFixture fixture)
{
    // F1 — a to-do is visible to its audience member (the **§5** pin —
    // the **§2.7** pin: the **exact** test name, verbatim).
    [Fact]
    public async Task F1_TodoVisibleToAudienceMember()
    {
        // Arrange: the fixture's `IDocumentStore` + `IProjectService`
        // (the **U02–U06** shape, reused); the actor (the **§2.3** pin —
        // the `actorId` + `actorRoles` set the service enforces
        // server-side, the **§2.5** pin); the to-do (the **§2.2** pin —
        // the `TodoItem` shape, the **U02** shape, reused); the
        // audience (the **§2.3** pin — the `Audience` shape, the
        // **U03** shape, reused).

        // Act: the `ListTodosAsync` call (the **§2.3** pin — the
        // `ListTodosAsync` method signature, the **U04** shape, reused).

        // Assert: the to-do is visible to the audience member (the
        // **F1** pin — the **service's** pin, the **§2.3** pin).
    }

    // ... (the remaining 15 tests follow the same shape — the **xunit.v3**
    // assertion shape (`Assert.*` methods), the **M4 shape**, the
    // **U02–U06** shape, reused).
}
```

**The tests do NOT:**
- **Run the acceptance gate** (the **§2.9** pin — the acceptance gate is
  **U13's** — the **closed-loop / handoff / part-vs-whole** gate, the
  **§2.9** pin).
- **Re-derive standing or visibility** (the **C3** pin: the service's
  `CanSeeAsync(Read)` / `CanAsync(Write)` split is the **service's** —
  the tests assert the service's behavior, not re-derive it).
- **Use `dotnet test` / VS Test Explorer** (the **AGENTS.md** pin: the
  test runner quirk — the reliable path is `dotnet build Kumunita.slnx -c
  Debug` + `dotnet exec
  tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll` —
  the **AGENTS.md** shape, the **M3/M4** precedent, reused).

### 2. `tests/Kumunita.Web.Tests/ProjectsControllerTests.cs` — the **8 Web
pinned tests**

The **8 Web pinned tests** (the **§2.8** pin — the **exact** test names,
verbatim) are created in the **new** `ProjectsControllerTests.cs` file (the
**xunit.v3** shape — the `Microsoft.Testing.Platform` pin, the **M3/M4**
precedent). The tests exercise the **F1–F10** FACES (the behavioral
acceptance scenarios — the **§5** pin) over the **controller routes**
U07–U08 wired. The **`IProjectService`** + **`IUserInfoService`** +
**`ILocalizationService`** mocks are the **sole** test fixtures (the
**M4 shape** — the `EventControllerTests.cs` shape, reused). The
**`ProjectsController`** is the **sole** controller under test (the
**§2.3** pin — the `ProjectsController` method signatures the tests call,
the **U07–U08** shape, reused).

**The 8 Web pinned tests** (the **§2.8** pin — the **exact** test names,
verbatim):

| # | Test name (verbatim) | FACE(s) exercised |
|---|---------------------|-------------------|
| 1 | `Todos_List_AudienceFiltered` | **F1** (a to-do is visible to its audience member / hidden from a non-member — the **`GET /projects/todos`** route) |
| 2 | `Todo_Detail_SubtasksRendered` | **F9** (a subtask is a full to-do — own status / assignee / placement — the **`GET /projects/todos/{id}`** route) |
| 3 | `Board_Detail_LanesAndCards` | **F2 / F3** (a to-do can be placed on several boards / appears on each; a board is gated by its own `Audience`; a to-do on the board is visible iff **both** are visible — the **`GET /projects/boards/{id}`** route) |
| 4 | `Board_MoveLeftRight_TriggerStatusUpdate` | **F4 / F5** (moving a to-do into a lane with a `Status` sets the to-do's status; moving a to-do into a lane with null `Status` leaves its status unchanged — the **`POST /projects/boards/{boardId}/lanes/{laneId}/cards/{placementId}/move-left`** + **`/move-right`** routes) |
| 5 | `Board_LaneAtMax_RefusesMoveIn` | **F6** (a lane at its `MaxItems` limit **refuses** a further move/copy into it — the **`POST /projects/boards/{boardId}/lanes/{laneId}/cards/{placementId}/move-up`** + **`/move-down`** + **`/move-left`** + **`/move-right`** routes) |
| 6 | `Board_CopyToBoard_Duplicates` | **F10** (copy-to-board **duplicates** the to-do — original untouched — the **`POST /projects/todos/{id}/copy-to`** route) |
| 7 | `Board_MoveToBoard_Relocates` | **F10** (move-to-board **relocates** the placement — the **`POST /projects/todos/{id}/move-to`** route) |
| 8 | `Todo_AssignToUser_StandingGranted` | **F7** (the assignee has standing over an assigned to-do — the **`POST /projects/todos/{id}/assign`** route) |

**The test shape** (the **xunit.v3** shape — the `EventControllerTests.cs`
shape, reused):

```csharp
// tests/Kumunita.Web.Tests/ProjectsControllerTests.cs (the **xunit.v3**
// shape — the **M4 shape**, the **U07–U08** shape, reused)

using Xunit;
using Kumunita.Web.Controllers;
using Kumunita.Web.Models;

namespace Kumunita.Web.Tests;

public class ProjectsControllerTests
{
    // F1 — a to-do is visible to its audience member / hidden from a
    // non-member (the **§5** pin — the **§2.8** pin: the **exact** test
    // name, verbatim).
    [Fact]
    public async Task Todos_List_AudienceFiltered()
    {
        // Arrange: the `IProjectService` mock (the **U04–U06** shape,
        // reused); the `IUserInfoService` mock (the **M4 shape**, the
        // **U07** shape, reused); the `ILocalizationService` mock (the
        // **M4 shape**, the **U07** shape, reused); the
        // `ProjectsController` (the **U07–U08** shape, reused); the
        // `ClaimsPrincipal` (the **M4 shape**, the **U07** shape, reused).

        // Act: the `GET /projects/todos` route (the **U07** shape,
        // reused).

        // Assert: the to-do is visible to the audience member / hidden
        // from a non-member (the **F1** pin — the **service's** pin, the
        // **§2.3** pin; the **C3** pin: the controller's `ForbidResult`
        // / `NotFound` split is the **service's**).
    }

    // ... (the remaining 7 tests follow the same shape — the **xunit.v3**
    // assertion shape (`Assert.*` methods), the **M4 shape**, the
    // **U07–U08** shape, reused).
}
```

**The tests do NOT:**
- **Run the acceptance gate** (the **§2.9** pin — the acceptance gate is
  **U13's** — the **closed-loop / handoff / part-vs-whole** gate, the
  **§2.9** pin).
- **Re-derive standing or visibility** (the **C3** pin: the service's
  `CanSeeAsync(Read)` / `CanAsync(Write)` split is the **service's** —
  the tests assert the service's behavior, not re-derive it).
- **Use `dotnet test` / VS Test Explorer** (the **AGENTS.md** pin: the
  test runner quirk — the reliable path is `dotnet build Kumunita.slnx -c
  Debug` + `dotnet exec
  tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll` —
  the **AGENTS.md** shape, the **M3/M4** precedent, reused).

**The drift guard** (the **§2.7 / §2.8** pin — the test that verifies the
`AllKeys` set matches the **§2.6 floor** + the **view/TS superset**) is
**U12's** — the **`KnownTranslationKeys.AllKeys`** collection (line 4447)
is the **exact set** of keys in `EnValues` (the **ADR 0015** shape — the
`AllKeys` is the **exact set** of keys in `EnValues`, the **M4 shape**,
reused). The **drift guard** test (the **§2.7 / §2.8** pin) verifies the
`AllKeys` set matches the **§2.6 floor** (18 keys — the `projects.todo.*`
+ `projects.board.*` set) + the **view/TS superset** (the `lede / new /
edit / save / assign / parent / clear_parent / subtasks / boards /
nav.projects / component / language / lanes / lane.edit / lane.order /
board.delete / board.create` keys — the **U09 / U10 / U11** shapes,
reused). The **drift guard** test is **U12's** (the **§2.7 / §2.8** pin).

**Exit:** `dotnet build Kumunita.slnx -c Debug` is green (the 16 Core
pinned tests + the 8 Web pinned tests compile; the **xunit.v3** shape
compiles; the **`PostgresFixture`** compiles; the **`IProjectService`**
mocks compile; the **`ProjectsController`** mocks compile). **No gate**
(the **§2.9** pin — the acceptance gate is **U13's** — the
**closed-loop / handoff / part-vs-whole** gate, the **§2.9** pin).

**Handoff note for U13:** the **acceptance gate** + **close** (the
**§2.9** pin) are the **next** unit — the **run + record** the
**three-test gate** (the **closed-loop / handoff / part-vs-whole** gate,
the **§2.9** pin); the **roadmap trio** (M5 → `StatusDone` / M6 →
`StatusNext`); the **`MilestonesTests.cs`** sync (the **M4 shape** — the
`MilestonesTests.cs` shape, the **M3/M4** precedent, reused); the
**`ARCHITECTURE.md`** §3 tree + §5 `Projects` block sync (the **M4
shape** — the `ARCHITECTURE.md` shape, the **M3/M4** precedent, reused);
the **README** Roadmap (the **M4 shape** — the `README.md` shape, the
**M3/M4** precedent, reused); the **handoff summary** (the **M4 shape** —
the handoff summary shape, the **M3/M4** precedent, reused); and the
**unit files** moved from `in-progress/projects/` → `done/projects/` (the
**M4 shape** — the unit files shape, the **M3/M4** precedent, reused).

# U03 — the two `Projects` adapters (`TodoItemToAuditableResource` / `KanbanBoardToAuditableResource`)

**Goal:** create `src/Kumunita.Core/Projects/TodoItemToAuditableResource.cs`
+ `src/Kumunita.Core/Projects/KanbanBoardToAuditableResource.cs`, each a
`sealed class` implementing the **frozen** `IAuditableResource` 6-member
surface, mirroring `EventToAuditableResource` verbatim (the **only**
difference is the `TargetKind` string — `"todo"` / `"board"`). Build green.
The service (U04) and the lanes are not touched in this unit — the adapters
are the **only** new authorization surface (C-M5·11), and they are pure
projections (no DI, no dependency on the service).

**Entry reads:**
- The design doc §2.2 (the `TodoItem` / `KanbanBoard` field names + the
  `Audience?` / `ComponentId?` / `AuthorId` / `Title` / `Body?` shapes the
  adapters project — the **exact** mapping is the pin).
- `src/Kumunita.Core/Events/EventToAuditableResource.cs` (the **M4 adapter
  shape to mirror verbatim** — the `sealed class` + the 6-member projection
  + the doc-comment convention + the `Name` truncation logic (60-char
  title / body fallback) + the `OwnerId` / `Audience` / `ComponentId` /
  `TargetKind` mapping + the "the adapter does not own the doc" note).
- `src/Kumunita.Core/Posts/PostToAuditableResource.cs` (the **post adapter
  shape** — the `TargetKind "post"` precedent; the same 6-member surface;
  the `Name` truncation shape carried onto the M5 adapters).
- `src/Kumunita.Core/Authorization/IAuthorizationService.cs` (the **frozen**
  4-method surface + the `IAuditableResource` 6-member shape — the adapters
  implement the **frozen** `IAuditableResource`, no signature change,
  **no** new `AccessAction`, **no** new `AccessVia`, **no** new branch in
  `Decide()` — C-M5·11).
- `src/Kumunita.Core/Authorization/AccessAction.cs` (confirm `Read` + the
  reserved `Moderate` — **no** new action id in M5; the adapters use the
  **existing** `Read` action, no new action is added).
- `src/Kumunita.Core/Events/Event.cs` (the `Event` field names — the
  `TodoItem` / `KanbanBoard` field names are the **exact** parallel; the
  `AuthorId` / `Audience` / `ComponentId` / `Title` / `Body` shapes are the
  same).
- `src/Kumunita.Core/Projects/TodoItem.cs` + `KanbanBoard.cs` (U02's
  deliverables — the field names the adapters project; confirm the
  `Title` / `Body?` / `AuthorId` / `Audience?` / `ComponentId?` fields are
  present and the names match).

**Deliverables (2 files, no edits, no tests):**
- `src/Kumunita.Core/Projects/TodoItemToAuditableResource.cs` (new) — a
  `sealed class` implementing `IAuditableResource`, mirroring
  `EventToAuditableResource` verbatim:
  - `public sealed class TodoItemToAuditableResource : IAuditableResource`
  - `public TodoItemToAuditableResource(TodoItem todo) => Todo = todo;`
  - `public TodoItem Todo { get; }`
  - `public string Id => Todo.Id;`
  - `public string Name => Todo.Title ?? (Todo.Body != null && Todo.Body.Length < 60 ? Todo.Body : (Todo.Body?.Length ?? 0) >= 60 ? (Todo.Body?.[..57] ?? "") + "..." : Todo.Body ?? "");`
    (the 60-char title / body truncation shape, the `EventToAuditableResource`
    pin; a `TodoItem` with no `Title` and no `Body` is a degenerate case —
    the `Name` is the empty string, the audit row still stores the
    `TargetKind "todo"` + the `Id`).
  - `public string? OwnerId => Todo.AuthorId;`
  - `public Audience? Audience => Todo.Audience;`
  - `public string? ComponentId => Todo.ComponentId;`
  - `public string TargetKind => "todo";` (the **exact** string — the
    `AccessAudit.TargetKind` discriminator for this line of decisions, C3).
  - The doc-comment mirrors `EventToAuditableResource`'s (the **M5** pin,
    the **frozen** `IAuthorizationService` surface, the **exact** `Audience`
    projection, the **filter-never-gate** `ComponentId` pin, the
    **not-owning** the doc pin).
- `src/Kumunita.Core/Projects/KanbanBoardToAuditableResource.cs` (new) — a
  `sealed class` implementing `IAuditableResource`, mirroring
  `EventToAuditableResource` verbatim:
  - `public sealed class KanbanBoardToAuditableResource : IAuditableResource`
  - `public KanbanBoardToAuditableResource(KanbanBoard board) => Board = board;`
  - `public KanbanBoard Board { get; }`
  - `public string Id => Board.Id;`
  - `public string Name => Board.Title ?? (Board.Description != null && Board.Description.Length < 60 ? Board.Description : (Board.Description?.Length ?? 0) >= 60 ? (Board.Description?.[..57] ?? "") + "..." : Board.Description ?? "");`
    (the 60-char title / description truncation shape, the
    `EventToAuditableResource` pin; a `KanbanBoard` with no `Title` and no
    `Description` is a degenerate case — the `Name` is the empty string, the
    audit row still stores the `TargetKind "board"` + the `Id`).
  - `public string? OwnerId => Board.AuthorId;`
  - `public Audience? Audience => Board.Audience;`
  - `public string? ComponentId => Board.ComponentId;`
  - `public string TargetKind => "board";` (the **exact** string — the
    `AccessAudit.TargetKind` discriminator for this line of decisions, C3).
  - The doc-comment mirrors `EventToAuditableResource`'s (the **M5** pin,
    the **frozen** `IAuthorizationService` surface, the **exact** `Audience`
    projection, the **filter-never-gate** `ComponentId` pin, the
    **not-owning** the doc pin).

**Exit:** `dotnet build Kumunita.slnx -c Debug` is green (the two adapters
compile, the `IAuditableResource` 6-member surface is satisfied, the
`TargetKind "todo"` / `"board"` strings are the **exact** pin). **No new
tests** (the adapters are exercised in U12's pinned tests — the F1 / F2 / F3
FACES). Handoff note: 4–6 lines starting `## U03 — adapters`, listing (a)
the two adapter class names (by file), (b) the `TargetKind` strings (by
file), (c) the `Name` truncation shape (the 60-char pin), (d) the build-green
confirmation. **Move the unit file**
`in-progress/projects/U03.md` → `done/projects/U03.md`.

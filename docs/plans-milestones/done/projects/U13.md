# U13 — Close: acceptance gate + ARCHITECTURE.md + README + Milestones.cs (M5 → Done)

- **Lane:** M5 Projects (to-dos + Kanban boards)
- **Unit:** U13 (close)
- **Kind:** close — no new code, no new tests. One gate run + three doc/roadmap flips + a handoff summary.

## Goal

Ship the M5 lane. The code is already in (U00–U12); this unit does the
book-keeping that makes it real:

1. **Run the §2.9 three-test acceptance gate** and record the outcome
   (this is U13's only "test work" — a green run, not new tests).
2. **Flip the roadmap trio** — in `Milestones.cs`:
   - `M5` row: `StatusNext` → `StatusDone`
   - `M6` row: `StatusPlanned` → `StatusNext`
   - and the matching lines in `README.md`'s Roadmap section.
3. **`MilestonesTests.cs` sync** — the single-in-progress pin moves from M5
   to M6 (the Ids order is unchanged; the M5/M6 status pair flips).
4. **`docs/ARCHITECTURE.md` sync** — the §3 tree's `Projects/` line and the
   §5 data-model block go from "not yet created / planned" to the shipped
   shapes (the `TodoItem` / `KanbanBoard` / `KanbanLane` /
   `BoardItemPlacement` docs + the `ProjectService` read/write/placement
   surface).

## Entry reads (≤5 files)

1. `docs/plans-milestones/in-progress/projects/U01.md` §2.9 — the three-test
   gate (closed loop / handoff / part-vs-whole) and §2.10 drift-guard.
2. `src/Kumunita.Web/Milestones.cs` — the M5/M6 rows and the `LabelFor`
   switch (M5 is `StatusNext`, M6 is `StatusPlanned` today).
3. `tests/Kumunita.Web.Tests/MilestonesTests.cs` — the 4 pins, the Ids order
   (…`"M5", "M6"`), and `M5_Is_The_Single_InProgress_Milestone`.
4. `docs/ARCHITECTURE.md` — the §3 tree (`Projects/` "not yet created") and
   the §5 block (the `Project { … }` placeholder) that this unit updates.
5. `README.md` — the Roadmap section (M5 "In progress", M6 "Planned").

## Deliverables (≤4 files)

- `src/Kumunita.Web/Milestones.cs` — M5→`StatusDone`, M6→`StatusNext`.
- `tests/Kumunita.Web.Tests/MilestonesTests.cs` — single-in-progress pin
  moves to M6 (rename `M5_Is_The_Single_InProgress_Milestone` →
  `M6_Is_The_Single_InProgress_Milestone`, keep the Ids array and the
  done-set pin — M5 joins the `Shipped_..._Are_Marked_Done` set).
- `docs/ARCHITECTURE.md` — §3 tree + §5 block → shipped M5 shapes.
- `README.md` — Roadmap: M5 "Done", M6 "In progress".

## File move (after the Exit checks pass)

Move all 14 unit files (U00–U13), this register, and the rolling
`projects-handoff-notes.md` (created during execution) out of
`in-progress/projects/` into `done/projects/` — the lane is closed. This
happens *after* the build + test gate is green, so a red run leaves the
files in place for a `## U<m> — Drift pause`.

## Exit

- `dotnet build Kumunita.slnx -c Debug` green.
- `dotnet exec tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll`
  green — the `MilestonesTests` family passes with **M6** now the single
  `StatusNext` row and M5 in the done set.
- `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll`
  green — the 16 `ProjectServiceTests` pins pass (closed loop + handoff +
  part-vs-whole all green — the lane is additive, nothing regressed).
- A `## U13 — close` handoff note appended to the register, ~5 lines.

## Notes / deviations

- No new tests in this unit — the gate is a *green run* of the 16 Core +
  8 Web pins (U12) that already encode closed loop, handoff, and
  part-vs-whole. If any pin fails here, that is a `## U<m> — Drift pause`,
  not a U13 failure.
- The roadmap trio is a **pair** flip, not a single-row change: M5 leaves
  the in-progress slot and M6 enters it. `MilestonesTests`'s single-in-
  progress pin is what enforces the pair, so it must be edited in the same
  commit or the test suite is red between the two.
- `LabelFor` is unchanged — it already maps all three statuses.

## Handoff note (append to the register)

`## U13 — close` (5 lines): M5 flipped to Done, M6 to In progress
(Milestones.cs + README + MilestonesTests pin all in one commit);
ARCHITECTURE.md §3 tree + §5 block now carry the shipped `TodoItem` /
`KanbanBoard` / `KanbanLane` / `BoardItemPlacement` docs and the
`ProjectService` surface; gate green (16 Core + 8 Web pins pass, nothing
regressed); ADR 0067 Accepted.

# U04 — `IProjectService` full surface + read lanes + write/placement stubs + DI registration

**Goal:** create `src/Kumunita.Core/Projects/IProjectService.cs` (the **full**
interface — read + write + placement, verbatim from the design doc §2.3) +
`src/Kumunita.Core/Projects/ProjectService.cs` (the read-lane implementation
+ the write / placement lanes as `NotImplementedException` stubs — the
**M4 U01 interface-first pin** carried onto M5) + the DTOs (the
`CreateTodoRequest` / `UpdateTodoRequest` / `CreateBoardRequest` /
`CreateLaneRequest` / `UpdateLaneRequest` / `TodoDetailResult` /
`BoardDetailResult` / `LaneDetail` record types) + the `IProjectService` DI
registration in `DependencyInjection.cs` (next to the `IEventService`
registration, L186). Build green. The write lanes (U05) and the placement /
reorder lanes (U06) land after this unit — this unit ships the **frozen**
seam + the read lanes + the stubs, so the schema + the seam exist before the
lanes are written.

**Entry reads:**
- The design doc §2.3 (the **full `IProjectService` surface** — the read +
  write + placement signatures, verbatim; the DTO shapes; the standing matrix
  notes per method) and §2.5 (the standing matrix — the **creator ∪ assignee
  ∪ GlobalAdmin** pin for to-do mutations; the **creator ∪ GlobalAdmin** pin
  for board / lane mutations; the `actorRoles` parameter shape).
- `src/Kumunita.Core/Events/IEventService.cs` (the **M4 service-seam shape to
  mirror** — the read lanes (U03 in M4, U04 in M5) + the write lanes (U04 in
  M4, U05 in M5) + the `NotImplementedException` stub convention + the
  doc-comment convention + the `actorId` / `actorRoles` / `CancellationToken`
  parameter shapes).
- `src/Kumunita.Core/Events/EventService.cs` (the **M4 read-lane
  implementation** to mirror — the `ListUpcomingAsync` feed (the
  `CanSeeAsync(Read)` filter + the aggregate `AccessAudit` row + the paged
  result), the `GetAsync` detail (the `CanAsync(Read)` decision + the
  404-vs-403 split), the `GetRsvpsAsync` owner-only read (the standing
  re-check pattern). The **M5 read lanes** mirror this shape:
  `ListTodosAsync` (the feed), `GetTodoAsync` (the detail + subtasks),
  `ListBoardsAsync` (the feed), `GetBoardAsync` (the detail + lanes +
  cards). The **two-level decision** (C-M5·3) is the new M5 pin: the board's
  `CanAsync(Read)` is the entry gate; each card's `TodoItem` is itself
  `CanAsync(Read)`-gated).
- `src/Kumunita.Core/Authorization/IAuthorizationService.cs` (the **frozen**
  4-method surface — the `CanAsync(actorId, Read, adapter)` detail decision
  + the `CanSeeAsync(actorId, Read, adapter)` feed filter; the **frozen**
  surface, no signature change, **no** new `AccessAction`, **no** new
  `AccessVia`, **no** new branch in `Decide()` — C-M5·11).
- `src/Kumunita.Core/Events/EventToAuditableResource.cs` (the **M4 adapter
  shape** — the `TargetKind "event"` precedent; the same 6-member
  `IAuditableResource` surface; the M5 adapters are `TodoItemToAuditableResource`
  / `KanbanBoardToAuditableResource` — U03's deliverables).
- `src/Kumunita.Core/Projects/TodoItemToAuditableResource.cs` +
  `KanbanBoardToAuditableResource.cs` (U03's deliverables — the adapters the
  read lanes use; confirm the `TargetKind "todo"` / `"board"` strings are
  the **exact** pin).
- `src/Kumunita.Core/DependencyInjection.cs` L179–190 (the `IEventService`
  registration shape — the `IProjectService` registration pattern U04
  mirrors; the `AddTransient` + the `IDocumentStore` /
  `IAuthorizationService` / `IUserInfoService` constructor shape).
- `src/Kumunita.Core/Projects/TodoItem.cs` + `KanbanBoard.cs` +
  `KanbanLane.cs` + `BoardItemPlacement.cs` (U02's deliverables — the
  document shapes the read lanes query; confirm the field names match the
  design doc §2.2).
- `tests/Kumunita.Core.Tests/PostgresFixture.cs` (the harness shape — no test
  change in this unit, but the fixture is where the read lanes are first
  exercised in U12).

**Deliverables (4 files + 1 edit, no tests):**
- `src/Kumunita.Core/Projects/IProjectService.cs` (new) — the **full**
  interface, verbatim from the design doc §2.3:
  - `namespace Kumunita.Core.Projects;`
  - `public interface IProjectService`
  - **Read lanes (U04):**
    - `Task<IReadOnlyList<TodoItem>> ListTodosAsync(string? componentId, string? assigneeId, string actorId, int page, CancellationToken ct = default);`
      — the **standalone to-do list** (the feed): the candidates are
      `!IsDeleted`, filtered by optional `ComponentId` (a filter, never a
      gate — C-M3·2) and optional `AssigneeId` (a filter, never a gate —
      C-M5·6); the survivors are `CanSeeAsync(Read)`-filtered (C6 / C3) over
      the `TodoItemToAuditableResource`; ordered by `Created` descending (the
      newest first — the post feed shape); paged. The **aggregate**
      `AccessAudit` row (`TargetKind "todo"`, `visibleCount` / `hiddenCount`)
      is the C-M3·3 shape.
    - `Task<TodoDetailResult> GetTodoAsync(string todoItemId, string actorId, CancellationToken ct = default);`
      — one to-do + its **subtasks** (the `TodoItem` rows with
      `ParentId == todoItemId`, each with its own `Audience` decision — a
      subtask is a full to-do, C-M5·7). One `CanAsync(Read)` over the to-do;
      if `Allowed`, the subtasks are loaded **under the parent's decision**
      (each subtask's own `CanAsync(Read)` — the ADR 0013 reply-inherits
      shape is **not** the M5 pin; a subtask is its own document with its own
      `Audience`). `KeyNotFoundException` (404) on absent,
      `UnauthorizedAccessException` (403) on denied (the C3 404-vs-403
      split).
    - `Task<IReadOnlyList<KanbanBoard>> ListBoardsAsync(string? componentId, string actorId, int page, CancellationToken ct = default);`
      — the **board list** (the feed): the candidates are `!IsDeleted`,
      filtered by optional `ComponentId` (a filter, never a gate — C-M3·2);
      the survivors are `CanSeeAsync(Read)`-filtered (C6 / C3) over the
      `KanbanBoardToAuditableResource`; ordered by `Created` descending;
      paged. The **aggregate** `AccessAudit` row (`TargetKind "board"`,
      `visibleCount` / `hiddenCount`) is the C-M3·3 shape.
    - `Task<BoardDetailResult> GetBoardAsync(string boardId, string actorId, CancellationToken ct = default);`
      — one board + its **lanes** (the `KanbanLane` rows with
      `BoardId == boardId`, ordered by `Order` ascending) + each lane's
      **cards** (the `BoardItemPlacement` rows with `BoardId == boardId &&
      LaneId == laneId`, ordered by `Order` ascending, each resolved to its
      `TodoItem`). **The two-level decision** (C-M5·3): the board's
      `CanAsync(Read)` is the entry gate (a `Deny` ⇒ 404/403 split — the C3
      shape); each card's `TodoItem` is **itself** `CanAsync(Read)`-gated
      (a to-do on the board is visible iff **both** the board and the to-do
      are visible — C-M5·3; a denied card is **not returned** in the result,
      not just hidden in the view). The **aggregate** `AccessAudit` row
      (`TargetKind "board"`, `visibleCount` = the visible card count,
      `hiddenCount` = the denied card count) is the C-M3·3 shape.
  - **Write lanes (U05):**
    - `Task<TodoItem> CreateTodoAsync(string actorId, IReadOnlySet<string> actorRoles, CreateTodoRequest request, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U05 lands the logic).
    - `Task<TodoItem> UpdateTodoAsync(string todoItemId, string actorId, IReadOnlySet<string> actorRoles, UpdateTodoRequest request, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U05 lands the logic).
    - `Task<TodoItem> AssignTodoAsync(string todoItemId, string actorId, IReadOnlySet<string> actorRoles, string? assigneeId, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U05 lands the logic).
    - `Task<TodoItem> AddSubtaskAsync(string parentTodoItemId, string actorId, IReadOnlySet<string> actorRoles, CreateTodoRequest request, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U05 lands the logic).
    - `Task DeleteTodoAsync(string todoItemId, string actorId, IReadOnlySet<string> actorRoles, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U05 lands the logic).
    - `Task<KanbanBoard> CreateBoardAsync(string actorId, IReadOnlySet<string> actorRoles, CreateBoardRequest request, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U05 lands the logic).
    - `Task<KanbanLane> UpdateLaneAsync(string laneId, string actorId, IReadOnlySet<string> actorRoles, UpdateLaneRequest request, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U05 lands the logic).
    - `Task DeleteBoardAsync(string boardId, string actorId, IReadOnlySet<string> actorRoles, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U05 lands the logic).
  - **Placement lanes (U06):**
    - `Task<BoardItemPlacement> MoveTodoWithinLaneAsync(string placementId, string direction, string actorId, IReadOnlySet<string> actorRoles, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U06 lands the logic).
    - `Task<BoardItemPlacement> MoveTodoToAdjacentLaneAsync(string placementId, string direction, string actorId, IReadOnlySet<string> actorRoles, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U06 lands the logic).
    - `Task<BoardItemPlacement> MoveTodoToBoardAsync(string todoItemId, string targetBoardId, string actorId, IReadOnlySet<string> actorRoles, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U06 lands the logic).
    - `Task<TodoItem> CopyTodoToBoardAsync(string todoItemId, string targetBoardId, string actorId, IReadOnlySet<string> actorRoles, CancellationToken ct = default);`
      — **stub** (`NotImplementedException` — U06 lands the logic).
  - The doc-comment mirrors `IEventService`'s (the **M5** pin, the
    **frozen** `IAuthorizationService` surface, the **read lanes** U04
    + the **write lanes** U05 + the **placement lanes** U06, the
    `NotImplementedException` stub convention, the **standing matrix**
    notes per method).
- `src/Kumunita.Core/Projects/ProjectService.cs` (new) — the **read-lane
  implementation** + the write / placement lanes as `NotImplementedException`
  stubs, mirroring `EventService`'s read-lane shape:
  - `public sealed class ProjectService : IProjectService`
  - `public ProjectService(IDocumentStore store, IAuthorizationService auth, IUserInfoService userInfo) => (Store, Auth, UserInfo) = (store, auth, userInfo);`
  - `private IDocumentStore Store { get; }`
  - `private IAuthorizationService Auth { get; }`
  - `private IUserInfoService UserInfo { get; }`
  - `public async Task<IReadOnlyList<TodoItem>> ListTodosAsync(...)` — the
    feed: query the `TodoItem` rows with `!IsDeleted`, filter by optional
    `ComponentId` + optional `AssigneeId`, `CanSeeAsync(Read)`-filter over
    the `TodoItemToAuditableResource`, order by `Created` descending, page.
    The aggregate `AccessAudit` row (`TargetKind "todo"`) is stored in the
    caller's session (C3).
  - `public async Task<TodoDetailResult> GetTodoAsync(...)` — the detail:
    load the `TodoItem` row; `CanAsync(Read)` over the
    `TodoItemToAuditableResource`; if `Allowed`, load the subtasks (the
    `TodoItem` rows with `ParentId == todoItemId`, each `CanAsync(Read)`-gated);
    return the `TodoDetailResult`. `KeyNotFoundException` (404) on absent,
    `UnauthorizedAccessException` (403) on denied (the C3 404-vs-403 split).
  - `public async Task<IReadOnlyList<KanbanBoard>> ListBoardsAsync(...)` —
    the feed: query the `KanbanBoard` rows with `!IsDeleted`, filter by
    optional `ComponentId`, `CanSeeAsync(Read)`-filter over the
    `KanbanBoardToAuditableResource`, order by `Created` descending, page.
    The aggregate `AccessAudit` row (`TargetKind "board"`) is stored in the
    caller's session (C3).
  - `public async Task<BoardDetailResult> GetBoardAsync(...)` — the detail:
    load the `KanbanBoard` row; `CanAsync(Read)` over the
    `KanbanBoardToAuditableResource`; if `Allowed`, load the lanes (the
    `KanbanLane` rows with `BoardId == boardId`, ordered by `Order`
    ascending) + each lane's cards (the `BoardItemPlacement` rows with
    `BoardId == boardId && LaneId == laneId`, ordered by `Order` ascending,
    each resolved to its `TodoItem`); each card's `TodoItem` is
    `CanAsync(Read)`-gated (a denied card is **not returned** in the result
    — C-M5·3); return the `BoardDetailResult`. The aggregate `AccessAudit`
    row (`TargetKind "board"`) is stored in the caller's session (C3).
  - The write lanes (U05) + the placement lanes (U06) are
    `NotImplementedException` stubs (the M4 U01 pin — the seam is the
    **frozen** surface; the logic lands in U05 / U06).
- `src/Kumunita.Core/Projects/ProjectRequests.cs` (new) — the DTO record
  types, verbatim from the design doc §2.3:
  - `namespace Kumunita.Core.Projects;`
  - `public sealed record CreateTodoRequest { public string Title { get; init; } public string? Body { get; init; } public string? ComponentId { get; init; } public string? AssigneeId { get; init; } public string? ParentId { get; init; } public Audience? Audience { get; init; } public string? LanguageCode { get; init; } public IReadOnlyList<string>? TagIds { get; init; } }`
  - `public sealed record UpdateTodoRequest { public string? Title { get; init; } public string? Body { get; init; } public string? ComponentId { get; init; } public string? Status { get; init; } public string? ParentId { get; init; } public bool ClearParent { get; init; } public string? LanguageCode { get; init; } public IReadOnlyList<string>? TagIds { get; init; } }`
  - `public sealed record CreateBoardRequest { public string Title { get; init; } public string? Description { get; init; } public string? ComponentId { get; init; } public Audience? Audience { get; init; } public string? LanguageCode { get; init; } public IReadOnlyList<CreateLaneRequest> Lanes { get; init; } }`
  - `public sealed record CreateLaneRequest { public string Title { get; init; } public string? Status { get; init; } public int? MaxItems { get; init; } public int Order { get; init; } }`
  - `public sealed record UpdateLaneRequest { public string? Title { get; init; } public string? Status { get; init; } public int? MaxItems { get; init; } public int? Order { get; init; } }`
  - `public sealed record TodoDetailResult { public TodoItem Todo { get; init; } public IReadOnlyList<TodoItem> Subtasks { get; init; } }`
  - `public sealed record BoardDetailResult { public KanbanBoard Board { get; init; } public IReadOnlyList<LaneDetail> Lanes { get; init; } }`
  - `public sealed record LaneDetail { public KanbanLane Lane { get; init; } public IReadOnlyList<TodoItem> Cards { get; init; } }`
- `src/Kumunita.Core/DependencyInjection.cs` (edit) — add the
  `IProjectService` registration **next to** the `IEventService`
  registration (L186), mirroring the `AddTransient` + the
  `IDocumentStore` / `IAuthorizationService` / `IUserInfoService`
  constructor shape:
  `services.AddTransient<Projects.IProjectService>(sp => new Projects.ProjectService(
    sp.GetRequiredService<Marten.IDocumentStore>(),
    sp.GetRequiredService<IAuthorizationService>(),
    sp.GetRequiredService<IUserInfoService>()));`
  (the **M4 U01** pin — the seam is registered in U04, the logic lands in
  U05 / U06; the **M5** pin is the same).

**Exit:** `dotnet build Kumunita.slnx -c Debug` is green (the
`IProjectService` interface compiles, the `ProjectService` read-lane
implementation compiles, the DTOs compile, the DI registration compiles).
**No new tests** (the read lanes are exercised in U12's pinned tests — the
F1 / F2 / F3 FACES). Handoff note: 6–8 lines starting `## U04 — service +
read lanes + DI`, listing (a) the `IProjectService` full surface (the read +
write + placement method names, by file), (b) the read-lane implementation
(the `ListTodosAsync` / `GetTodoAsync` / `ListBoardsAsync` / `GetBoardAsync`
shapes, by file), (c) the DTOs (by file), (d) the DI registration (by file),
(e) the build-green confirmation. **Move the unit file**
`in-progress/projects/U04.md` → `done/projects/U04.md`.

# U02 — the **two documents** + `M6DocTypes` + the **`NotificationKinds`** static class + boot wiring

**Goal:** create the **`Kumunita.Core.Notifications`** context —
**`Notification.cs`**, **`NotificationPreference.cs`**,
**`NotificationKinds.cs`** (the nine `public const string`
members), and **`M6DocTypes.cs`** (the `M5DocTypes` pattern
verbatim) — and register the surface in
`src/Kumunita.Core/DependencyInjection.cs` (the
`M5DocTypes` / `M4DocTypes` boot-wiring precedent).

**Entry reads (≤ 5 files):**
- **`docs/design/m6-notifications-design.md`** — **§6.1**
  (the exact C# shapes of the two documents + the
  `NotificationKinds` static class — the field names, types,
  nullability, the `IReadOnlyList<string>? KindsEnabled`
  nullable-list shape) and **§3** (the C-M6·1 / C-M6·2
  invariants — the closed-string-set pin + the
  additive-surface pin).
- **`src/Kumunita.Core/M5DocTypes.cs`** — the **surface
  pattern to mirror verbatim** (the `public static class
  M5DocTypes` shape, the `AddM5(...)` / `Register(...)` method,
  the delta-detected + idempotent registration, the
  ADR 0004 §B.1 comment shape).
- **`src/Kumunita.Core/Projects/Project.cs`** (or
  `TodoItem.cs`) — the **M5 POCO precedent** (the `string Id`
  convention, the `IReadOnlyList<...>?` nullable-list shape,
  the `DateTime` field convention — the `Created` /
  `ReadAt` / `Updated` shapes).
- **`src/Kumunita.Core/DependencyInjection.cs`** (lines 1–80) —
  the **boot wiring** (the `M5DocTypes` / `M4DocTypes` /
  `M3DocTypes` registration lines, the `AddM5(...)` /
  `AddM4(...)` / `AddM3(...)` call sites, the
  `IMailerStage` → `OutboxEmailStager` registration — the
  frozen seam M6 reuses, not re-registers).
- **`src/Kumunita.Core/Projects/KanbanStatuses.cs`** (or the
  equivalent M5 closed-string-set file) — the **M5
  `KanbanStatuses` precedent** (the `public static class` +
  `public const string` members — the exact shape to mirror
  for `NotificationKinds`).

**Deliverables (closed set):**
- **`src/Kumunita.Core/Notifications/Notification.cs`** (new) —
  the exact shape from design doc §6.1: `sealed record
  Notification { string Id; string RecipientId; string Kind;
  string? SourceId; string? Body; DateTime Created;
  DateTime? ReadAt; ... }` (the field names + types +
  nullability verbatim from §6.1; the `string Kind` is one of
  the nine `NotificationKinds` constants — the closed set,
  C-M6·2).
- **`src/Kumunita.Core/Notifications/NotificationPreference.cs`**
  (new) — the exact shape from design doc §6.1: `sealed
  record NotificationPreference { string RecipientId;
  IReadOnlyList<string>? KindsEnabled; DateTime Updated; ...
  }` (the `IReadOnlyList<string>? KindsEnabled` nullable-list
  shape — `null` / empty = all enabled, the C-M6·9 pin).
- **`src/Kumunita.Core/Notifications/NotificationKinds.cs`**
  (new) — the exact shape from design doc §6.1: `public static
  class NotificationKinds { public const string PostReply =
  "post.reply"; public const string PostMention =
  "post.mention"; public const string GroupPost =
  "group.post"; public const string EventRsvp =
  "event.rsvp"; public const string EventReminder =
  "event.reminder"; public const string ReportFiled =
  "report.filed"; public const string ReportAssigned =
  "report.assigned"; public const string ReportResolved =
  "report.resolved"; public const string TodoAssign =
  "todo.assign"; }` (the **nine** constants, the M5
  `KanbanStatuses` shape verbatim — a code-owned closed set,
  C-M6·2; `PostMention` is *reserved, not wired* — the constant
  exists, no emitter calls it in M6).
- **`src/Kumunita.Core/M6DocTypes.cs`** (new) — the
  `M5DocTypes` pattern verbatim: `public static class
  M6DocTypes` with the `Notification` + `NotificationPreference`
  registration (the delta-detected + idempotent shape, the
  ADR 0004 §B.1 comment — additive, no seeding, zero
  migrations for existing surfaces, C-M6·1).
- **`src/Kumunita.Core/DependencyInjection.cs`** (modified) —
  add the `M6DocTypes` registration (the `AddM6(...)` line,
  mirroring the `AddM5(...)` / `AddM4(...)` / `AddM3(...)`
  call sites; the `NotificationService` registration is
  **U03's** deliverable — do *not* add it here).
- **Nothing else.** No service, no controller, no views, no
  tests, no ADR.

**Exit:**
- `dotnet build Kumunita.slnx -c Debug` is **green** (the two
  new documents + the surface + the `NotificationKinds` static
  class compile; the boot wiring is registered; no
  `IMailerStage` / `IUserInfoService` / `ITranslationProvider`
  seam was re-shaped — the C-M6·11 reuse pin holds).
- The two documents + the `NotificationKinds` static class are
  **verbatim** from design doc §6.1 (a later unit can copy
  them verbatim into the service's `.cs` file).
- Append **`## U02`** to the handoff note.
- Move **`in-progress/notifications/U02.md` →
  `done/notifications/U02.md`**.

# U10 — the **acceptance gate** + the **close** (roadmap trio, ARCHITECTURE.md §3 + §5 sync, README, handoff summary, folder move)

**Goal:** run the **three-test acceptance gate** (the
closed-loop / handoff / part-vs-whole shapes, the M4 /
M5 gate precedent) and **record** the result in the
handoff note; **confirm** the roadmap trio (M5
`StatusDone`, M6 `StatusNext` → **`StatusDone`**, M7
`StatusNext` — the *close* move, the M4 / M5 close
precedent); **sync** `docs/ARCHITECTURE.md` §3 (the
bounded-context layout — the `Kumunita.Core.Notifications`
context added) + §5 (the M6 value-chain row — the
"shared awareness" arrow, the *shipped* status);
**confirm** the README Roadmap (M6 Notifications →
"Done", M7 → "In progress"); **move** the lane's unit
files from
`in-progress/notifications/` → `done/notifications/`;
**append** the **`## U10`** close section to the
handoff note (the gate result, the one-paragraph
summary, the deferral list re-stated).

**Entry reads (≤ 5 files):**
- **`docs/design/m6-notifications-design.md`** — **§6.5**
  (the **three-test acceptance gate** shapes — the
  closed-loop / handoff / part-vs-whole, the M4 / M5
  gate precedent), **§6.6** (the drift-guard — the
  *sole* mechanism for a mid-lane doc fix, the
  register's unit-series rule 9).
- **`docs/plans-milestones/plan-m6-notifications.md`** —
  the lane register: the Understanding / Assumptions /
  invariants / FACES (the *source of truth* for the
  close's ARCHITECTURE.md + README sync), the unit
  index (the 11 units, the close's *scope*).
- **`docs/ARCHITECTURE.md`** (lines 1–160) — the §0
  value-chain row for M6 (the "shared awareness" arrow,
  the *shipped* status — the close's *sync* target),
  the §3 bounded-context layout (the
  `Kumunita.Core.Notifications` context — the close's
  *addition*), the §5 milestone table (the M6 row — the
  close's *status flip*).
- **`src/Kumunita.Web/Milestones.cs`** +
  **`tests/Kumunita.Web.Tests/MilestonesTests.cs`** —
  the **roadmap trio to flip** (M5 `StatusDone` → *unchanged*;
  M6 `StatusNext` → **`StatusDone`**; M7 `StatusPlanned`
  → **`StatusNext`** — the M4 / M5 close precedent; the
  `MilestonesTests.cs` pin — the test asserts the
  *exact* order + the *single-in-progress* milestone,
  the close's *test update*).
- **`README.md`** (the Status section + the Roadmap) —
  the **roadmap row to flip** (M6 Notifications →
  "Done", M7 → "In progress" — the M4 / M5 close
  precedent; the deferral list — the close's *re-state*).

**Deliverables (closed set):**
- **The acceptance gate run** (the M4 / M5 gate
  precedent — the three tests, the *recorded* result):
  - **The closed-loop test:** the F1 face end-to-end
    (a reply on a post the resident authored → the
    inbox row is stored + the email is staged + the
    recipient's `Profile.EmailLanguage` is used for the
    template) — the **`dotnet exec
    tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll
    --filter-method *F1*`** run (the AGENTS.md
    test-runner pin, the U09 test).
  - **The handoff test:** the F9 + F10 faces (the
    preference + the re-emission) — the **`dotnet
    exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll
    --filter-method *F9* *F10*`** run (the U09 tests).
  - **The part-vs-whole test:** the F11 + F12 faces
    (the no-audit-row + the recipient-language pins) —
    the **`dotnet exec
    tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll
    --filter-method *F11* *F12*`** run (the U09 tests).
  - **The gate result:** the **`## U10 — Acceptance
    gate`** section appended to the handoff note (the
    three test names, the pass/fail status, the
    `postgres:18` container cleanup note — the
    AGENTS.md `docker container prune` pin).
- **`src/Kumunita.Web/Milestones.cs`** (modified) — the
  **roadmap trio flip** (the M4 / M5 close precedent):
  M5 `StatusDone` → *unchanged*; M6 `StatusNext` →
  **`StatusDone`** (the M6 Notifications row, the
  *shipped* status); M7 `StatusPlanned` →
  **`StatusNext`** (the M7 row, the *in-progress*
  status). **The `Milestones.cs` doc-comment** (the
  "keep in sync with the README Roadmap" pin) is
  *untouched*.
- **`tests/Kumunita.Web.Tests/MilestonesTests.cs`**
  (modified) — the **roadmap-trio test update** (the
  *exact* order + the *single-in-progress* milestone
  pin — the M5 `StatusDone` / M6 `StatusDone` / M7
  `StatusNext` shape, the M4 / M5 close precedent; the
  test asserts the *new* trio, the *old* trio is
  *removed*).
- **`README.md`** (modified) — the **Roadmap row
  flip** (M6 Notifications → "Done", M7 → "In
  progress" — the M4 / M5 close precedent; the
  deferral list — the *re-state*: PWA push → M9,
  digests, per-kind inbox filters, group/community todo
  assignment, guardian notification *still deferred*,
  read receipts, federation — the register's
  Assumptions, *verbatim*).
- **`docs/ARCHITECTURE.md`** (modified) — the **§3 +
  §5 sync** (the M4 / M5 close precedent):
  - **§3 (the bounded-context layout):** add the
    `Kumunita.Core.Notifications` context (the
    `Notification` + `NotificationPreference`
    documents, the `M6DocTypes` surface, the
    `NotificationService` service — the *shipped*
    shape, the M5 `Kumunita.Core.Projects` precedent).
  - **§5 (the milestone table):** the M6 row — the
    "shared awareness" arrow, the *shipped* status
    (the M4 / M5 close precedent).
  - **§0 (the value-chain table):** the M6 row — the
    "shared awareness" arrow, the *shipped* status
    (the M4 / M5 close precedent).
- **The folder move** (the M4 / M5 close precedent):
  **`in-progress/notifications/` →
  `done/notifications/`** — the *whole* unit-file set
  (the `U00.md` … `U10.md` files + the
  `notifications-handoff-notes.md`) moves *wholesale*
  (the `done/m4/` / `done/ev-dwm/` / `done/projects/`
  precedent — the per-lane subfolder, the cross-lane
  `U0#` collision avoidance). **The lane register
  stays** at `docs/plans-milestones/plan-m6-notifications.md`
  (it does *not* move with the units — the
  register's *position* is stable, the unit-folder
  *position* flips).
- **The `## U10` close section** appended to
  `docs/plans-milestones/in-progress/notifications/notifications-handoff-notes.md`
  (the *last* section, the M4 / M5 close precedent):
  the gate result (the three test names + the
  pass/fail status), the one-paragraph summary (the
  M6 Notifications lane — the *shipped* shape, the
  *reused* seams, the *deferred* list), the deferral
  list re-stated (the register's Assumptions, *verbatim*),
  the **next lane** (the M7 milestone — the *next*
  `StatusNext` on the roadmap).
- **Nothing else.** No new code (the U02–U08 code is
  *shipped*; the U09 tests are *shipped*; U10 is the
  *close* — the doc sync + the folder move + the gate
  recording). **No ADR** (the ADR 0076 is *Accepted* in
  U01 — the close does *not* re-open it; a *new* ADR
  for a *new* capability is a *new* lane, the
  register's unit-series rule 2).

**Exit:**
- **`dotnet build Kumunita.slnx -c Debug`** is **green**
  (the `Milestones.cs` + `MilestonesTests.cs`
  modifications compile; the ARCHITECTURE.md + README
  modifications are *markdown*, not compiled).
- **`dotnet exec
  tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll`**
  is **green** (the 12 FACES tests pass, the
  `postgres:18` container is cleaned up — the
  AGENTS.md `docker container prune` pin).
- **`dotnet exec
  tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll`**
  is **green** (the 5 controller pins pass, the
  `MilestonesTests.cs` update passes — the *new*
  trio M5-Done / M6-Done / M7-Next is the *pinned*
  shape).
- **The three-test acceptance gate** is **recorded**
  (the `## U10 — Acceptance gate` section in the
  handoff note — the three test names + the pass/fail
  status, the M4 / M5 gate precedent).
- **The roadmap trio** is **flipped** (M5 `StatusDone`
  *unchanged*, M6 `StatusNext` → **`StatusDone`**, M7
  `StatusPlanned` → **`StatusNext`** — the M4 / M5
  close precedent; the `Milestones.cs` + README +
  `MilestonesTests.cs` are *in sync*).
- **The lane's unit files** are **moved**
  (`in-progress/notifications/` →
  `done/notifications/` — the M4 / M5 close precedent;
  the lane register *stays* at
  `docs/plans-milestones/plan-m6-notifications.md`).
- **The `## U10` close section** is **appended** to the
  handoff note (the gate result, the one-paragraph
  summary, the deferral list re-stated, the next lane
  named).
- **Move `in-progress/notifications/U10.md` →
  `done/notifications/U10.md`** (the *last* unit file,
  the close — the lane is now *done*).

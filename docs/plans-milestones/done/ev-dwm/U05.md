# U05 — Day view (1-column time grid) + `site.css` polish + the 3 `kw-l` keys × 4 languages

**Goal:** add the **Day view** — a 1-column time grid that **reuses the U04
`events-calendar-time.ts` module** (no new TS) — and finalize the `site.css`
time-grid + toggle polish and add the **3 `kw-l` keys × 4 languages**
(`events.calendar.view.day/.week/.month`).

**Entry reads:**
- `docs/design/events-calendar-dwm-design.md` §3.3 + §3.6 (Day shape + the 3
  keys).
- `src/Kumunita.Web/Views/Event/Calendar.cshtml` (U03+U04's shape — the
  Day/Week branch; the toggle labels are the one place the 3 new keys are
  used).
- `src/Kumunita.Web/client/lib/events-calendar-time.ts` (U04's module —
  confirm it self-wires for `data-view == "day"` too — it already guards on
  `!= "month"`, so Day needs no TS change; read-only).
- `src/Kumunita.Core/Localization/KnownTranslationKeys.cs` the
  `events.calendar.*` block in **each** of the four language sections
  (en ~L1131, de ~L2206, fr ~L3283, da ~L4355 — the host for the 3 new keys).
- `src/Kumunita.Web/wwwroot/css/site.css` the toggle + time-grid rules
  (U03/U04's additions; the final polish).

**Deliverables (2 files):**
- `src/Kumunita.Web/Views/Event/Calendar.cshtml`:
  - add the **Day branch** (1-column time grid) — it is the **same**
    time-ruler markup as Week (U04) with a **single** `WindowDays` column;
    since the U04 Day/Week branch already renders per `WindowDays` column,
    the Day view works with `WindowDays.Count == 1` (the U02 engine ships
    that) — confirm the markup is column-count-agnostic and add only what
    Day needs (a single-column layout + a day heading). **No new TS.**
  - label the three toggle buttons with the **3 new keys**:
    `<kw-l key="events.calendar.view.day">Day</kw-l>`,
    `<kw-l key="events.calendar.view.week">Week</kw-l>`,
    `<kw-l key="events.calendar.view.month">Month</kw-l>` (English fallback
    inline — C-DWM·9).
- `src/Kumunita.Core/Localization/KnownTranslationKeys.cs` — add the **3
  keys** (`events.calendar.view.day`, `.week`, `.month`) to **all four**
  language blocks (en/de/fr/da), immediately after the existing
  `events.calendar.*` keys in each block (the ADR 0015 registry shape; en is
  authoritative, de/fr/da are translations). En text: "Day" / "Week" /
  "Month"; de: "Tag" / "Woche" / "Monat"; fr: "Jour" / "Semaine" / "Mois";
  da: "Dag" / "Uge" / "Måned".
- `src/Kumunita.Web/wwwroot/css/site.css` — the **Day single-column**
  polish (full-width day column, the hour ruler spanning the width) + any
  final toggle/time-grid polish so all three views are consistent. (CSS-only;
  a third file if the polish is non-trivial, otherwise folded into U04's
  rules.)

**Exit:** `run_build` on `Kumunita.Web` green; **all three views render**
(Day = 1 column, Week = 7 columns Monday-start, Month = a true calendar
month) with the toggle + labels correct in **all four languages** (verify
with the `LocaleCookie` per AGENTS.md / the browser harness). The 3 keys
present in en/de/fr/da (grep-verified). Handoff note: 5–6 lines starting
`## U05 — Day + keys + CSS` — (a) the Day branch (1 column, reuses the
U04 module — no new TS), (b) the 3 keys × 4 langs (file + the four block
locations), (c) the CSS polish (one line), (d) the two/three file paths.
**Move the unit file** `in-progress/ev-dwm/U05.md` → `done/ev-dwm/U05.md`.

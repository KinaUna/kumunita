# U07 — acceptance gate + close (roadmap trio, ARCHITECTURE.md, handoff summary, move folder to done/)

**Goal:** execute and **record** the three-test acceptance gate (closed-loop /
handoff / part-vs-whole) from the design doc §6.4, **using** U06's 9 tests as
the part-vs-whole evidence; **close the lane** on the roadmap trio
(`EV-DWM` → `StatusDone`, `M5` → `StatusNext`); sync `ARCHITECTURE.md`; write
the handoff summary; and **move the lane folder** `in-progress/ev-dwm/` →
`done/ev-dwm/`.

**Entry reads:**
- `docs/design/events-calendar-dwm-design.md` §6.4 (the gate's three tests +
  their definitions) + §6.5 (the drift-guard).
- `docs/plans-milestones/in-progress/ev-dwm/handoff-notes.md` (all `## U#`
  sections U00–U06 wrote — especially U06's 9-test results).
- `tests/Kumunita.Core.Tests/EventCalendarSeamTests.cs` +
  `tests/Kumunita.Web.Tests/EventControllerTests.cs` (the 9 lane tests the
  gate's part-vs-whole invokes).
- `src/Kumunita.Web/Milestones.cs` (the `EV-DWM` row to flip `StatusNext` →
  `StatusDone`, and `M5` `StatusPlanned` → `StatusNext`).
- `tests/Kumunita.Web.Tests/MilestonesTests.cs` (the pins to flip back:
  `Ids` keeps `EV-DWM`; the single-in-progress pin test reverts to
  `M5_Is_The_Single_InProgress_Milestone`; the `Shipped` list gains
  `"EV-DWM"`).
- `docs/ARCHITECTURE.md` § the events surface (the `Events/` line + the
  `EV-CAL` calendar note to extend with the `EV-DWM` views).

**Deliverables (≤ 5 files + the folder move):**
- `docs/design/events-calendar-dwm-design.md` — append `### Run result (EV-DWM
  acceptance gate — <date>)`: the three test names, their pass/red status, the
  9-test count (from U06), and one line per any `## U<m> — Drift pause`
  section in the handoff note (each resolved or still open). **No code, no
  build.**
- `src/Kumunita.Web/Milestones.cs` — flip `EV-DWM` `StatusNext` →
  `StatusDone`; flip `M5` `StatusPlanned` → `StatusNext`.
- `tests/Kumunita.Web.Tests/MilestonesTests.cs` — the single-in-progress pin
  test **reverts** to `M5_Is_The_Single_InProgress_Milestone` (id `"M5"`);
  the `Shipped` list **gains** `"EV-DWM"`; `Ids` is unchanged.
- `README.md` — the Roadmap `EV-DWM` row moves from **In progress** to
  **Done**; the `M5` row moves from **Planned** back to **In progress**
  (next).
- `docs/ARCHITECTURE.md` — the events surface note extends: the `EV-CAL`
  calendar gains **Day / Week (Monday-start, time-ruler) / Month (true
  calendar month) views** — one additive `?view=` selector, per-view window
  in the controller, **zero Core change** (ADR 0064).
- `docs/plans-milestones/in-progress/ev-dwm/handoff-notes.md` — append
  `## Summary` — a table of the shipped units (U00–U07), each with its
  one-liner goal + test count + any deviations + the out-of-scope/future-lane
  list (each named, each with a one-line candidate lane).
- **The folder move:** move the whole lane folder
  `docs/plans-milestones/in-progress/ev-dwm/` →
  `docs/plans-milestones/done/ev-dwm/` (the per-lane subfolder convention —
  matches `done/events-calendar/`; keeps the `U0#` files + `handoff-notes.md`
  together and avoids cross-lane `U0#` collisions). Also move the lane-plan
  reference: the top-level `docs/plans-milestones/plan-ev-dwm.md` stays as the
  lane register (it is the persistent lane doc; the per-unit files under it
  are now in `done/ev-dwm/`).

**Exit:** no build (U06's build is the last code build). **The three-tier
contract is intact:** the design doc ends with the `## EV-DWM — Closed`
record (append it: the gate's three tests + the `ARCHITECTURE.md` flip + the
future-lane list), the handoff note has its `## Summary`, and the lane folder
is in `done/`. **MilestonesTests green** via `dotnet exec …Web.Tests.dll`.
Handoff note: the `## Summary` section is present + the folder is in
`done/ev-dwm/`. **This is the last handoff note for the lane** (it is the
handoff for the next agent — the one who opens a future lane, e.g. a
week-start override or the overlap-lane algorithm, named above).

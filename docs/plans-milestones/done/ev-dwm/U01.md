# U01 — Design doc Part 2 (exact shapes, pinned tests, gate, drift-guard) + ADR 0064 + roadmap open

**Goal:** append Part 2 (the exact view-model + controller + TS shapes, the 9
pinned test names, the three-test acceptance gate, the drift-guard) to the
design doc; **create ADR 0064** (Status **Accepted**, all §3 markers flipped to
**[DECIDED — ADR 0064]**); **open the lane row** on the roadmap trio
(`EV-DWM` → `StatusNext`, `M5` → `StatusPlanned`). No domain code.

**Entry reads:**
- U00's Part 1 (the primary source — the D1–D7 decisions, C-DWM·1…9, F1–F8).
- `docs/adr/0063-events-calendar-view.md` (the format + the `Amends` clause
  shape; the precedent for a follow-on-lane ADR on the same context).
- `docs/adr/0019-timezone-platform-default-and-resident-override.md` (the
  `EffectiveTimezoneResolver` anchor math the window uses — confirm the
  local-midnight → UTC conversion shape).
- `src/Kumunita.Web/Controllers/EventController.cs` `Calendar` action only
  (the existing window math: parse `from` → local midnight → UTC → `+30d`;
  the `PrevAnchor`/`NextAnchor` pre-render; the place the `?view=` engine
  lands).
- `src/Kumunita.Web/Models/EventEditorModel.cs` `EventCalendarViewModel`
  (the record to extend — the exact field list to pin as additive).
- `src/Kumunita.Web/Milestones.cs` (the `EV-DWM` row to insert after
  `EV-CAL`, before `M5`).
- `tests/Kumunita.Web.Tests/MilestonesTests.cs` (the three pins to update:
  the `Ids` array, the shipped list, the single-in-progress pin test).
- `README.md` Roadmap section (the `EV-DWM` row to add).

**Deliverables (≤ 6 files):**
- `docs/design/events-calendar-dwm-design.md` — append `## 6. The exact
  shapes (Part 2)`:
  - `### 6.1 The view-model (exact C#)` — `EventCalendarViewModel` gains
    **exactly two additive fields** (defaulted, so no other call site
    breaks) + a generalization: add `string View = "month"` and
    `IReadOnlyList<DateTime> WindowDays` (the ordered list of the view's
    anchor days — the grid's columns), and rename `MonthLabel` to `Label`
    (the view-appropriate display string: a full date for Day, a date range
    for Week, a month name for Month). Pin the exact record shape:
    `(IReadOnlyList<EventRow> Events, string FromAnchor, string? PrevAnchor,
    string? NextAnchor, string Label, string? CurrentComponentId,
    IReadOnlyList<(string Id, string Name)> Components, string TimeZoneId,
    string View = "month", IReadOnlyList<DateTime> WindowDays = null!)`.
    (The `MonthLabel`→`Label` rename touches the one existing
    `Calendar.cshtml` reference — allowed, U03 owns that view.)
  - `### 6.2 The `Calendar` action engine (exact C#)` —
    `EventController.Calendar(string? from, string? componentId, string?
    view)`: parse `view` (default + invalid/out-of-set fallback = `"month"`);
    parse `from` (default today in the effective zone) → the **anchor date**;
    compute the **window** per view: **day** = `[anchorLocalStartUtc,
    anchorLocalStartUtc + 1d)`; **week** = `[weekStartLocalStartUtc,
    weekStartLocalStartUtc + 7d)` where `weekStartLocalStartUtc` = the
    anchor's **Monday** (local) at midnight → UTC (C-DWM·5); **month** =
    `[monthStartLocalStartUtc, monthStartLocalStartUtc + <months' days>)`
    (the anchor's calendar month, its length computed from the month). Build
    `WindowDays` = the ordered local days of the grid (day: 1; week: 7 from
    Monday; month: the 5–6 weeks covering the calendar month, starting on the
    Monday on/before the 1st). Call `ListInRangeAsync(windowStartUtc,
    windowEndUtc, componentId, actorId)` — the seam is **unchanged** (C-DWM·1).
    Nav: prev/next = the anchor shifted by the **view's unit** (±1 day / ±1
    week / ±1 month), pre-rendered as plain GET links with `?view=` +
    `componentId` riding along (C-DWM·6); `Label` = the view-appropriate
    display string (Day: full date; Week: `Mon d – Mon d`; Month: month
    name + year), in the UI culture.
  - `### 6.3 The pinned tests (exact names)` — see the two lists below.
  - `### 6.4 The acceptance gate (U07 records)` — the three-test shape
    (the EV-CAL U09 precedent): **closed loop** (an author's published event
    on the anchor day appears in **Day**, **Week**, and **Month**, each with
    the right chip/block + one aggregate row), **handoff** (a group member
    added to the audience sees the event in all three views on the next
    render; the non-member's views still exclude it), **part-vs-whole** (the
    9 pinned tests pass together with the full M4 `EventServiceTests` + the
    existing EV-CAL seam + Web pin suites green — additive, nothing
    regressed).
  - `### 6.5 The drift-guard (frozen once written)` — the
    `EventCalendarViewModel` shape (+ the 2 additive fields + the `Label`
    generalization), the `Calendar(string? from, string? componentId,
    string? view)` signature, the per-view window math (day/week/month +
    Monday-start), the 3 `kw-l` key names, the 9 invariants, the 8 FACES, the
    D1–D7 decisions, and the 9 pinned test names — all frozen pins; any
    mismatch is a `## U<m> — Drift pause`.
- `docs/adr/0064-events-calendar-dwm-views.md` — the ADR (Status:
  **Accepted**, Date the day it lands). `Amends`: **0063** (the `EV-CAL`
  calendar this extends — the `?view=` param + per-view window + Day/Week
  time grids; the `ListInRangeAsync` seam is *reused, unchanged*),
  **0019 / 0020** (the timezone / date-format machinery the anchor + labels
  use), **0015** (the `kw-l` key registry — the 3 new keys), **0031** (the
  plain-TS / no-dependency pin — the `events-calendar-time.ts` module).
  Decision section encodes D1–D7 + the exact view-model + controller shape +
  the 9 test pins + the gate. **Status: zero Core change (D1) is the headline
  decision.**
- `src/Kumunita.Web/Milestones.cs` — insert the `EV-DWM` row after
  `EV-CAL`: `new("EV-DWM", "Events calendar day/week/month views — Day, Week (Monday-start, time-ruler), and Month (true calendar month) views over the same authorized events; one additive `?view=` selector on the existing route + per-view window in the controller; zero Core change (ADR 0064)", StatusNext)`;
  flip `M5` from `StatusNext` to `StatusPlanned`.
- `tests/Kumunita.Web.Tests/MilestonesTests.cs` — the `Ids` array gains
  `"EV-DWM"` between `"EV-CAL"` and `"M5"`; the single-in-progress pin test
  **renamed** `M5_Is_The_Single_InProgress_Milestone` →
  `EV_DWM_Is_The_Single_InProgress_Milestone` with id `"EV-DWM"`; the
  `Shipped` list is unchanged (EV-DWM is not shipped yet).
- `README.md` — the Roadmap section gains the `EV-DWM` row (the `EV-CAL`
  named-lane row shape) as **In progress**; the `M5` row moves to **Planned**.

**Exit:** `run_build` on `Kumunita.Web` + `Kumunita.Web.Tests` green
(the trio compiles + `MilestonesTests` passes via `dotnet exec
tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll` per
AGENTS.md). **Zero [PROPOSED] markers remain** in the design doc (grep-
verified). Handoff note: 6–8 lines starting `## U01 — design doc Part 2 + ADR
0064 + roadmap open` — (a) the sealed view-model shape + the `Calendar`
signature (one line each), (b) the 9 pinned test names by id, (c) the gate's
three test names, (d) the roadmap-trio files + the renamed pin test, (e) the
ADR 0064 path. **Move the unit file** `in-progress/ev-dwm/U01.md` →
`done/ev-dwm/U01.md`.

# U06 — Goal detail + composer + edit

**Goal:** add the **goal authoring surface**: `GET /projects/goals/{id}`
(the detail — title, description rendered markdown, audience,
**projects in this goal** via `ListProjectsForGoalAsync` (U03), created
date `kw-dt`, author) + `GET /projects/goals/new` / `POST /projects/goals`
(the composer — Title, Description with `bindRichEditor`, AudienceEditor
trio, language + component pickers) + `GET /projects/goals/{id}/edit` /
`POST /projects/goals/{id}` (the edit — standing creator ∪ GlobalAdmin,
the `UpdateGoalRequest` full-update shape). **`dotnet build` + `npm run
build` clean; Web tests pass.**

**Entry reads:** U05's handoff section (the landing + nav tab exist +
the `pl.*` keys registered so far + `dotnet build` clean);
`docs/design/pl-goals-projects-design.md` §5 (the goal detail / composer
wire — the field order, the audience shape, the projects-in-goal section)
+ §9.1 (the `pl.goal.*` keys this unit registers);
`src/Kumunita.Web/Controllers/ProjectsController.cs` (after U05 — the
`ProjectsIndex` action's actor-minting + picker-seed shapes to mirror;
the M5 `BoardNew` / `BoardEdit` actions are the **audience + picker +
full-update POST** shapes to mirror);
`src/Kumunita.Web/Views/Projects/BoardNew.cshtml` + `BoardEdit.cshtml`
(the composer / edit markup to mirror — the `bindRichEditor`
contenteditable wiring, the AudienceEditor trio markup, the picker
selects); `src/Kumunita.Web/Models/AudienceEditorModel.cs` (the
audience parse/shape — the single-source pin);
`src/Kumunita.Core/Projects/IProjectService.cs` (the U02 goal seams +
the U03 `ListProjectsForGoalAsync` — the exact signatures + DTOs);
`src/Kumunita.Core/Localization/KnownTranslationKeys.cs` (the `pl.*`
keys registered so far — this unit adds its own `pl.goal.*` set only).

**Deliverables (5 files touched / created):**
- `src/Kumunita.Web/Models/ProjectViewModels.cs` (extended):
  `GoalDetailViewModel` (the goal doc fields + `descriptionHtml`
  pre-rendered via `MarkdownRenderer` — the M5 board-detail shape +
  `audienceEditor` (the `AudienceEditorModel` verbatim) + `projectCards`
  (reused `GoalCard`-style cards, the `ListProjectsForGoalAsync`
  results) + `canEdit` (the standing preview, the M5 board-detail
  `canEdit` shape) + `picker` state),
  `GoalComposerViewModel` (Title, Description, `AudienceEditorModel`,
  `languagePickerOptions` — the ADR 0018 shape,
  `componentPickerOptions`) — for **both** new + edit (the M5
  `BoardNew` / `BoardEdit` share-one-view-model precedent — confirm the
  M5 shape in `ProjectViewModels.cs` / `EventEditorModel.cs` and
  mirror it exactly).
- `src/Kumunita.Web/Controllers/ProjectsController.cs` (edit): the six
  actions (the M5 `BoardDetail` / `BoardNew` / `BoardEdit` action
  shapes, verbatim-convention): `GoalDetail(string id, string? lang)`,
  `GoalNew(string? componentId, string? lang)`, `GoalCreate([FromForm]
  GoalComposerViewModel, ...)` → `CreateGoalRequest` (404-vs-403 by
  service construction; redirect to the new goal's detail on success —
  the M5 board-create redirect shape), `GoalEdit(string id, ...)`,
  `GoalUpdate(string id, [FromForm] GoalComposerViewModel, ...)` →
  `UpdateGoalRequest` (the service's standing re-check is the
  enforcement; the controller's `canEdit` preview is display-only —
  the frozen ADR 0006 split), each seeding its pickers (the
  `SeedComponentPickerAsync` / `SeedLanguagePickerAsync` shapes);
  **no new `AccessAction` / `AccessVia`** (the frozen authorization
  surface — the drift-guard pin).
- `src/Kumunita.Web/Views/Projects/GoalDetail.cshtml` (new): the
  sections in the design doc §5 order — title + status-line (the
  M5 `BoardDetail` header shape), description (the rendered
  `descriptionHtml`, the ADR 0025/0031 shape), audience (the
  `AudienceEditorModel` display, the M5 board-detail audience markup),
  **projects in this goal** (the `projectCards` list, each card →
  `/projects/projects/{id}` — the U07 route, same adjacent-unit
  relaxation as U05's), created date `kw-dt`, the **Edit** button
  (`canEdit`-gated, the M5 board-detail Edit shape).
- `src/Kumunita.Web/Views/Projects/GoalNew.cshtml` +
  `GoalEdit.cshtml` (new): Title input, Description
  contenteditable (the `bindRichEditor` wiring — the **real**
  toolbar/pane markup from `BoardNew.cshtml`, not a placeholder),
  the AudienceEditor trio (the `AudienceEditorModel` markup, verbatim
  from `BoardNew.cshtml`), the language + component picker selects,
  the submit button (`pl.goal.create` / `pl.goal.save` keys);
  `GoalEdit` adds the `formmethod`-correct POST to
  `/projects/goals/{id}` + the standing-note if `!canEdit` (the M5
  board-edit shape — the read-only rendering for a non-creator).
- `src/Kumunita.Core/Localization/KnownTranslationKeys.cs` (edit): the
  `pl.goal.*` set this unit's views use — **all four languages**
  (en/de/fr/da parity — the `KnownTranslationKeysTests` four-language
  pin).

**Sequencing note:** the **goal link** on the project detail (U07's
`pl.project.goal_link`) points **back** at this unit's
`/projects/goals/{id}` — the route is real after this unit. The
**project detail** route U06's project cards link to lands in U07
(adjacent-unit relaxation, same as U05's). The delete button (U09) is
**not** on this view yet.

**Exit criteria:**
- `dotnet build Kumunita.slnx -c Debug` clean.
- `npm --prefix src/Kumunita.Web run build` clean.
- `dotnet exec tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll`
  passes — the new `ProjectsControllerTests` goal-action pins (the
  design doc §9.7 list: goal detail 404-vs-403, goal create redirect,
  goal edit standing-gate rendering, goal update non-creator 403)
  **+** all existing M5 pins.
- `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll`
  still passes (the four-language pin).
- **No** project-detail views (U07), pickers (U08), or delete (U09)
  change.

**Feedback loop:** append the `## U06` section to
`docs/plans-milestones/in-progress/pl/pl-handoff-notes.md` (the six
actions, the view names, the view-models, the `pl.goal.*` keys +
four-language pass, the M5-route regression pass, the build clean
confirmations). The next unit (U07) reads only that section + its own
entry-read list.

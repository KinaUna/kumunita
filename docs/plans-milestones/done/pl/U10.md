# U10 — Close the lane: docs sync + gate check + folder move

**Goal:** bring the **docs / ADR / README / handoff-notes** surface in
lockstep with the code that U00–U09 shipped, run the lane's
**three-test gate**, then **move the lane folder** from
`docs/plans-milestones/in-progress/pl/` to
`docs/plans-milestones/done/pl/`. **No new code** — this unit is
docs + verification + the move. `dotnet build` + `npm run build` +
**all** tests still pass after the move (the regression pin — the
move must not break anything, it is a docs-folder move, not a
code move).

**Entry reads:** U09's handoff section (the delete lanes exist +
`dotnet build` clean); **the full lane plan** (`plan-pl-goals-projects.md`
— the three-test gate + the doc-sync checklist + the drift-guard,
verbatim); `docs/adr/README.md` (the ADR index — the U00-added ADR 0086
row, to verify it is present + correctly worded, and the lane's
"Amends 0067 / 0070 / 0079 / 0024 / 0006 / 0001-B / 0036 / 0025 / 0031 /
0018 / 0019–0020" provenance line matches the ADR 0086 file's own
"Amends" header); `docs/adr/0086-goals-projects-lane.md` (the U00-added
ADR — the U10 verification target, **not** a rewrite target — confirm
its "Accepted, 2026-09-25" status line + the decision body's D1–D12
parity with the lane plan);
`docs/design/pl-goals-projects-design.md` (the U00-added design doc —
the same verification, not rewrite); `README.md` (the Roadmap section
— the U10 edit target for the new `PL` lane entry);
`docs/ARCHITECTURE.md` (the U10 edit target — the Projects-context
tree node + the `M5DocTypes` / `IProjectService` surface mentions, if
any, that now need the two new docs + two new adapters + the new
seam groups reflected);
`docs/plans-milestones/in-progress/pl/pl-handoff-notes.md` (the
U00–U09 `## U##` sections — the gate-check target: each section's
"build clean" + "tests pass" claims, the D6 / D9 / ADR 0079 /
four-language-parity pins, the additive-surface pins);
`src/Kumunita.Web/Models/` + `src/Kumunita.Web/Controllers/ProjectsController.cs`
+ `src/Kumunita.Web/Views/Projects/` (the **post-U09** state — the
U10 verification target for the three-test gate's "closed loop" +
"handoff" + "part-vs-whole" claims, read-only, no edits).

**Deliverables (docs edits + the gate + the move):**
- `README.md` (edit): the **Roadmap** section — add the new `PL`
  lane entry, **in the lane's own voice** (the M5 / M6 / M7 lane
  entries' existing convention, matched exactly — the short
  description + the `PL` lane id + the ADR 0086 reference + the
  **Done** status), **inserted in the lane's own position** (after
  the M7 entry, per the lane plan's "next is M7 … then this lane"
  ordering — **or** wherever the existing lane entries' ordering
  convention places a newly-shipped named lane, matched exactly; the
  key pin is that the entry is **present**, **worded in the
  lane's-own-voice convention**, and **marked Done** — the exact
  insertion point is a judgment call the U10 agent makes by
  reading the existing Roadmap section's ordering and matching its
  convention, **not** by reordering the existing M7 / M8 / M9…
  entries). **Milestones.cs + MilestonesTests.cs are untouched**
  (the named-lane rule — the M-letter milestones' `StatusNext`
  ordering is not changed by a named lane shipping; the `M7`
  "In progress" status line stays exactly as U09 left it).
- `docs/ARCHITECTURE.md` (edit): the **Projects bounded-context
  node** — the doc-tree / context-tree section, add the two new docs
  (`ProjectGoal` / `Project`) + the two new adapters
  (`ProjectGoalToAuditableResource` /
  `ProjectToAuditableResource`) to the existing `TodoItem` /
  `KanbanBoard` + their two adapters' listing; the
  `IProjectService` surface listing, if any, add the new seam
  groups (the goal lanes U02, the project lanes U03, the
  association lanes U04, the delete lanes U09) to the existing M5
  seam listing — **additively**, matching the existing
  `TodoItem` / `KanbanBoard` / M5-seams listing's convention; the
  `M5DocTypes` mention, if any, add the two new doc registrations +
  the two new index additions (the U01 `M5DocTypes.cs` edit) —
  **additively**, matching the existing `M5DocTypes` listing's
  convention. No existing M5 / M6 / M7 line is **renamed / reordered
  / removed** (the additive-surface pin, extended to the docs
  layer).
- `docs/adr/README.md` (verify, **not** edit — U00 already added the
  ADR 0086 row): confirm the row is present, the number is 0086,
  the title + one-line description match ADR 0086's own header, the
  "Accepted" status matches, the "Amends …" provenance list matches
  ADR 0086's own "Amends" line. **If any of these is off, fix the
  row (or the ADR 0086 file, whichever is the outlier) to match** —
  the pin is parity between the two, the direction of the fix is a
  judgment call.
- `docs/plans-milestones/in-progress/pl/pl-handoff-notes.md` (edit):
  append the **`## U10`** section — the gate-check results (the
  three-test gate, the build / test clean confirmations post-move),
  the docs-sync confirmation (the `README.md` / `docs/adr/README.md`
  / `docs/ARCHITECTURE.md` edits made this unit), the folder-move
  confirmation (the `git mv` or equivalent + the new path), the
  "lane closed" line. This is the **last** `## U##` section in the
  file — the lane's handoff record is now complete.
- **The folder move** (the U10 signature act): move
  `docs/plans-milestones/in-progress/pl/` →
  `docs/plans-milestones/done/pl/` — the **entire** folder
  (`plan-pl-goals-projects.md` + `U00.md` … `U10.md` +
  `pl-handoff-notes.md`), not a selective copy. The `pl-handoff-notes.md`
  "Moves with the lane folder at close (U##)" header line is the
  pin — the file moves **with** the folder, not before it.
  **Use `git mv`** (not a plain `Move-Item` / `mv`), so the move is
  tracked as a rename in git's history, not a delete + add — the
  repo's existing `done/` lane folders' git-history convention
  (the M5 / M6 / M7 lane folders' move history, if any, to match;
  if the existing `done/` folders show plain `mv` history, match
  that instead — the pin is "the move is done the way the repo
  already does lane-folder moves," confirmed by reading the
  existing `done/` folders' git history).

**Sequencing note:** this unit is **strictly last** — no unit
follows it. The lane plan's "sequencing invariants" list
(`U00` → `U10`) ends here. After this unit's folder move, the
lane's `in-progress/pl/` folder is **gone** (moved to `done/pl/`),
and the lane is **closed** — no further unit in this lane reads
from or writes to `in-progress/pl/`.

**The three-test gate** (the lane plan's gate, run as the U10
exit's core, **before** the folder move — the move is the
celebration, the gate is the check):
- **Closed loop** (the D8 / D9 / D10 / D6 chain renders): the `/projects`
  landing (U05) shows a goal + a standalone project; the goal
  detail (U06) shows the goal's projects (the U03
  `ListProjectsForGoalAsync` seam); the project detail (U07) shows
  the goal link (U06 — real) + the associated to-dos / boards (the
  U04 `projectId` filter lanes); a to-do / board detail (U08) shows
  the project link (U07 — real, the D9 rule) when associated; a
  **deleted** goal's projects keep their `GoalId` + their goal link
  is not rendered (the U09 D6 rule); a **deleted** project's
  to-dos / boards keep their `ProjectId` + their project link is
  not rendered (the U09 D6 rule). **Verified by** reading the
  U05–U09 handoff sections' "tests pass" + "regression pass"
  claims + the `ProjectsControllerTests` + `ProjectServiceTests`
  pin list (the design doc §9.6 / §9.7 pin lists, the U05–U09
  "Exit criteria" test-name lists) — **no** new test is written in
  this unit, the gate is a **read + confirm** of the existing
  pins' coverage of this chain, not a new test run.
- **Handoff** (the frozen `IProjectService` surface + the standing
  re-check + the `AccessAudit` rows): the `IProjectService`
  surface's **additive-only** pin (no M5 / M6 / M7 seam renamed /
  reordered / removed across U02–U09 — confirmed by reading the
  U02–U09 handoff sections' "additive-surface pin" /
  "frozen-M5-surface pin" claims); the **standing re-check**
  pin (every write lane — U02 goal create/update, U03 project
  create/update, U04 association, U09 delete — re-checks
  standing server-side, the ADR 0006 split honored, no
  `AccessAction` / `AccessVia` added — confirmed by reading the
  U02–U09 handoff sections' "standing re-check" / "frozen ADR
  0006 split" claims); the **`AccessAudit` row** pin (every write
  lane stores an `AccessAudit` row with the correct
  `TargetKind` — `"goal"` / `"project"` / `"todo"` / `"board"` —
  confirmed by reading the U02–U09 handoff sections'
  "`AccessAudit` row" claims + the design doc §9.6 / §9.7 pin
  list).
- **Part-vs-whole** (the M5 surface is **untouched**, the lane's
  additions are **additive**): the M5 routes (`/projects/todos`,
  `/projects/boards`, `/projects/todos/{id}`,
  `/projects/boards/{id}`, + the M5 delete / assign / lane routes)
  render **exactly** as before U00 (confirmed by reading the
  U05–U09 handoff sections' "M5-route regression pass" /
  "frozen-M5-route" claims + the `ProjectsControllerTests` M5-pin
  list); the M5 DTOs (`CreateTodoRequest` / `UpdateTodoRequest` /
  `CreateBoardRequest` / `UpdateBoardRequest`) — the **only**
  change is the additive `ProjectId?` on the two create DTOs (the
  U08 decision, the `ParentId` composer convention mirrored) —
  confirmed by reading U08's handoff section's
  "additive `CreateTodoRequest` / `CreateBoardRequest` `ProjectId?`
  fields" claim; the `TodoItem` / `KanbanBoard` docs — the **only**
  change is the additive `ProjectId?` field (the U01 pin) —
  confirmed by reading U01's handoff section; the `M5DocTypes`
  registrations — the **only** change is the additive two new doc
  registrations + two new index additions (the U01 pin) —
  confirmed by reading U01's handoff section; the
  `Milestones.cs` / `MilestonesTests.cs` / `README.md` Roadmap's
  M7 "In progress" status — **untouched** (the named-lane rule,
  the U10 `README.md` edit adds the `PL` entry **additively**
  without reordering the existing M7 / M8 / M9… entries) —
  confirmed by this unit's `README.md` edit + a read of
  `Milestones.cs` / `MilestonesTests.cs` (no diff expected).

**Exit criteria:**
- `dotnet build Kumunita.slnx -c Debug` clean (post-move — the
  move is a docs-folder move, the build is unaffected, but the
  pin is "the build is clean after the move," confirming the move
  didn't accidentally touch a code file).
- `npm --prefix src/Kumunita.Web run build` clean (the same pin).
- `dotnet exec tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll`
  passes (the same pin — the full M5 + PL pin list, post-move).
- `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll`
  passes (the same pin — the full M5 + PL pin list, post-move).
- The folder move is done: `docs/plans-milestones/done/pl/`
  exists, `docs/plans-milestones/in-progress/pl/` does **not**
  (the move, not a copy), the `pl-handoff-notes.md` `## U10`
  section is present + is the **last** `## U##` section in the
  file.
- The three-test gate's read + confirm is done (the three
  bullets above, each with its "confirmed by reading …" line
  filled in with the actual handoff-section / pin-list references
  the U10 agent read — not a blanket "all good," the specific
  section + claim + pin-name references).

**Feedback loop:** the `## U10` section in
`pl-handoff-notes.md` **is** this unit's feedback-loop artifact —
it is the last `## U##` section, it closes the lane's handoff
record, and it is the thing the folder move carries to
`docs/plans-milestones/done/pl/pl-handoff-notes.md`. There is no
"next unit" for this lane — the lane is closed after this unit.

# U00 — Design doc + **ADR 0086** + roadmap confirm

**Goal:** author `docs/design/pl-goals-projects-design.md` (the D1–D12
decisions from the lane plan, **locked**, the exact C# seam shapes, the
pinned test names, the three-test gate, the drift-guard) and
**`docs/adr/0086-goals-projects-lane.md`** (Accepted); add the **ADR 0086**
row to `docs/adr/README.md`; **confirm the roadmap** (`M7` is the single
`StatusNext`; `PL` is a **named lane**, not a milestone — **no**
`Milestones.cs` / `MilestonesTests.cs` move). **No code, no build.**

**Entry reads:** the lane plan
`docs/plans-milestones/in-progress/pl/plan-pl-goals-projects.md` (the
D1–D12 assumptions are the primary source); `docs/design/m5-projects-design.md`
Part 1 + Part 2 (the **M5 design doc** — the lane template to mirror: the
two-part shape, the D1–D12 decisions, the C-M5·1… invariants, the Part 2
seam/test/gate shape, the `IProjectService` verbatim surface);
`docs/adr/0067-m5-projects-todos-and-kanban.md` (the ADR this one **extends**
— the bounded context, the `IProjectService` surface, the two adapters, the
`M5DocTypes` surface, the standing matrix; confirm what is frozen);
`docs/adr/0070-board-edit-lane-title-and-description.md` (the
**creator ∪ GlobalAdmin** standing precedent — the `UpdateBoardAsync`
full-update shape this lane reuses); `docs/adr/0079-todo-optional-start-and-due-dates.md`
(the **optional-date** shape — the `datetime-local` field shape, the blank →
`null` rule this lane reuses for `Project.StartAt` / `DueAt`);
`docs/adr/0024-author-soft-delete-lane.md` (the `IsDeleted` shape this lane
reuses for `ProjectGoal` + `Project`); `docs/adr/README.md` (the
ADR-numbering convention — confirm the next free number is **0086**);
`src/Kumunita.Core/Projects/IProjectService.cs` (the **frozen** M5 surface —
the new seams are **additive** on it); `src/Kumunita.Core/M5DocTypes.cs`
(the **existing** surface — the two new docs + the `ProjectId` indexes are
**additive** on it); `src/Kumunita.Web/Milestones.cs` (confirm `M7` is the
single `StatusNext`); `docs/philosophy/templates/design-doc.md` (required
section set).

**Deliverables (3 files + 1 edit):**
- `docs/design/pl-goals-projects-design.md` (new):
  - `## 1. What this lane is` — the overview (the higher-level **goals** +
    **projects** page on top of the M5 to-do / board surface; the two new
    docs, the `ProjectId` on `TodoItem` / `KanbanBoard`, the `/projects`
    landing page, the third nav tab).
  - `## 2. The existing surface this lane reuses (verified)` — the `Audience`
    doc (ADR 0001-B / 0036), the frozen `IAuthorizationService.CanAsync(Read)`
    / `CanSeeAsync(Read)` (ADR 0006), the standing matrix **creator ∪
    GlobalAdmin** (ADR 0070), the `MarkdownRenderer` + `bindRichEditor`
    (ADR 0025 / 0031), the `kw-dt` TagHelper (ADR 0019 / 0020), the
    `AudienceEditorModel` / component-picker / language-picker composer trio
    (the M2/M3/M4/M5 pattern), `M5DocTypes` (the surface the two new docs
    register on), `ProjectsController` (the controller the new routes extend),
    `_ProjectsTabs.cshtml` (the nav-tab partial the **Projects** tab extends),
    `KnownTranslationKeys.cs` (the `kw-l` registry the `pl.*` keys extend).
  - `## 3. Design decisions (locked)` — D1–D12 from the lane plan, verbatim
    (each `[PROPOSED]` in the lane plan becomes a locked decision here).
  - `## 4. Invariants` — the C-PL·1… invariants (the one-paragraph drift-
    guard from the lane plan, expanded to the individual pins: no new
    `AccessAction` / `AccessVia`; the standing matrix is creator ∪ GlobalAdmin;
    the `ProjectId` is a feed filter, never a gate; the `Status` on
    `Project` is a string, not an enum; the `StartAt` / `DueAt` are optional;
    the delete cascade is soft + non-destructive; the `/projects/todos` +
    `/projects/boards` routes keep their exact M5 paths).
  - `## 5. FACES` — the F1…F10 list (the closed-loop / handoff / part-vs-whole
    test shapes, the standing re-check, the `AccessAudit` row shape, the
    dangling-association rule, the feed-filter-not-a-gate pin, the 404-vs-403
    split, the nav-tab update, the project-picker seeding).
  - `## 6. Parts affected` — the file list (the two new docs, the two new
    adapters, `M5DocTypes`, `IProjectService`, `ProjectService`,
    `ProjectsController`, the `Views/Projects/` new views, the
    `_ProjectsTabs.cshtml` update, the `TodoDetail` / `BoardDetail` /
    `TodoEdit` / `BoardEdit` updates, `KnownTranslationKeys.cs`, the two
    test files).
  - `## 7. Feedback loops` — the unit sequence + the handoff-note convention
    (one `## U#` section per unit, appended never rewritten).
  - `## 8. Risks` — the `ProjectId` field on `TodoItem` / `KanbanBoard`
    (the additive-on-existing-doc risk — the ADR 0004 §B.1 delta-detect
    mitigation), the `ProjectService` surface growth (the frozen-surface
    risk — the full-interface-first pattern mitigation), the `kw-l` key
    count (the × 4-language risk — the `KnownTranslationKeys.cs` registry
    pin).
  - `## 9. Seams & contracts (Part 2, written by U0)` —
    - `### 9.1 the exact C# of the two new docs (verbatim)` —
      `ProjectGoal` (ns `Kumunita.Core.Projects`): `Id`; `Title` (non-empty);
      `Description?` (Markdown — the ADR 0025 shape, optional);
      `ComponentId?` (a feed filter, never a gate — C-M3·2); `AuthorId` (the
      standing owner — C-M5·6); `Audience?` (the **exact** post `Audience` —
      ADR 0001-B / 0036, `null` = public); `IsDeleted` (the ADR 0024 soft-
      delete flag, `false` default); `LanguageCode` (ADR 0018 authored-in
      tag); `Created`; `Modified?`. **No** `IsDraft`, **no** dates, **no**
      `ProjectId`.
      `Project` (ns `Kumunita.Core.Projects`): `Id`; `Title` (non-empty);
      `Description?` (Markdown); `GoalId?` (the optional goal — `null` =
      standalone project; the **sole** association mechanism, the
      `TodoItem.ParentId` shape in intent); `Status?` (a **string** — the
      state label; `null` = none; **not** an enum — the C-M5·4 string-status
      pin carried over); `StartAt?` / `DueAt?` (both **nullable**
      `DateTimeOffset` — the ADR 0079 optional-date shape); `ComponentId?`
      (feed filter, never a gate); `AuthorId` (standing owner); `Audience?`
      (the exact post `Audience`); `IsDeleted` (ADR 0024 flag);
      `LanguageCode` (ADR 0018); `Created`; `Modified?`. **No** `IsDraft`.
    - `### 9.2 the two new adapters (exact C#, verbatim)` —
      `ProjectGoalToAuditableResource : IAuditableResource` (ns
      `Kumunita.Core.Projects`): `Id => ProjectGoal.Id`,
      `Name => Title`, `OwnerId => AuthorId`, `Audience =>
      ProjectGoal.Audience`, `ComponentId => ProjectGoal.ComponentId`,
      `TargetKind => "goal"` (the **exact** string — matches the aggregate
      audit row shape in `AccessAudit`; C3).
      `ProjectToAuditableResource : IAuditableResource` (ns
      `Kumunita.Core.Projects`): `Id => Project.Id`, `Name => Title`,
      `OwnerId => AuthorId`, `Audience => Project.Audience`,
      `ComponentId => Project.ComponentId`, `TargetKind => "project"` (the
      **exact** string — matches the aggregate audit row shape in
      `AccessAudit`; C3).
    - `### 9.3 the additive `IProjectService` seams (exact C#, verbatim)` —
      - **Goal read lanes (U02):**
        `Task<IReadOnlyList<ProjectGoal>> ListGoalsAsync(string? componentId, string actorId, int page, CancellationToken ct = default);`
        — the **goal list** (the feed): the candidates are `!IsDeleted`,
        filtered by optional `ComponentId` (a filter, never a gate — C-M3·2);
        the survivors are `CanSeeAsync(Read)`-filtered (C6 / C3) over the
        `ProjectGoalToAuditableResource`; ordered by `Created` descending
        (the newest first — the post feed shape); paged. The **aggregate**
        `AccessAudit` row (`TargetKind "goal"`, `visibleCount` /
        `hiddenCount`) is the C-M3·3 shape.
        `Task<ProjectGoal> GetGoalAsync(string goalId, string actorId, CancellationToken ct = default);`
        — one goal; one `CanAsync(Read)`; `KeyNotFoundException` (404) on
        absent, `UnauthorizedAccessException` (403) on denied (the C3
        404-vs-403 split).
      - **Goal write lanes (U02):**
        `Task<ProjectGoal> CreateGoalAsync(string actorId, IReadOnlySet<string> actorRoles, CreateGoalRequest request, CancellationToken ct = default);`
        — the author becomes the standing owner (the `AuthorId` branch); the
        goal is **live on creation** (no `IsDraft` — the D8a precedent); the
        `AccessAudit` row (`goal.create`, `TargetKind = "goal"`) is stored in
        the caller's session (C3).
        `Task<ProjectGoal> UpdateGoalAsync(string goalId, string actorId, IReadOnlySet<string> actorRoles, UpdateGoalRequest request, CancellationToken ct = default);`
        — **creator ∪ GlobalAdmin** (the ADR 0070 board-edit precedent,
        enforced server-side per C-M5·6); a **full update** of
        `Title` + `Description` (the ADR 0070 shape — the board edit lane's
        `UpdateBoardAsync` shape, blank `Description` → `null`);
        `AuthorId` / `Created` preserved untouched; `Modified` stamped on a
        real change; the `AccessAudit` row (`goal.update`, `TargetKind =
        "goal"`) is stored in the caller's session (C3).
      - **Project read lanes (U03):**
        `Task<IReadOnlyList<Project>> ListProjectsAsync(string? componentId, string? goalId, string actorId, int page, CancellationToken ct = default);`
        — the **project list** (the feed): the candidates are `!IsDeleted`,
        filtered by optional `ComponentId` (a filter, never a gate — C-M3·2)
        **and** optional `GoalId` (a filter, never a gate — the
        `GoalId == null` filter is the **standalone-projects** feed, the
        `/projects` landing page's projects section); the survivors are
        `CanSeeAsync(Read)`-filtered (C6 / C3) over the
        `ProjectToAuditableResource`; ordered by `Created` descending; paged.
        The **aggregate** `AccessAudit` row (`TargetKind "project"`,
        `visibleCount` / `hiddenCount`) is the C-M3·3 shape.
        `Task<Project> GetProjectAsync(string projectId, string actorId, CancellationToken ct = default);`
        — one project; one `CanAsync(Read)`; the 404-vs-403 split (C3).
      - **Project write lanes (U03):**
        `Task<Project> CreateProjectAsync(string actorId, IReadOnlySet<string> actorRoles, CreateProjectRequest request, CancellationToken ct = default);`
        — the author becomes the standing owner; the project is **live on
        creation** (no `IsDraft`); the **`GoalId` guard**: a non-null
        `request.GoalId` pointing at a soft-deleted or unreadable goal is
        **refused** (`KeyNotFoundException` 404 on absent,
        `UnauthorizedAccessException` 403 on denied — the C3 split); the
        `AccessAudit` row (`project.create`, `TargetKind = "project"`) is
        stored in the caller's session (C3).
        `Task<Project> UpdateProjectAsync(string projectId, string actorId, IReadOnlySet<string> actorRoles, UpdateProjectRequest request, CancellationToken ct = default);`
        — **creator ∪ GlobalAdmin** (the ADR 0070 precedent, enforced
        server-side per C-M5·6); a **partial update** of `Title` /
        `Description` / `GoalId` / `Status` / `StartAt` / `DueAt` (the
        ADR 0079 optional-date shape — a non-null value is applied, a
        `null` clears it, the edit form's blank `datetime-local` field →
        `null`); the **`GoalId` guard** on re-association: a non-null
        `request.GoalId` pointing at a soft-deleted or unreadable goal is
        **refused** (the C3 split); `ClearGoal = true` is an explicit
        un-goal (sets `GoalId = null`); `AuthorId` / `Created` preserved
        untouched; `Modified` stamped on a real change; the `AccessAudit`
        row (`project.update`, `TargetKind = "project"`) is stored in the
        caller's session (C3).
      - **Association lanes (U04):**
        + the **existing** `ListTodosAsync` / `ListBoardsAsync` feed seams gain
        a **single additive** `string? projectId = null` optional parameter
        (appended after their existing trailing optional params — `unassignedOnly`
        for `ListTodosAsync`, `page` for `ListBoardsAsync`) — a **feed filter,
        never a gate** (C-M3·2): when non-null, narrows the candidate set to
        the to-dos / boards whose `ProjectId == projectId`; when `null`
        (the default), no filter — **every** existing call site + the existing
        M5 Core tests compile and behave **unchanged** (the additive-surface
        pin). This is the seam U08's project-detail "associated to-dos /
        boards" list (U07) + the `/projects` landing's project-card
        association count, if any, call.
        `Task<TodoItem> SetTodoProjectAsync(string todoItemId, string actorId, IReadOnlySet<string> actorRoles, string? projectId, CancellationToken ct = default);`
        — sets `TodoItem.ProjectId` to `projectId` (`null` = unassociate).
        **Creator ∪ assignee ∪ GlobalAdmin** over the **to-do** (the C-M5·6
        standing matrix, re-checked server-side). A non-null
        `projectId` pointing at a soft-deleted or unreadable project is
        **refused** (the C3 split). `AuthorId` / `Created` preserved
        untouched; `Modified` stamped; the `AccessAudit` row
        (`todo.set_project`, `TargetKind = "todo"`) is stored in the
        caller's session (C3).
        `Task<KanbanBoard> SetBoardProjectAsync(string boardId, string actorId, IReadOnlySet<string> actorRoles, string? projectId, CancellationToken ct = default);`
        — sets `KanbanBoard.ProjectId` to `projectId` (`null` =
        unassociate). **Creator ∪ GlobalAdmin** over the **board** (the
        ADR 0070 precedent, re-checked server-side). A non-null
        `projectId` pointing at a soft-deleted or unreadable project is
        **refused** (the C3 split). `AuthorId` / `Created` preserved
        untouched; `Modified` stamped; the `AccessAudit` row
        (`board.set_project`, `TargetKind = "board"`) is stored in the
        caller's session (C3).
      - **Delete lanes (U09):**
        `Task DeleteGoalAsync(string goalId, string actorId, IReadOnlySet<string> actorRoles, CancellationToken ct = default);`
        — sets `ProjectGoal.IsDeleted = true` (the ADR 0024 shape); the
        `Project.GoalId` rows are **kept** (the dangling-association rule —
        D6); **creator ∪ GlobalAdmin** (re-checked server-side); the
        `AccessAudit` row (`goal.delete`, `TargetKind = "goal"`) is stored
        in the caller's session (C3).
        `Task DeleteProjectAsync(string projectId, string actorId, IReadOnlySet<string> actorRoles, CancellationToken ct = default);`
        — sets `Project.IsDeleted = true` (the ADR 0024 shape); the
        `TodoItem.ProjectId` / `KanbanBoard.ProjectId` rows are **kept**
        (the dangling-association rule — D6); **creator ∪ GlobalAdmin**
        (re-checked server-side); the `AccessAudit` row
        (`project.delete`, `TargetKind = "project"`) is stored in the
        caller's session (C3).
    - `### 9.4 the request DTOs (exact C#, verbatim)` —
      `CreateGoalRequest`: `required string Title`; `string? Description`;
      `string? ComponentId`; `Audience?`; `string? LanguageCode`.
      `UpdateGoalRequest`: `required string Title`; `string? Description`
      (blank → `null`, the ADR 0070 shape).
      `CreateProjectRequest`: `required string Title`; `string? Description`;
      `string? GoalId` (the optional goal — `null` = standalone);
      `string? Status`; `DateTimeOffset? StartAt`; `DateTimeOffset? DueAt`;
      `string? ComponentId`; `Audience?`; `string? LanguageCode`.
      `UpdateProjectRequest`: `string? Title`; `string? Description`;
      `string? GoalId` (a non-null value **re-associates** the project to
      that goal); `bool ClearGoal` (`true` = explicit un-goal, sets
      `GoalId = null`); `string? Status`; `DateTimeOffset? StartAt`
      (ADR 0079 — non-null applied, null clears); `DateTimeOffset? DueAt`
      (ADR 0079 — non-null applied, null clears).
    - `### 9.5 the `M5DocTypes` additive shape (exact C#)` — the two new
      docs registered in `M5DocTypes.Configure` (the `TodoItem` /
      `KanbanBoard` feed-ordering index shape: the `(ComponentId, Created)`
      index on `ProjectGoal` + `Project`; the `GoalId` index on
      `Project`; the `ProjectId` index on `TodoItem` + `KanbanBoard`);
      **no** unique indexes (a goal / project may be associated with many
      to-dos / boards; a project may be under many goals is **not** the
      pin — a project has **at most one** goal, the `GoalId` is a
      **single** field, not a list).
    - `### 9.6 the pinned test names (the ~20 shape)` — the Core pins
      (`ProjectServiceTests.cs`): the goal feed (the `CanSeeAsync(Read)`
      filter), the goal detail (the 404-vs-403 split), the goal create +
      update (the standing re-check, the `AccessAudit` row), the project
      feed (the `GoalId` filter), the project detail (the 404-vs-403 split),
      the project create (the `GoalId` guard), the project update (the
      `ClearGoal` shape), the `SetTodoProjectAsync` (the standing re-check,
      the dangling-association rule), the `SetBoardProjectAsync` (the
      standing re-check), the `DeleteGoalAsync` (the soft-delete + the
      dangling-association rule), the `DeleteProjectAsync` (the soft-delete
      + the dangling-association rule). The Web pins
      (`ProjectsControllerTests.cs`): the `/projects` landing page (the
      goals + standalone-projects feed renders), the goal detail (the
      projects-in-this-goal list), the project detail (the associated to-dos
      / boards list), the nav-tab (the **Projects** tab active), the to-do
      edit (the project picker seeds), the to-do detail (the project link
      renders), the board edit (the project picker seeds), the board detail
      (the project link renders), the goal delete (the soft-delete + the
      dangling-association rule), the project delete (the soft-delete + the
      dangling-association rule).
    - `### 9.7 the three-test gate (U10 records)` — the closed loop /
      handoff / part-vs-whole (the lane plan's §The three-test gate,
      verbatim).
    - `### 9.8 the drift-guard (the lane plan's §Drift-guard, verbatim)`.

- `docs/adr/0086-goals-projects-lane.md` (new, **Accepted**, 2026-09-25):
  the `Status` / `Date` header, the `Amends` block (0067 / 0070 / 0079 /
  0024 / 0006 / 0001-B / 0036 / 0025 / 0031 / 0018 / 0019-0020), the
  `Context` (the M5 surface, the `ProjectId` gap, the lane's intent), the
  `Decision` bullets (D1–D12, the two new docs, the two new adapters, the
  additive `IProjectService` seams, the `ProjectId` on `TodoItem` /
  `KanbanBoard`, the `/projects` landing page, the third nav tab, the
  standing matrix, the soft-delete + dangling-association rule, the `kw-l`
  `pl.*` keys × 4 languages, the `Milestones.cs` / README Roadmap /
  `MilestonesTests.cs` **untouched** — a named lane, not a milestone), the
  `Consequences` (the two new docs on the `M5DocTypes` surface, the two new
  adapters on the frozen `IAuthorizationService` path, the additive
  `IProjectService` surface, the `/projects` route, the nav-tab update, the
  `kw-l` registry growth).

- `docs/adr/README.md` (edit): add the **ADR 0086** row (the `| 0086 |`
  shape, the `Accepted` status, the one-sentence summary).

- `docs/plans-milestones/in-progress/pl/pl-handoff-notes.md` (new): the
  scratch-tier header (the "one `## U#` section per unit, appended (never
  rewritten)" convention, the M5 `projects-handoff-notes.md` header shape)
  + the **`## U00`** section (the feedback loop below).

**Exit criteria:**
- `docs/design/pl-goals-projects-design.md` exists, 9 sections, the D1–D12
  locked, the exact C# shapes verbatim, the pinned test names, the three-
  test gate, the drift-guard.
- `docs/plans-milestones/in-progress/pl/pl-handoff-notes.md` exists with
  the header + the `## U00` section.
- `docs/adr/0086-goals-projects-lane.md` exists, `Accepted`, 2026-09-25,
  the `Amends` block, the `Decision` bullets, the `Consequences`.
- `docs/adr/README.md` has the **ADR 0086** row.
- **No code, no build, no tests.** `Milestones.cs` / `MilestonesTests.cs`
  **untouched** (a named lane, not a milestone).

**Feedback loop:** append the `## U00` section to
`docs/plans-milestones/in-progress/pl/pl-handoff-notes.md` (the one-paragraph
shape: what was authored, the ADR number, the D1–D12 lock, the exact C# seam
list, the pinned test count, the roadmap confirm). The next unit (U01) reads
only that section + its own entry-read list.

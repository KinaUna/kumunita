# U00 — design doc **Part 1** (context, scope, invariants, FACES)

**Goal:** author **Part 1** of
`docs/design/m6-notifications-design.md` — the design doc's
**Context** (the shared-awareness arrow + every ADR that deferred to
M6), **Scope** (the in / out split, verbatim from the register's
Assumptions), the **invariants table** C-M6·1…C-M6·11, the **FACES
F1–F12** table, and the **D1–D11 decisions** in **[PROPOSED]** state.
**No code in this unit** — prose + tables only.

**Entry reads (≤ 5 files):**
- **`docs/plans-milestones/plan-m6-notifications.md`** — the lane
  register: the Understanding / Assumptions sections (the scope in/out
  split + the deferred list to restate), the invariants C-M6·1…C-M6·11
  (copy into the design doc verbatim, with the D# cross-references),
  and the FACES F1–F12 (copy verbatim). This is the *source*; the
  design doc is its *authoritative rendering*.
- **`docs/ARCHITECTURE.md`** — the §0 value-chain row for M6
  ("shared awareness — the platform reaches out to the resident where
  they are"), the §3 bounded-context layout (where
  `Kumunita.Core.Notifications` will slot), and the §6.2
  best-effort-audience-notification row (the email-is-nudge posture
  the design doc cites).
- **`docs/adr/0061-email-and-notification-language.md`** — the
  per-recipient outbound-channel language resolution the design doc
  inherits verbatim (the §Context quote: "a resident may keep the UI
  in English but want reminders in German").
- **`docs/adr/0075-*.md`** (the current highest ADR) — the ADR house
  style (Status / Context / Decision / Consequences sections) to
  match when U01 authors ADR 0076.

**Deliverables (closed set):**
- **`docs/design/m6-notifications-design.md`** (new file) —
  **Part 1 only:**
  - **§1 Context** — one paragraph on the shared-awareness arrow
    (the ARCHITECTURE.md §0 row, quoted); one bullet list of every
    ADR that deferred a notification to M6 (ADR 0013, ADR 0054,
    ADR 0063 / 0064, ADR 0067, the GU/GA lanes, M3b report
    file/assign/resolve, M4 §6.4 reminder) and what M6 settles for
    each; the one rule inherited from ADR 0061 (the
    per-recipient-email-language resolution is *inherited*, not
    re-invented).
  - **§2 Scope** — the **in** set (the two documents, the one
    surface, the one service, the one controller, the views, the
    bell, the `kw-l` keys, the pinned tests, the gate) and the
    **out** set (the deferred list, each named: PWA push → M9,
    digests, per-kind inbox filters, group/community todo
    assignment, guardian notification *still deferred*, read
    receipts, federation) — verbatim from the register's
    Assumptions.
  - **§3 Invariants** — the table C-M6·1…C-M6·11 (the invariant
    text, verbatim from the register, each with its D# cross-ref).
  - **§4 FACES** — the table F1–F12 (the FACE text + the C-M6·#
    pins, verbatim from the register).
  - **§5 Decisions (D1–D11)** — each decision as a
    **[PROPOSED]** statement (the D# ↔ C-M6·# mapping, one
    paragraph each: what the decision pins, what it forbids, the
    precedent it reuses).
  - **§6 Placeholder headers** (empty, for U01): **§6.1 Document
    shapes**, **§6.2 Service surface**, **§6.3 Idempotency keys**,
    **§6.4 Pinned tests**, **§6.5 Acceptance gate**, **§6.6
    Drift-guard**. *(U01 fills these.)*
- **Nothing else.** No `.cs`, no `.cshtml`, no `.ts`, no ADR, no
  README / `Milestones.cs` touch.

**Exit:**
- `docs/design/m6-notifications-design.md` exists; §1–§5 are
  complete; §6 has the six placeholder headers; the D# ↔ C-M6·#
  mapping is 1:1 and complete (eleven each).
- The register's invariants + FACES are **verbatim** in the doc
  (a word-for-word copy is the point — the register is the source
  of truth until ADR 0076 is Accepted by U01).
- Append **`## U00`** to
  `docs/plans-milestones/in-progress/notifications/notifications-handoff-notes.md`
  (one short section: what Part 1 pins, the one thing the next
  agent must not change).
- Move **`in-progress/notifications/U00.md` →
  `done/notifications/U00.md`** (create the `done/notifications/`
  folder if absent).

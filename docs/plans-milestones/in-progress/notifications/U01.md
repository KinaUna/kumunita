# U01 — design doc **Part 2** (exact shapes, keys, tests, gate) + **ADR 0076** sign-off + roadmap trio confirmation

**Goal:** author **Part 2** of
`docs/design/m6-notifications-design.md` (§6.1–§6.6 filled — the
**exact C# shapes** of `Notification`, `NotificationPreference`,
`NotificationKinds`, `NotificationService`, the idempotency-key
shapes, the **pinned 12 test names**, the three-test acceptance
gate, and the drift-guard); author **ADR 0076**
(`docs/adr/0076-notifications-inbox-and-recipient-email.md`) as
**Accepted** (the decisions source); and **confirm** the roadmap
trio on `Milestones.cs` + README + `MilestonesTests.cs` (M5
`StatusDone`, M6 `StatusNext`, M7 `StatusPlanned` — already the
case; confirm, do not move).

**Entry reads (≤ 5 files):**
- **`docs/design/m6-notifications-design.md`** (§1–§6 placeholder
  headers, authored by U00) — the Part-1 text to build on.
- **`docs/plans-milestones/plan-m6-notifications.md`** — the
  register: the Assumptions (the document shapes, the service
  surface, the idempotency-key shape, the 12 FACES) — the *exact*
  source for §6.1–§6.4.
- **`docs/adr/0061-email-and-notification-language.md`** — the
  ADR house style + the resolution M6 inherits (ADR 0076 cites it
  verbatim, not paraphrased).
- **`src/Kumunita.Core/Projects/ProjectService.cs`** (lines 1–80,
  the ctor + `IProjectService` shape) and
  **`src/Kumunita.Core/Projects/Project.cs`** (or `TodoItem.cs`) —
  the **M5 service-shape precedent** (the `sealed class
  ProjectService : IProjectService` + the Marten POCO shapes — the
  `string Id` + the nullable-list convention) to match in §6.1/§6.2.
- **`src/Kumunita.Web/Milestones.cs`** + **`tests/Kumunita.Web.Tests/MilestonesTests.cs`** — the
  roadmap trio to confirm (the test pins the exact order + the
  single-in-progress milestone — the U01 check is "is the trio
  already M5-Done / M6-Next / M7-Planned?").

**Deliverables (closed set):**
- **`docs/design/m6-notifications-design.md`** (§6 filled):
  - **§6.1 Document shapes** — the exact C# for the **two
    documents** + the `NotificationKinds` static class (the nine
    `public const string` members, the M5 `KanbanStatuses` shape —
    `sealed record Notification { string Id; string RecipientId;
    string Kind; string? SourceId; string? Body; DateTime Created;
    DateTime? ReadAt; ... }`, `sealed record
    NotificationPreference { string RecipientId;
    IReadOnlyList<string>? KindsEnabled; DateTime Updated; ... }`,
    `public static class NotificationKinds { public const string
    PostReply = "post.reply"; ... }` — the *exact* field names +
    types + nullability, the M5 `TodoItem` / `KanbanBoard`
    precedent: `string Id`, conventional POCOs, the
    `IReadOnlyList<string>?` nullable-list shape).
  - **§6.2 Service surface** — the exact C# for
    `sealed class NotificationService` (the ctor: `IDocumentStore
    store, IUserInfoService userInfo, ITranslationProvider
    translator, IMailerStage mailer` — the frozen seams, no ADD),
    the **six public methods** (`EmitAsync`,
    `ListInboxAsync`, `CountUnreadAsync`, `MarkAllReadAsync`,
    `GetPreferencesAsync`, `SetPreferencesAsync`) with their
    **exact signatures** (parameter order, types, nullability,
    return types — the `Task<Notification>` / `Task<IReadOnlyList<Notification>>`
    / `Task<int>` / `Task<NotificationPreference>` shapes), and the
    **C3 same-transaction** pin (the `EmitAsync` calls
    `mailer.StageAsync` in the caller's session, the M1
    `verify:{userId}:{attempt}` shape).
  - **§6.3 Idempotency keys** — the **eight** key shapes
    (verbatim from the register:
    `notification:post.reply:{replyId}`,
    `notification:group.post:{postId}`,
    `notification:event.rsvp:{rsvpId}`,
    `notification:event.reminder:{eventId}:{date}`,
    `notification:report.filed:{reportId}`,
    `notification:report.assigned:{reportId}`,
    `notification:report.resolved:{reportId}`,
    `notification:todo.assign:{todoId}`) + the re-emission-is-a-
    no-op pin (the `IMailerStage.StageAsync` dedup guarantee is the
    *sole* mechanism).
  - **§6.4 Pinned tests** — the **12 test names** (the FACES
    F1–F12, one per test: `Emit_PostReply_Stores_InboxRow_And_Stages_Email_For_Author`
    … the exact `Method_Face_Expectation` shape, the
    `Kumunita.Core.Tests.NotificationServiceTests` class) + the
    **Web controller pins** (the 5 route pins: `GET
    /notifications` → 200 + the 50-row cap;
    `GET /notifications/unread-count` → the JSON `{ "count": N }`;
    `POST /notifications/mark-all-read` → 302 + the `ReadAt` set;
    `GET /notifications/preferences` → 200 + the toggles;
    `POST /notifications/preferences` → 302 + the
    `KindsEnabled` update) + the **three-test acceptance gate**
    (the `closed-loop` / `handoff` / `part-vs-whole` shapes, the
    M4 / M5 gate precedent — the U10 recording).
  - **§6.5 Acceptance gate** — the three test shapes (the
    closed-loop: the F1 face end-to-end; the handoff: the
    F9 + F10 faces — the preference + re-emission; the
    part-vs-whole: the F11 + F12 faces — the no-audit-row +
    the recipient-language pins) + the **U10 recording**
    (the gate result appended to the handoff note, the
    `in-progress/notifications/` → `done/notifications/`
    folder move).
  - **§6.6 Drift-guard** — the rule: if any unit's entry reads
    reveal the design doc is out of date (a shape changed, a
    seam was re-shaped), the unit pauses and records
    `## U<m> — Drift pause` in the handoff note (the M4 / M5
    drift-guard precedent — the *sole* mechanism for a mid-lane
    doc fix, the register's unit-series rule 9).
- **`docs/adr/0076-notifications-inbox-and-recipient-email.md`**
  (new file) — the **Accepted** ADR: the **Context** (the
  shared-awareness arrow + the deferred list, the ADR 0061
  inheritance), the **Decision** (the D1–D11 statements,
  Accepted — the register's invariants, the M5 ADR 0067
  house style), the **Consequences** (the one new context +
  the one new surface + the one new service + the one new
  controller; the **no-ADD** pin — no new `AccessAction`,
  no new `AccessVia`, no new adapter, no new email mechanism;
  the **reuse** pin — the M1 trio + ADR 0061 + `kw-dt` +
  `KnownTranslationKeys` + `client/lib` TS).
- **`src/Kumunita.Web/Milestones.cs`** — **confirm** (not
  move): M5 `StatusDone`, M6 `StatusNext`, M7 `StatusPlanned`.
  **If the trio is already correct, no edit** (the U01 check
  is a *confirmation*, not a *change* — the M5 close
  (ADR 0067) already moved M6 to `StatusNext`).
- **`README.md`** — **confirm** the Roadmap row (M6
  Notifications, "In progress"). No edit if already correct.
- **`tests/Kumunita.Web.Tests/MilestonesTests.cs`** — **confirm**
  the trio pins (the test asserts M5-Done / M6-Next / M7-Planned;
  no edit if the trio is already the pinned shape).
- **Nothing else.** No `.cs` service, no controller, no views,
  no `.ts`, no test.

**Exit:**
- `docs/design/m6-notifications-design.md` §6.1–§6.6 are
  complete; the C# shapes are **exact** (a later unit can copy
  them verbatim into a `.cs` file); the 12 test names + the
  5 route pins + the 3 gate tests are **named** (the U09
  unit codes against these names, not a re-derivation).
- `docs/adr/0076-notifications-inbox-and-recipient-email.md`
  exists, **Accepted**; the D1–D11 ↔ C-M6·1…C-M6·11 mapping is
  1:1.
- The roadmap trio (M5-Done / M6-Next / M7-Planned) is
  **confirmed** on `Milestones.cs` + README +
  `MilestonesTests.cs` (a *confirmation*, not a change — if
  any of the three is *not* the pinned shape, the unit pauses
  and records a drift-pause).
- Append **`## U01`** to the handoff note.
- Move **`in-progress/notifications/U01.md` →
  `done/notifications/U01.md`**.

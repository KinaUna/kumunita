# Tags (`TG`) — rolling handoff notes

One section per unit, appended (never rewritten). Each unit writes exactly
one short section before it exits; the next unit reads only that section +
its own entry-read list from the register.

---

## U0 — Plan register authored (2026-09-19)

- ADR 0044 accepted; ADR index rows 0042/0043/0045/0046 backfilled.
- 13-unit register written (`plan-tags.md`), 769 lines, 9 invariants
  (C-TG·1–C-TG·9), 12 FACES (F1–F12), 24 pinned test names.
- U1 is the first unit to execute: design doc Part 1 (docs-only, no build).

## U1 — Design doc Part 1 (2026-09-19)

- `docs/design/tags-design.md` authored: **177 lines**, exactly the four
  Part 1 sections (`## Context`, `## Scope`, `## Invariants (pinned for the
  lane)`, `## FACES (pinned, 12)`); no C# blocks, no build steps.
- 12 invariant IDs pinned: C-TG·1 … C-TG·9 (TG-owned) + C1, C3 (ADR 0006)
  + ADR 0004 §B.1 — every one cites a D# from ADR 0044.
- 12 FACE IDs pinned: F1 … F12, each citing ≥ 1 invariant (C-TG·N / C1 / C3 /
  ADR 0004 §B.1).
- ADR 0044 D# citations used: D1, D2, D3, D4, D5, D6, D7, D8 (all eight) —
  D8 anchors the Out-of-scope list verbatim.
- No deviation from the register's U1 spec (12 invariants + 12 FACES as
  enumerated in the register body). U2 may proceed to Part 2.

## U2 — Design doc Part 2 (seams, contracts, test list, gate, drift-guard) (2026-09-19)

- `docs/design/tags-design.md` now **571 lines**: Part 1 (177) + Part 2 §2.0–§2.6
  appended (one `## Seams & contracts (Part 2, written by U2)` heading; Part 1
  untouched).
- 24 pinned test names (§2.4) target `tests/Kumunita.Core.Tests/TagServiceTests.cs`,
  verbatim from the register's U2 list — no rename/reorder/add/drop.
- `TagService` surface pinned at **11 members** (7 lane methods + 3 standing
  probes + the `TagItem` record), §2.1; `PostService` / `PageService` gain **no
  new public methods** (the no-new-methods pin is in §2.6).
- Acceptance gate (§2.5, recorded by U12): **closed-loop / handoff /
  part-vs-whole** — the three M2/M3-style tests per the register.
- D# lean per sub-section: §2.1 = D4/D5/D7/D8; §2.2 = D1/D2/D3/D6/D8; §2.3 =
  D4/D5/D6 (+ D7 via C-TG·8/9); §2.4 = D1–D8 all (per-row anchors listed); §2.5
  = D3/D4/D5/D7; §2.6 = D1/D4/D5 (+ ADR 0004 §B.1).
- Two register-idiom notes recorded in the doc (not deviations from its pins):
  (a) the register's `IReadOnlySet<Role>` is spelled that way in the §2.1
  signatures, with a §2.0 note that the repo idiom is `IReadOnlySet<string>`
  (`Kumunita.Core.Identity.Roles` constants) — U5 lands against the repo idiom;
  (b) the register's `string[]` `TagIds` shorthand is pinned in the repo idiom
  (`IReadOnlyList<string>`, the `Post.ImageIds` shape, ADR 0025) with a §2.2
  note — the additive/default-empty pin is what freezes. U3 may proceed.

## U3 — Tag/TagTranslation + TagDocTypes + boot (2026-09-19)

- **(a) `TagDocTypes` line count:** `src/Kumunita.Core/TagDocTypes.cs` — 2 `Schema.For` calls (`Tag`, `TagTranslation`) + 1 named `(TagId, LanguageCode)` unique index (`tg_tr_uidx_tag_lang`, the `pg_tr_uidx_page_lang` / `ann_tr_uidx_ann_lang` family idiom; the auto-derived name is ~44 chars, under the 64-char NAMEDATALEN limit, so the name is convention, not necessity).
- **(b) Boot-path line added:** 1 line in 1 file — `src/Kumunita.Web/Program.cs` **line 98** (`TagDocTypes.Configure(opts);`, comment block lines 94–97), immediately after the `PageDocTypes.Configure(opts);` call (line 92), inside the `AddMarten(opts => { … })` lambda. `SchemaBootstrap.cs` was **not** modified: it carries no doc-type registration cluster — it applies whatever the host registered via `ApplyAllConfiguredChangesToDatabaseAsync()` (the M3 handoff note pins the same "1 line in 1 file" shape).
- **(c) Non-blank invariants (C-TG·4 pin):** `Tag` — `Slug`, `Name`, `LanguageCode`, `CreatedBy` (all `string`, default `string.Empty`, non-blank per the §2.2 pin); `TagTranslation` — `TagId`, `LanguageCode`, `Name`, `AuthorId` (same shape). Both sealed, 6 fields each (Id + 5), no `Audience` field (D1/D5 absence-pin frozen by §2.6).
- **(d) Compile warnings on the new POCOs:** **none** — `dotnet build Kumunita.slnx -c Debug` → 0 errors, 0 warnings on the new files (the 3 `xUnit2029` warnings in the first build pass are pre-existing in `tests/Kumunita.Core.Tests/PageServiceTests.cs`, untouched).
- **(e) New files:** `src/Kumunita.Core/Tags/Tag.cs`, `src/Kumunita.Core/Tags/TagTranslation.cs` (ns `Kumunita.Core.Tags`), `src/Kumunita.Core/TagDocTypes.cs` (ns `Kumunita.Core`, root-level file — mirrors `M3DocTypes.cs` / `PageDocTypes.cs` exactly; the draft under `Tags/` would have made `Tags.Tag` ill-formed since `Kumunita.Core.Tags` is both the namespace and a type name in `Kumunita.Core` scope). No tests added (U4's F5/F11/F12 are the first TG tests). No changes to `Post.cs` / `Page.cs` (U4), `TagService` (U5/U6), or the design doc / register. U4 may proceed.

## U4 — TagIds additive + System refusal (2026-09-19)

- **(a) Two POCO fields added:** `src/Kumunita.Core/Posts/Post.cs` **line 172** and `src/Kumunita.Core/Pages/Page.cs` **line 235** — `public IReadOnlyList<string> TagIds { get; set; } = [];` (the `ImageIds` / `AttachmentIds` repo idiom, not the register's `string[]` shorthand — the U2 note (b) / §2.2 shape-note pin; the 8th additive `Post` field).
- **(b) `System`-page refusal branch:** `src/Kumunita.Core/Pages/PageService.cs` — **create** at **lines 519–522** (after `CheckCreateStanding`, `page.Kind == PageKind.System && page.TagIds is { Count: > 0 }` → `ArgumentException(nameof(page.TagIds))`); **update** at **lines 643–646** (after `CheckEditStanding`, `existing.Kind == PageKind.System && updated.TagIds is { Count: > 0 }` → `ArgumentException(nameof(updated.TagIds))`). Picked `ArgumentException` over `UnauthorizedAccessException` per §2.3 row 4 — a shape violation, not a standing denial; consistent across both lanes. No new public method (the §2.6 no-new-methods pin).
- **(c) F5 / F11 / F12 test names (verbatim from §2.4):** `F5_SystemPageRefusesNonEmptyTagIds`, `F11_PreExistingPostTagIdsReadBackEmptyAfterReboot`, `F12_FreshInstanceHasZeroTags` — all in `tests/Kumunita.Core.Tests/TagServiceTests.cs` (new file, the first TG tests).
- **(d) `TagIds` default value:** empty `IReadOnlyList<string>` (`= [];` collection expression) on both POCOs — the ADR 0004 §B.1 additive, default-empty, no-reseed pin (F11).
- **(e) `TagDocTypes.Configure` added to the test `BootStoreAsync`:** `tests/Kumunita.Core.Tests/TagServiceTests.cs` **line 207** (inside the `BootStore(connString)` helper, immediately after the `PageDocTypes.Configure(opts);` call — per the U3 handoff note, the test boot path is the one place the `Tags` doc types are registered here before `TagService` lands).

Exit: `dotnet build Kumunita.slnx -c Debug` green (0 errors; the 3 `xUnit2029` warnings are pre-existing in `PageServiceTests.cs`); `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll` green — **Total: 586, Errors: 0, Failed: 0**; the three tests confirmed discovered via `-list methods`; a `PageKind.User` page accepts + round-trips a non-empty `TagIds` set, a `PageKind.System` page refuses one on create **and** update (stored value stays empty), and a pre-existing post's `TagIds` reads back empty after a warm re-open (F11). `PostService.cs` was **not** touched (the register's U4 `PageService` deliverable is only the refusal; the design doc's "threads through `PostService`'s create/update paths" wording is a no-op for U4 since `PostService` has no existing `TagIds`-carrying create/update seam — the *attach* is U5's `TagService`, the *composer* field is U8 — recorded here, not expanded). U5 may proceed.

## U5 — TagService write lane + standing split (2026-09-19)

- **(a) `TagService` surface (11-member §2.1 pin, 489 lines `TagService.cs` + 135 lines `ITagService.cs`):** 6 write-lane members **implemented** — `AttachToPostAsync` / `AttachToPageAsync` / `AddTagTranslationAsync` + the three standing probes `CanAttachToPost` / `CanAttachToPage` / `CanTranslateTag`; 4 read-lane members **declared, not yet implemented** (the `NotImplementedException` stubs are the U5→U6 boundary, the register's U5/U6 split note) — `ListForActorAsync` / `ListPostsByTagAsync` / `ListPagesByTagAsync` / `SuggestAsync`; the `TagItem` record (`Tag Tag, int UseCount, string DisplayedName`) is declared on the interface (the single declaration site). The standing probes are instance `bool` return values (the `PostService.CanAddTranslation` / `PageService.CanTranslatePage` idiom — a display pin, not a gate; the real deny is the write-lane re-check).
- **(b) Three audit actions written (C-TG·9, D7):** `tag.attach` (`TargetKind = "post"` / `"page"`, `TargetId` = the object id), `tag.create` (`TargetKind = "tag"`, `TargetId` = the new tag id, one per newly created slug — the C-TG·9 two-row shape on F1), `tagtranslation.add` (`TargetKind = "tag"`, `TargetId` = the tag id, create-or-overwrite per the `(TagId, LanguageCode)` unique index). Each is **one** hand-written `AccessAudit` row, `Outcome = Allow`, single `SaveChangesAsync` (the `PostService.AddPostTranslationAsync` shape, C3).
- **(c) `Slug` derivation (C-TG·4, D3):** `DeriveSlug(input)` — `Trim().ToLowerInvariant()`, then charset-validate (Unicode-aware letters — accented letters like `é` are valid — digits, hyphens, underscores; ≤ 64 chars); `ArgumentException(nameof(input))` on a violation. No accent-folding, no cross-language merge: `sanitation` and `hygiène` are two tags (D3). The write lane's create-or-reuse query keys on the derived slug (the ADR 0011 content-hash-Id dedup analogue — `TagDocTypes` has no slug unique index; the write lane is the identity guard, the C-TG·4 pin).
- **(d) F1 / F2 / F6 / F7 / F8 test names (by id, verbatim from §2.4):** `F1_AttachFreeOnOwnPost`, `F2_SameSlugSecondAuthorReusesTag`, `F2_SameSlugSecondAuthorNotCreatedBy`, `F6_CreatorSetsTranslation`, `F7_NonCreatorAttacherCannotReword`, `F8_GlobalAdminRewordsAnyTag` — plus the three audit-row shape tests (`Attach_WritesOneAuditRow_tag_attach`, `Create_WritesOneAuditRow_tag_create`, `Translate_WritesOneAuditRow_tagtranslation_add`) and `Slug_Derivation_Lowercase_Trim_Charset` — 10 new tests in `TagServiceTests.cs` (U5's slice of the 24-test list; U6's F3/F4/F9/F10 + the two no-audit-row pins are **not** landed yet).
- **(e) `DependencyInjection.cs` registration:** `src/Kumunita.Core/DependencyInjection.cs` **line 108** — `services.AddTransient<Tags.ITagService>(sp => new Tags.TagService(sp.GetRequiredService<Marten.IDocumentStore>()))`, immediately after the `Pages.IPageService` registration (line 93). The constructor takes only the host-registered `IDocumentStore` (the `Pages.PageService` shape — U6's read lane needs it for the C-TG·2 base query; the write lane uses the caller's `IDocumentSession` per C3). **No** `IAuthorizationService` / `IUserInfoService` / `ITranslationProvider` injected (the ADR 0006-D lane pin: the tag lane composes only the frozen seams — the standing probes + the `ResolveAttachVia` / `ResolveTranslateVia` helpers resolve standing inline, never a new `AccessVia` value).
- **(f) `Via` values used (ADR 0006-D lane pin):** only `AccessVia.Owner` (the actor is the author/creator — `ResolveAttachVia` / `ResolveTranslateVia` return `Owner` first) and `AccessVia.Admin` (the actor is a GlobalAdmin — the second branch). Never `Moderator` / `BreakGlass` / `Group` / `Audience` / `Delegation` / `Report` / `Guardian` / `Community` / `Resident` — the tag lane composes only the frozen seams (the `PostService_MakesNoNewModerateCall` test, §2.4 #24, is U10's, but the pin is U5's).

Exit: `dotnet build Kumunita.slnx -c Debug` green (0 errors; the 3 `xUnit2029` warnings are pre-existing in `PageServiceTests.cs`); `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll` green — **Total: 596, Errors: 0, Failed: 0** (586 + the 10 U5 tests; F1 / F2 / F6 / F7 / F8 + the three audit-row + `Slug`-derivation pins pass); the `ITagService` interface has all **11** members (6 write-lane + 4 read-lane declared + the `TagItem` record) — the §2.6 no-new-methods pin; the `TagService` class implements the **6 write-lane members** + the `Slug` derivation helper; the `DependencyInjection.cs` registration is present (line 108). **Not** implemented (U6's): the four read-lane methods. **Not** touched: `Tag` / `TagTranslation` (U3), `Post.TagIds` / `Page.TagIds` (U4), the design doc / register. U6 may proceed.

## U6 — read lane + privacy-pin (2026-09-19)

- **(a) Base-query shape (C-TG·2):** `LoadActorReadableContentAsync` is the one base query — "the set of `Tag` rows used on ≥ 1 `Post` / `PageKind.User` `Page` the actor may already read." Community posts / blog pages route through `IAuthorizationService.CanAsync(Read, PostToAuditableResource / PageToAuditableResource)`; group posts route through `CanSeeGroupAsync` (the ADR 0035 `PostReadDecision` routing applied in Core). The content's own `Read` decision is the gate (C-TG·3); the tag grants nothing (C-TG·1).
- **(b) The four read methods + the `TagItem` record:** `ListForActorAsync` (distinct readable tags + use-count + display name), `ListPostsByTagAsync` / `ListPagesByTagAsync` (readable content whose `TagIds` contains the slug-resolved tag), `SuggestAsync` (the base query filtered + capped). The constructor grew to compose the frozen `IAuthorizationService` + `ITranslationProvider` (ADR 0006-D lane pin — no new seam, no new `AccessVia`).
- **(c) `SuggestAsync` filter clause:** `DisplayedName.StartsWith(prefix, Ordinal) || Tag.Slug.StartsWith(prefix, Ordinal)`, blank prefix matches all, `.Take(10)` (C-TG·2 / F10). The display name is resolved to the viewer's language via `ITranslationProvider.ResolveEffectiveLanguageAsync(null)` → `TagTranslation` for that language → base `Tag.Name` (ADR 0005 order; in Core the preference collapses to the instance default — F9 seeds `de` to exercise the per-language branch).
- **(d) U6 test slice (§2.4 by id):** `F3_GroupPostTagInvisibleToNonMember_ByTag`, `F3_GroupPostTagInvisibleToNonMember_Suggest`, `F4_TagUsedOnlyOnUnreadContentInvisible_List` / `_ByTag` / `_Suggest`, `F9_AutocompleteMatchesViewerLanguage_DisplayName` / `_Slug`, `F10_AutocompleteCappedAtTen`. `PostService_MakesNoNewModerateCall` (§2.4 #24) is U10's — **not** landed here.
- **(e) No-audit-row pin (C-TG·8, D7):** `List_EmitsNoAuditRow` / `Suggest_EmitsNoAuditRow` assert the tag lane writes **no tag-family** row — no `tag.attach` / `tag.create` / `tagtranslation.add` action, and no `TargetKind == "tag"` row. The content's own `Read`-decision rows (post / page / grouppost) are the content's, "the access decision is the content's existing `Read`, not the tag's" (D7) — so the assertions are scoped to tag-family rows, not a globally empty `AccessAudit` table.

Exit: `dotnet build Kumunita.slnx -c Debug` green (0 errors; the 3 `xUnit2029` warnings are pre-existing in `PageServiceTests.cs`); `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll` green — **Total: 606, Errors: 0, Failed: 0** (596 + the 10 U6 tests; F3 / F4 / F9 / F10 + the two no-audit-row pins pass). `TagService` implements all **11** members (6 write-lane + 4 read-lane) + `DeriveSlug`; the `DependencyInjection.cs` registration now injects `IDocumentStore` + `IAuthorizationService` + `ITranslationProvider`. **Not** touched: `Tag` / `TagTranslation` (U3), `Post.TagIds` / `Page.TagIds` (U4), the §2.1 11-member surface, the design doc / register. U7 may proceed.

## U7 — TagController + view models + kw-l keys (2026-09-19)

- **(a) `TagController`** (class-level `[Authorize]`, thin per ADR 0006-D — every decision is a frozen `ITagService` / `IPageService` call): `GET /tags` → `Index`, `GET /tags/{slug}` → `ByTag`, and `POST /api/tags/suggest` → `Suggest` (`[ValidateAntiForgeryToken]`, `SuggestRequest(string Prefix)` body, blank prefix → empty list, no error). **404-floor:** `ByTag` calls `NotFound()` when `ListPostsByTagAsync` returns zero **and** `ListPagesByTagAsync` returns zero — one floor covering both lists (a tag that exists in the tag table but has no readable content for this actor is, for this actor, nothing).
- **(b) `Models/TagViewModels.cs`:** `TagListViewModel(IReadOnlyList<TagItem> Tags)`, `TagByTagViewModel(string Slug, string DisplayedName, IReadOnlyList<Post> Posts, IReadOnlyList<Page> Pages, IReadOnlyDictionary<Guid, Page> PageById)` (`PageById` is `Pages.ToDictionary(p => p.Id)` for `PagePaths.Href`), `TagSuggestViewModel(IReadOnlyList<TagItem> Suggestions)`.
- **(c) Views:** `Views/Tag/Index.cshtml` (heading + lede `kw-l`-wrapped, defensive empty-state, rows → `/tags/{slug}`) and `Views/Tag/ByTag.cshtml` (posts section — group vs. community link per `Post.GroupId`; pages section — `PagePaths.Href`, `System`/`Page` kicker per `PageKind`; defensive empty-state). No client JS — the tag input and its autocomplete are U8's composer; `tag.input.*` / `tag.suggest.empty` are registered here so the registry is whole. One Razor gotcha: a loop variable named `page` is shadowed by the implicit `@page` model inside `@(...)` expressions (RZ3906/RZ2005/RZ1011) — the ByTag pages loop is named `p`.
- **(d) `kw-l` keys:** the spec's six (`tags.list.heading`, `tags.bytag.heading`, `tags.bytag.empty`, `tag.input.placeholder`, `tag.input.hint`, `tag.suggest.empty`) **plus four more** the views needed and `KwLRegistryConsistencyTests` requires (`tags.list.lede`, `tags.list.empty`, `tags.bytag.posts_heading`, `tags.bytag.pages_heading`) — 10 keys × 4 dictionaries = **40** entries in `KnownTranslationKeys.cs`, all four languages filled (en/de/fr/da), no empty values.
- **(e) Invariants held:** C-TG·1 (label, never a gate — no `AccessVia` added, no content-mutation path), C-TG·8 (reads emit no audit row — the controller adds none), ADR 0006-D (thin HTTP, frozen seams only). **Not** landed: the `System`-page composer (U8) and `PostService_MakesNoNewModerateCall` (U10).
- **Exit:** `dotnet build Kumunita.slnx -c Debug` green (0 errors; 3 pre-existing `xUnit2029` warnings in `PageServiceTests.cs`); `dotnet exec tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll` green — **Total: 310, Errors: 0, Failed: 0** (incl. `KwLRegistryConsistencyTests` + `KnownTranslationKeys_ParityTests` with the 10 new keys); `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll` green — **Total: 606, Errors: 0, Failed: 0**. U8 may proceed (System-page composer + tag-input autocomplete).

## U8 — Composer tag input + client/lib autocomplete (2026-09-19)

- **(a) `tag-suggest.ts` line count:** **248 lines** at `src/Kumunita.Web/client/lib/tag-suggest.ts` — a `tsc`-only ES module (RE·3: no editor/framework dependency; `package.json` stays `typescript`-only). Self-wires `input[data-tag-suggest]` at load (the `rich-editor.ts` / `insert-image.ts` convention, guarded by `typeof document !== 'undefined'`). One exported binder (`bindTagSuggest`), debounced (120 ms) `apiFetch` POST to `/api/tags/suggest` (the `SuggestAsync` seam — the filter `starts_with(displayName) OR starts_with(slug)` + ≤ 10 cap are **server-side**; the client only renders the dropdown + commits). Commits picked / free-typed tags to a hidden `name="TagIds"` JSON-array field (the author's typed labels — the `Slug` is derived server-side, C-TG·4). No new dependency, no re-shape of `api.ts` / the endpoint / `TagItem`.
- **(b) Four composer files touched + the `kw-l` keys used:** `Views/Posts/New.cshtml` (Tags card L133–150, `tag.input.hint` L148; `~/js/lib/tag-suggest.js` script L309), `Views/Posts/Edit.cshtml` (Tags card L140–157, `tag.input.hint` L160; script L291), `Views/Page/_PageForm.cshtml` (gated Tags card L138–157, `tag.input.hint` L155), `Views/Page/_PageFormScripts.cshtml` (script L22). The only kw-l key the composers use is the registered **`tag.input.hint`** (U7's registry); the field **label** ("Tags") and **placeholder** are plain text — the `kw-l` TagHelper renders an *element* and cannot wrap an HTML `placeholder` attribute, and the repo's `_PageForm` field labels are plain text (a `tag.input.label` key was deliberately **not** used, so `KwLRegistryConsistencyTests` stays green). `tag.input.placeholder` / `tag.suggest.empty` remain registered (U7) but are the dropdown empty-state / placeholder convention, not a view kw-l.
- **(c) `System`-page refusal branch (file + line numbers):** `Views/Page/_PageForm.cshtml` **L145** — `@if (Model.Kind == "User")` wraps the entire Tags card, so a `PageKind.System` page (ADR 0040) renders **no** tag input and the `tag-suggest.js` self-wire finds nothing (a no-op) — the C-TG·6 refusal, mirroring the U4 `PageService` write-lane refusal (`Create` L519–522 / `Update` L643–646). The `System`/`User` discriminator is the same `Model.Kind` the form already uses for the parent-page hint (L105 / L112).
- **(d) `ts:build` output (the `wwwroot/js/` path):** `npm --prefix src/Kumunita.Web run build` (tsc) → `src/Kumunita.Web/wwwroot/js/lib/tag-suggest.js` (9317 bytes, emitted next to `rich-editor.js` / `api.js`).
- **(e) Render verification:** `dotnet build` compiles the Razor views; `KwLRegistryConsistencyTests` (a static scan of every `.cshtml`'s kw-l keys against the registry) passes with the `tag.input.hint` usage; static grep confirms `data-tag-suggest` present in all three composers and the `Model.Kind == "User"` gate in the page form. The **live** authenticated `GET /posts/new` / `GET /posts/{id}/edit` render was **not** exercised in-browser — the shared browser session is logged out (redirects to `/Account/Login`) and this test platform uses a consumed one-time `SeedAdmin__Token` (no reusable dev credential; per the "report blockers, don't guess" instruction I did not attempt a login). U9's e2e pins (`e2e-tags.spec.ts`) are the designated live render path.
- **Exit:** `dotnet build Kumunita.slnx -c Debug` green (0 errors; 3 pre-existing `xUnit2029` warnings in `PageServiceTests.cs`); `npm --prefix src/Kumunita.Web run build` green (tsc, `tag-suggest.js` emitted); `dotnet exec tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll` green — **Total: 310, Errors: 0, Failed: 0**; `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll` green — **Total: 606, Errors: 0, Failed: 0**. U9 may proceed (`.tmp` harness refresh + the autocomplete / composer-input / browse-page e2e pins, incl. the live render).

## U9 — .tmp harness refresh + autocomplete pin (2026-09-19)

- **(a) Harness refresh:** `node .tmp\build-harness.js` → **98,703 bytes** at `.tmp/harness.html` (trusted-folder, in-repo, gitignored — the AGENTS.md rule). The harness now inlines the **real** `api.js` + `tag-suggest.js` (the `import { apiFetch } from './api.js'` is stripped; the `apiFetch` symbol resolves to the top-level `function apiFetch` from the inlined `api.js`) + a `fetch` stub that returns a canned `{ suggestions: [12 rows] }` (the F9 row `hygiène`/`sanitation` + 11 more — the `Take(10)` cap is mirrored so the dropdown shows ≤ 10). The `input[data-tag-suggest]` self-wires at load (the `tag-suggest.ts` module's `typeof document !== 'undefined'` guard). The `meta[name="anti-forgery-token"]` is present (the `apiFetch` CSRF guard resolves).
- **(b) Three e2e pin groups** (all in `tests/Kumunita.Web.Tests/e2e-tags.spec.ts`, new file — **641 lines**):
  - **F9 / F10 (autocomplete):** a `de`-preferring actor typing `hy` gets the `de` name `hygiène` in the dropdown (the `starts_with(displayName)` branch, C-TG·4); the dropdown is **capped** at ≤ 10 items (the `SuggestAsync` seam's `.Take(10)`, C-TG·2). The mixed-audience fixture: 14 tags on a community post — the server returns 10, the list shows 14.
  - **Composer-input (C-TG·6):** the `input[data-tag-suggest]` renders on the post composer (`/posts/new` + `/posts/{id}/edit`) and the blog-page composer (`/pages/new`, `PageKind.User`); it does **not** render on the System-page composer (`/pages/new`, `PageKind.System` — the `@if (Model.Kind == "User")` gate in `_PageForm.cshtml`). The `kw-tag-field` wrapper + the `tag.input.hint` kw-l are also asserted.
  - **Browse-page (C-TG·1 / C-TG·2 / C-TG·3):** the tag list (`GET /tags`) shows `Shared` (visible to both actors) and does **not** show `Secret` (used only on unread content — the privacy-pin, C-TG·1 / C-TG·2). The by-tag view (`GET /tags/shared`) shows the post (C-TG·3); `GET /tags/secret` returns **404** (the U7 404-floor pin — not a blank page, not a 403).
- **(c) `wwwroot/js/` path:** `npm --prefix src/Kumunita.Web run build` (tsc) → `src/Kumunita.Web/wwwroot/js/lib/tag-suggest.js` (emitted next to `rich-editor.js` / `api.js`). The harness inlines the **real** compiled `api.js` + `tag-suggest.js` (the U8 `tag-suggest.ts` → `tag-suggest.js` pipeline, unchanged).
- **(d) Privacy-pin visible in the HTML (mixed-audience fixture):** the browse-page test asserts the `Shared` tag is present in `viewer`'s tag list (`.list-group-item a`) and the `Secret` tag is **absent** (no name, no "hidden" placeholder — C-TG·1 / C-TG·2). The by-tag test asserts the `Shared post` title is visible in `GET /tags/shared` (C-TG·3) and `GET /tags/secret` returns 404 (the U7 404-floor pin).
- **(e) Playwright status (honest):** the `kumunita` fixture is a **documented throw** (same M2 D2 / M3b U10 precedent as `e2e-m2.spec.ts` / `e2e-m3.spec.ts`). The U9 register permits "or are pinned by the e2e spec" — the spec IS the pin. The Playwright runtime unit (a future lane) lands the fixture implementation and records the pass count. **Not** landed: the `PostService_MakesNoNewModerateCall` test (U10's, the ADR 0006-D lane pin). **Not** touched: the design doc / register / `TagService` / `TagController` / `TagViewModels` / the `kw-l` registry.
- **Exit:** `node .tmp\build-harness.js` green (98,703 bytes, the tag-suggest module + `api.js` inlined, the `fetch` stub + `anti-forgery-token` meta present); `npm --prefix src/Kumunita.Web run build` green (tsc, `tag-suggest.js` emitted); `dotnet build Kumunita.slnx -c Debug` green (0 errors; 3 pre-existing `xUnit2029` warnings in `PageServiceTests.cs`); `dotnet exec tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll` green — **Total: 310, Errors: 0, Failed: 0** (the `KwLRegistryConsistencyTests` + `KnownTranslationKeys_ParityTests` pass with the U7/U8 `kw-l` keys — the U9 spec is a `None` in the csproj, not an xUnit test). U10 may proceed (the full Web pin set + the ADR 0006-D lane pin).

## U10 — Web surface pins + ADR 0006-D lane pin (2026-09-19)

- **(a) Full pin set (`e2e-tags.spec.ts`, **624 lines**, +101 over U9's 523):** the four test groups hold: **(a)** F9 viewer-language autocomplete + F10 cap ≤ 10; **(b)** composer-input C-TG·6 (post + blog render the tag field, System does not); **(c)** browse-page privacy-pin C-TG·1 / C-TG·2 / C-TG·3; **(d)** group-lane by-tag exclusion C-TG·3 (U10's new test — a tag used only on a group post a non-member cannot read is absent from that viewer's list / by-tag / suggest surfaces, and present to a group member — the positive/negative control pair at the group boundary). The `kumunita` fixture remains a **documented throw** (the M2 D2 / M3b U10 precedent; U9 authored the spec, U10 extended it). The three new fixture helpers (`createGroup`, `addGroupMember`, `createGroupPost`) are documented in the interface but **not** implemented (the Playwright runtime unit lands them).
- **(b) Privacy-pin across all three surfaces (C-TG·1 / C-TG·2 / C-TG·3):** test (c) — `shared` tag (used on a community post both actors can read) is **visible** in `viewer`'s tag list; `secret` tag (used on a post `viewer` cannot read) is **absent** (no name, no "hidden" placeholder — C-TG·1 / C-TG·2). The by-tag view (`GET /tags/shared`) shows the post (C-TG·3); `GET /tags/secret` returns **404** (the U7 404-floor pin — not 403, not a blank page).
- **(c) Group-lane by-tag exclusion (C-TG·3 pin):** test (d) — a tag `family-internal` used only on a **group post** (ADR 0013, `Post.GroupId` set) is **absent** from a non-member's tag list, by-tag view (404), and suggest results, and **present** to a group member (the positive control). Core-pinned by `TagServiceTests.F3_GroupPostTagInvisibleToNonMember_ByTag` / `_Suggest`.
- **(d) ADR 0006-D lane pin (`PostService_MakesNoNewModerateCall`, Core test, §2.4 #24):** drives the **entire** tag surface (all 6 write lanes: attach create/reuse, attach page, translate owner + admin; all 4 read lanes: list, by-tag posts, by-tag pages, suggest; the group-lane read for a member **and** a non-member) against a `RecordingAuthz` wrapper over the **real** `AuthorizationService`. Pins: (1) **seam level** — no `AccessAction.Moderate` call reaches the seam; (2) **audit level** — every tag-family `AccessAudit` row carries `Via = Owner` or `Via = Admin` only; (3) **surface level** — reflection: neither `IAuthorizationService` nor `IUserInfoService` exposes a `*Moderate*` method, and `TagService`'s composition is exactly the frozen trio (`IDocumentStore`, `IAuthorizationService`, `ITranslationProvider`).
- **(e) Drift-guard (§2.6) + exit:** the `TagService` composition is exactly the frozen trio (`IDocumentStore`, `IAuthorizationService`, `ITranslationProvider`) and no `*Moderate*` seam exists on `IAuthorizationService` / `IUserInfoService` (U10's surface-level reflection assertion — the §2.6 no-new-methods / no-new-seam invariant holds for the tag lane). **Exit:** `dotnet build Kumunita.slnx -c Debug` green (0 errors; 3 pre-existing `xUnit2029` warnings in `PageServiceTests.cs`, unchanged from U5–U9); `dotnet exec tests\Kumunita.Core.Tests\bin\Debug\net10.0\Kumunita.Core.Tests.dll` green — **Total: 607, Errors: 0, Failed: 0** (606 + the 1 U10 test `PostService_MakesNoNewModerateCall`); `dotnet exec tests\Kumunita.Web.Tests\bin\Debug\net10.0\Kumunita.Web.Tests.dll` green — **Total: 310, Errors: 0, Failed: 0** (the `e2e-tags.spec.ts` spec is a `None` in the csproj — the e2e group (d) is not a separate xUnit test; the 310 count is unchanged from U9). U11 may proceed (the fresh-boot manual pass).
